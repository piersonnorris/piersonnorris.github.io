/* Generated by tools/obsidian-sync.js --public. Do not edit by hand. */
window.PNVaultPublic = {"format":"pn-vault-public","version":1,"label":"Site docs","generatedAt":"2026-09-08T15:14:10.623Z","linkedPaths":27,"waived":["money"],"count":11,"notes":[{"path":"docs/ASSET_TRACKER_SPEC.md","folder":"docs","title":"ASSET TRACKER — feature spec (public-repo-safe edition)","body":"# ASSET TRACKER — feature spec (public-repo-safe edition)\n\nThe gated portfolio dashboard at `/tools/tracker/`. Adapted from Pierson's full private spec (lives in his Google Drive: \"assettrackingwebsitespec.md\") for a public repo: **all real holdings replaced with sample data, and the spreadsheet ID moved to a secret.** ChatGPT: if you need the real data snapshot or the click-by-click Google Cloud screenshots, ask Pierson to paste from that private doc — never commit any of it.\n\nBuild this at Milestone 4, after the rest of the site works.\n\n---\n\n## 1. What it is\n\nPierson tracks his investment/crypto holdings across five platforms in a Google Sheet (\"Asset Tracking\"), one tab per month. The site shows this data live — but the page is password-gated and the repo/public build never contain readable numbers.\n\n## 2. Architecture (locked)\n\n```\nGoogle Sheet (private)\n   │  read-only, service account (JSON key in Actions secret)\n   ▼\nGitHub Action  — cron (daily) + workflow_dispatch (manual refresh)\n   │  1. fetch tabs matching /^(January|…|December) \\d{4}$/\n   │  2. parse → normalized holdings JSON\n   │  3. inject JSON into the dashboard template\n   │  4. StatiCrypt-encrypt the whole page with TRACKER_PASSWORD\n   ▼\ncommit ONLY tools/tracker/index.html (ciphertext)  →  GitHub Pages serves it\n```\n\nWhy this shape: GitHub Pages is static and the repo is public (free Pages), so a browser can never hold the service-account key and plaintext data can never be committed. StatiCrypt (AES-256, client-side decrypt on password entry) means the published file is safe even though the repo is public. The old alternatives in the private spec (client-side API key, serverless function) are **superseded** by this.\n\n### Secrets & variables (GitHub → Settings → Secrets and variables → Actions)\n\n| Name | Type | Contents |\n|---|---|---|\n| `GOOGLE_SERVICE_ACCOUNT_JSON` | Secret | Full JSON key file contents |\n| `TRACKER_PASSWORD` | Secret | The gate password (Pierson sets it; never appears in chat or code) |\n| `SHEET_ID` | Secret | The spreadsheet ID |\n| `DIVIDEND_CALENDAR_JSON` | Secret | Optional researched dividend data; never commit a holdings-linked calendar |\n\nThe Action is the ONLY place these are read. `.gitignore` already blocks `secrets/` and `*service-account*.json` as a second line of defense for local testing.\n\n### Dividend calendar payload\n\nThe encrypted build may receive `DIVIDEND_CALENDAR_JSON` in this shape. Keep only issuer-confirmed dates; a historical cadence is not a confirmed future payment.\n\n```json\n{\n  \"TICKER\": {\n    \"annual\": 0.00,\n    \"exDate\": \"YYYY-MM-DD\",\n    \"payDate\": \"YYYY-MM-DD\",\n    \"frequency\": \"quarterly\"\n  }\n}\n```\n\nAfter tracker unlock, the dashboard can export the confirmed dates as a `.ics` file for import into the owner's private Google Calendar. The dashboard PIN protects access to the calendar on the site; Google Calendar access remains governed by the owner's Google account.\n\nThe same unlock also synchronizes dividend-paying tickers and a linked `Dividend calendar` Markdown note into the encrypted stock-notes vault. Dividend edits stay in memory until they are written into that vault; they are never stored as readable browser data.\n\n## 3. Source data shape\n\n### 3.1 Tabs\nOne tab per month, named `<Month> <Year>` (e.g. `August 2026`). Discover tabs dynamically via the regex above — never hardcode names. One legacy tab (`July 2026`) uses an older flat layout; skip it in v1 (optionally render read-only later).\n\n### 3.2 Current schema (all tabs from August 2026 onward)\nRow-oriented, five platform sections in fixed order **Sofi → Webull → Robinhood → Gemini → GoMining**:\n\n```\nRow 1   Title:  \"Stock flows — <Month> <Year> update\"   (spans A:D)\nRow 2   Note:   free text                                (spans A:D)\nRow 3   (blank)\nRow 4   Section header:  <Platform> | Amount | Unit | Notes\nRow 5+  Data rows:       <ticker/label> | <number> | Shares|Units|USD | <notes>\n(blank row ends a section; next section header follows)\n```\n\n**Column meanings** — A: ticker or free-text label (\"Cash\", \"Robo (auto-invest sub-account)\", \"NVDA $230 Call (option)\"). B: numeric amount. C: unit — `Shares` (equity count), `Units` (coin count), `USD` (already a dollar figure). D: notes, may be blank.\n\n**Parsing rule:** walk rows after row 3. A row is a section header when col A equals one of the five platform names AND col B = \"Amount\". Rows until the next fully-blank row belong to that platform.\n\n**Mixed units — critical:** you cannot sum column B across rows. Shares, coins, and dollars are mixed. The tracker first classifies each row, then values share and coin counts with baked, manual, or live read-only quotes. Any unpriced row is excluded from the dollar total and identified in the interface.\n\n### 3.3 Normalized model (what the Action emits)\n\n```json\n{\n  \"tab\": \"August 2026\", \"month\": \"August\", \"year\": 2026,\n  \"note\": \"<row 2 text>\", \"fetchedAt\": \"2026-08-23T11:00:00Z\",\n  \"holdings\": [\n    {\"platform\": \"Sofi\", \"label\": \"TICK\", \"amount\": 12.5, \"unit\": \"Shares\", \"notes\": \"\"}\n  ]\n}\n```\n\n### 3.4 Sample data (SAFE — fabricated; use for the template, tests, and the public screenshot)\n\n| Platform | Label | Amount | Unit | Notes |\n|---|---|---|---|---|\n| Sofi | AAPL | 4.2 | Shares | |\n| Sofi | VTI | 10 | Shares | |\n| Sofi | Robo (auto-invest) | 250.00 | USD | robo sub-account |\n| Sofi | SOL (crypto) | 0.8 | Units | |\n| Webull | Cash | 120.00 | USD | no open positions |\n| Robinhood | SPY $500 Call (option) | 95.00 | USD | contract value |\n| Robinhood | VDE | 2.1 | Shares | |\n| Gemini | ETH | 0.05 | Units | |\n| Gemini | SOL | 1.4 | Units | |\n| GoMining | Total value | 15.00 | USD | |\n\n## 4. Platform colors (carry into badges, section accents, chart fills)\n\n| Platform | Hex | Note |\n|---|---|---|\n| Sofi | `#00A2C7` | brand blue |\n| Webull | `#001B39` | brand navy, deliberately darker (Pierson's preference) — on the dark site, pair with a lighter border/text so it stays visible |\n| Robinhood | `#00C805` | brand green |\n| Gemini | `#F5A623` | orange — Pierson's stated preference over Gemini's real teal |\n| GoMining | `#7C3AED` | purple — Pierson's preference |\n\nTint for fills: blend 85% toward the page background rather than white (dark site). Badges: color dot + mono label beats solid fills at small sizes.\n\n## 5. Dashboard UI (inside the gate; site design system applies)\n\n- **Month switcher** — mono tab strip of discovered months, newest active.\n- **Platform sections** — card per platform: color dot + name + holding count; rows: label / amount+unit (tabular-nums, mono) / notes muted.\n- **Header** — \"Fetched <date>\" timestamp from `fetchedAt` + the sheet's own note line.\n- **Valuation summary** — totals only normalized dollar values and clearly identifies unpriced rows. The source label states whether prices are baked, cached, manual, or live.\n- **Stock chart** — charts included equity positions after unlock, with price, volume, range controls, and position context. See §8.\n- **Stock row details** — a right-side disclosure arrow opens an encrypted purchase journal with dated buy lots, derived cost metrics, and an internal note that round-trips through the Obsidian export.\n- Wrapper page (`/tools/tracker/`) carries `<meta name=\"robots\" content=\"noindex\">` and is excluded from sitemap.xml.\n\n## 6. Action workflow — template to adapt\n\n```yaml\nname: refresh-tracker\non:\n  schedule: [{cron: \"0 11 * * *\"}]   # daily ~6am Chicago\n  workflow_dispatch: {}\npermissions: {contents: write}\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: {node-version: 20}\n      - run: npm install googleapis staticrypt\n      - run: node tools/tracker/build.js   # fetch → parse → inject → encrypt\n        env:\n          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}\n          SHEET_ID: ${{ secrets.SHEET_ID }}\n          TRACKER_PASSWORD: ${{ secrets.TRACKER_PASSWORD }}\n      - run: |\n          git config user.name \"tracker-bot\" && git config user.email \"actions@github.com\"\n          git add tools/tracker/index.html\n          git diff --cached --quiet || (git commit -m \"tracker: refresh\" && git push)\n```\n\n`build.js` responsibilities: auth via the JSON in env (write to a temp file OUTSIDE the repo dir or parse in-memory), `spreadsheets.get` for tab list, `values.get` per month tab (`'<tab>'!A1:D200`), run the §3.2 parser, render `index.template.html` (committed, sample data only) with real JSON, then StatiCrypt CLI → overwrite `tools/tracker/index.html`. The template with sample data doubles as the public screenshot source.\n\n## 7. Security checklist (part of M4 acceptance)\n\n- [ ] Repo history contains zero real holdings, zero key material, zero password (grep before merging).\n- [ ] `index.template.html` contains sample data only.\n- [ ] Action logs don't echo secrets (no `set -x`, no printing env).\n- [ ] Wrong password → StatiCrypt prompt again; page source shows ciphertext only.\n- [ ] Sheet shared with the service account as **Viewer** only.\n- [ ] If the key ever leaks: revoke in Google Cloud Console → Credentials, regenerate, update the secret.\n\n## 8. Stock chart roadmap\n\nThe chart experience, provider boundary, accessibility requirements, phased enhancements, and validation criteria are maintained in [STOCK_CHART_PLAN.md](STOCK_CHART_PLAN.md).\n\n## 9. Private portfolio calendar\n\nThe calendar data model, privacy boundary, accuracy rules, Google Calendar handoff, and deferred task-board/data-connection phases are maintained in [PORTFOLIO_CALENDAR_PLAN.md](PORTFOLIO_CALENDAR_PLAN.md).\n\n## 10. Private project task board\n\nThe five-stage goals workflow, encrypted persistence model, Obsidian round trip, and Phase 3 integration boundary are maintained in [PORTFOLIO_TASKBOARD_PLAN.md](PORTFOLIO_TASKBOARD_PLAN.md).\n","tags":[],"created":"2026-09-08T15:14:10.601Z","updated":"2026-09-08T15:14:10.602Z"},{"path":"docs/BACKSTAGE_PLAN.md","folder":"docs","title":"BACKSTAGE — the vault, in public, with no gate","body":"# BACKSTAGE — the vault, in public, with no gate\n\n**Status: P0–P3 built 2026-09-08. P4 (publishing the personal vault) is waiting on Pierce.**\nDrafted and executed the same day, for ROADMAP **R17**.\n\n> **Where this stands.** `/vault/` is live, ungated, and reads a real published snapshot — but the snapshot it\n> currently carries is the site's own `docs/` folder, every file of which was already public in this repo. That\n> was deliberate: it exercises the whole pipe end to end and makes the visual judgeable **before** a single\n> private note is committed. Pointing it at the personal vault is one command (`--vault \"<vault>\"`), and it\n> should not be run until §7's questions are answered — see P4 in §6.\nDecision on record: Pierce, 2026-09-08 — *\"view the website back end Obsidian, no gate to get in, and a good visual.\"*\nAsked which content, he picked **the whole personal vault, ungated** over the two narrower options.\n\n---\n\n## 0. The thing to understand before anything else\n\n**\"No gate\" is not a PIN you delete. The vault is not on the website today.**\n\n`/notes/` is not a viewer pointing at a vault on a server. It is browser-local storage: notes live in\nIndexedDB in *one* browser, sealed with AES-256-GCM under a PBKDF2 key derived from the PIN, and the PIN\nprotects data that never left the machine. Delete the PIN prompt and a stranger opening `/notes/` sees an\nempty page, because there was never anything on the server for them to see. Pierce's own vault sits on his\ndisk; the only way it reaches the browser today is the Import button.\n\nSo the real work is the opposite of removing a lock: **publishing a copy of the vault into the repo** and\nbuilding a reader for it. Every hard question in this plan follows from that one sentence — once the notes\nare in a public GitHub repo they are public permanently, including in git history, including anything a\nlater scrub removes.\n\nSecond thing to know: **the repo's `.gitignore` denies everything by default** (`*`, then an allowlist),\nbecause this folder doubles as Pierce's working vault. That is the single control that has kept the notes\noff the internet for the life of the project. It is also, right now, silently eating a finished feature —\nsee §1.\n\n---\n\n## 1. Standing bug this plan must fix first\n\n`atlas/index.html` — the knowledge atlas built 2026-09-07 (R16), wired into the nav, `sitemap.xml` and\n`llms.txt` on every page — **is gitignored and has never deployed.**\n\n```\n$ git check-ignore -v atlas/index.html\n.gitignore:7:*    atlas/index.html\n```\n\n`/assets/**` is allowlisted, so `atlas.css`, `atlas-data.js` and `atlas-ui.js` show up as untracked and\nwould commit fine; the page they exist for does not. Six pages currently link to a live 404. The allowlist\nnever got an `!/atlas/` pair, the way `!/island/` and `!/notes/` have. The same trap will swallow `/vault/`.\n\n**Fix before anything else ships:** add `!/atlas/` + `!/atlas/index.html` to `.gitignore`, commit the four\natlas files together, confirm `/atlas/` loads on Pages. One line of fix, five minutes of verification.\n\n---\n\n## 2. Current connectors — what already exists\n\nEverything below is built and working. The new surface should add exactly one connector, not replace these.\n\n| # | Connector | Direction | Where it lives | Gate | Reaches the public web? |\n|---|---|---|---|---|---|\n| C1 | **Vault engine** — `PNVault`: `fromMarkdown` / `toMarkdown` / `importBundle`, wikilink resolution, backlinks | in-browser | `assets/js/vault.js` | PIN | No |\n| C2 | **Notes UI** — reader, tag filter, editor, backlinks panel | in-browser | `assets/js/notes-ui.js`, `/notes/` | PIN | No |\n| C3 | **Graph engine** — `PNGraphify.build()` (pure, unit-tested) + `mount()` (force-directed SVG) | in-browser | `assets/js/notes-graph.js` | none itself | Yes, via C6 |\n| C4 | **Vault sync** — walks a real vault folder, calls C1's own parser, emits one `pn-vault-bundle` JSON | disk → file | `tools/obsidian-sync.js` | runs on Pierce's machine | No — output is gitignored |\n| C5 | **Baked route** — `loadObsidianVault()` seals a bundle into the encrypted tracker payload | file → build | `tools/tracker/build.js` | tracker PIN | No (ciphertext only) |\n| C6 | **Atlas** — 25 curated public notes, three-pane workspace, real link graph, timeline | static file | `assets/js/atlas-data.js`, `/atlas/` | **none** | Intended to — blocked by §1 |\n| C7 | **Bottle porthole** — Easter-egg door reporting whether a vault exists *on this device* | in-browser | `assets/js/bottle.js` | key-presence check only | Yes (the door, not the notes) |\n\nTwo facts worth pulling out of that table:\n\n- **C6 is the template.** The atlas already proves the exact shape this plan needs: a public JSON data file,\n  an ungated three-pane Obsidian-style workspace, and a graph that is the *real* link graph of the content\n  above it rather than a decoration. What C6 lacks is a real vault behind it — it is 25 notes typed by hand\n  from [[CONTENT — piersonnorris.com]].\n- **C4 is the missing half.** It already reads the real vault, correctly, using the site's own parser. Its\n  output has simply never been allowed to leave the machine. Publishing is a new *mode* on C4, not a new\n  script.\n\n**Boundary that does not move:** the site is static, on GitHub Pages, with no server and no OAuth. CI cannot\npull the vault — the vault is on Pierce's disk, not behind a portable credential. Publishing is a snapshot,\nproduced by a command he runs, exactly like the Google Calendar pull (R9) and the tracker build.\n\n---\n\n## 3. Where this goes\n\n**A new public surface at `/vault/`, titled \"Backstage\".** Not a change to `/notes/`.\n\n```mermaid\nflowchart LR\n  V[\"Obsidian vault<br/>(Pierce's disk)\"] -->|\"C4 obsidian-sync.js\"| B[\"private/notes/<br/>obsidian-vault.json<br/>gitignored\"]\n  V -->|\"NEW: --public + review gate\"| P[\"assets/data/<br/>vault-public.json<br/>committed\"]\n  B -->|\"Import button\"| N[\"/notes/<br/>PIN, browser-local, editable\"]\n  B -->|\"C5 build.js\"| T[\"/tools/tracker/<br/>PIN, encrypted payload\"]\n  P --> W[\"/vault/ — Backstage<br/>public, read-only, no gate\"]\n  A[\"/atlas/ — curated public\"] -.->|\"same shell\"| W\n  C3[\"C3 PNGraphify\"] -.-> W\n```\n\nThree reasons it is a separate page and not an ungated `/notes/`:\n\n1. **`/notes/` is a writing surface; `/vault/` is a reading surface.** Editing, deleting and exporting only\n   make sense against your own browser storage. A public reader has no business offering them, and merging\n   the two means every future change to either has to reason about which mode it is in.\n2. **The PIN on `/notes/` is still doing real work** — it guards the *editable* local copy, drafts included,\n   on a laptop that gets opened in public. Nothing about publishing a snapshot argues for removing it.\n3. **Two audiences, two front doors.** `/atlas/` answers \"what has he learned\"; `/vault/` answers \"how does\n   he actually work\". Nav gets both; the bottle porthole (C7) finally gets a real destination to open onto,\n   which is what the original Easter-egg idea wanted and could not have.\n\nNaming: **Backstage**, at `/vault/`. \"Backstage\" says *this is the working material behind the finished\npages* without promising it is polished, and it does not collide with the atlas's \"curated\" framing.\n\n---\n\n## 4. The part that decides whether this is safe\n\nPierce's answer was \"the whole vault\", and this plan builds for that. But **\"publish the whole vault\" and\n\"publish it without reading it first\" are different decisions**, and only the first one has been made.\n\nOne flag, plainly, then out of the way: a public repo is permanent and indexed. [[CONTENT — piersonnorris.com]] §7 keeps a\n\"never on the site\" list — client names and addresses, portfolio values and share counts, Screencastify\ninternal figures and vendor names — and `Portfolio Access.md` and the daily notes are exactly where those\nlive today. Publishing without a pass over the text would put on a career-facing site the one category of\nthing the site has deliberately kept off it for its whole life. Third parties (clients, colleagues) also\nappear in those notes without having chosen to.\n\nSo the plan keeps the scope Pierce chose and adds one gate that is **his**, not the reader's:\n\n- **Default is publish.** Whole vault, as decided. No allowlist to maintain, no note quietly missing.\n- **`publish: false` in frontmatter opts a note out** — one line, in Obsidian, where he is already standing.\n  Same for a `#private` tag; a `nopublish/` folder excludes a whole tree.\n- **The scrubber refuses on a hit, it does not silently redact.** Regexes for dollar amounts, street\n  addresses, phone numbers, and the literal names on the [[CONTENT — piersonnorris.com]] §7 list. A hit fails the run and\n  prints `file:line`, so the fix happens in the vault rather than in a filter nobody remembers.\n- **`--report` before the first publish is mandatory, and it is a human read.** Every note that would go\n  public, listed with title, path and word count. Pierce reads that list top to bottom, once. After that,\n  incremental runs report only what is new or changed.\n- **Nothing lands until he says so.** The publish command writes the file; committing it is a separate,\n  deliberate act.\n\nIf the first `--report` comes back and the vault is mostly daily working notes, the honest recommendation\nmay change to \"publish `docs/` plus the notes you'd hand a colleague\". That is a call to make *with the\nreport in hand*, not now.\n\n---\n\n## 5. The visual\n\nDirection: **Slate command center** — Version 1 in `notes/visual-options/`, the dense, dark, keyboard-first\none. It is the closest of the three to real Obsidian, it inherits the site's existing dark palette instead\nof fighting it, and it is the only one of the three that comfortably holds a file tree, a reader, a graph\nand an inspector at once. This also finally closes **R10**, parked on exactly this choice since 2026-09-06.\n\nLayout — the atlas's three-pane workspace shell, reused rather than rewritten:\n\n```\n┌──────────────────────────────────────────────────────────────────────┐\n│  Backstage   ⌕ search…            [ Read ] [ Graph ] [ Timeline ]    │  ← C6's wbar\n├──────────┬────────────────────────────────────────┬──────────────────┤\n│ VAULT    │  # Weekly review                       │  LINKED          │\n│ ▾ docs   │  ─────────────────────────             │  ← 4 backlinks   │\n│   BLUE…  │  Rendered markdown: headings, lists,   │  → 6 outgoing    │\n│   ROAD…  │  code, tables, callouts, and           │  ⚠ 1 unresolved  │\n│ ▾ daily  │  [[wikilinks]] that actually resolve.  │                  │\n│   2026…  │                                        │  TAGS  #systems  │\n│ ▸ stocks │                                        │  local graph ▨   │\n├──────────┴────────────────────────────────────────┴──────────────────┤\n│  47 notes · 112 links · 2.4 per note · 3 orphans · 1 unresolved       │\n└──────────────────────────────────────────────────────────────────────┘\n```\n\nWhat makes it read as Obsidian rather than as a docs site:\n\n- **A real folder tree**, collapsible, mirroring vault paths. The atlas groups by domain, which is wrong for\n  a vault — folders are how Pierce actually organises it.\n- **The graph is the graph.** `PNGraphify` (C3), the same engine `/notes/` and `/atlas/` use, so every edge\n  is a `[[wikilink]]` in the text above it. Tag nodes off on mount, toggleable back. Local-graph mode in the\n  inspector: the selected note plus its immediate neighbourhood.\n- **The inspector earns the third pane** — backlinks, outgoing links, unresolved links, tags. Unresolved\n  links stay visible rather than hidden; on a public page they are honest, and they were R15's whole point.\n- **⌘K command palette**, cursor keys to move, Enter to open, Esc to close. The atlas already binds ⌘K to\n  search; this extends that convention instead of inventing a second one.\n- **Deep links.** `/vault/#docs/BLUEPRINT.md` opens that note. Shareable, and the bottle porthole and the\n  Experience page can point at specific notes.\n- **Honest heavy and empty states.** A vault of a few hundred notes is not 25: the tree virtualises past\n  ~200 rows, and the graph gets a \"showing the largest connected component\" control rather than a hairball.\n\nReused as-is: `site.css` tokens, `notes.css` (`.pg-*` graph styles), the atlas's `.workspace` grid, `nav.js`,\nthe skip link, focus rings (U1/U2/U3), `theme-color`, favicon.\n\n**One genuinely new piece of code: a Markdown renderer.** There isn't one. `/notes/` and `/atlas/` escape the\nbody text and linkify `[[brackets]]` — fine for short curated notes, wrong for [[BLUEPRINT — piersonnorris.com]]. It needs\nheadings, lists, fenced code, tables, blockquotes, inline emphasis and links, callouts and task checkboxes,\nin ~200 lines of plain JS with **escape-first** ordering (escape, then structure — never the reverse), no\ndependency, per standing rule 3. This is the piece most likely to be underestimated.\n\n---\n\n## 6. Moving plan\n\nFive phases. Each one ends somewhere it is safe to stop.\n\n| Phase | What | Status |\n|---|---|---|\n| **P0 — Unblock** | `.gitignore` allowlist for `/atlas/` (and `/vault/`); commit the atlas files; verify it loads | ✅ done — `git check-ignore` was hiding `atlas/index.html`; the page now loads with 25 notes, 30 links, 0 orphans |\n| **P1 — Look before publishing** | `--report`: every note that *would* publish, with path and size, no text | ✅ built. Run against `docs/`, it caught 7 dollar amounts — all cleared-for-publication figures, so `money` was waived deliberately and the bundle records the waiver |\n| **P2 — The publish pipe** | `--public`: `publish:false` / `#private` / `nopublish/` exclusions, refusing scrubber, `--prefix`, `--waive`, writes `assets/data/vault-public.js` | ✅ built, 26 tests in `tools/tracker/vault-publish.test.js` |\n| **P3 — The reader** | `/vault/`, `vault-ui.js`, `vault.css`, `markdown.js`, folder tree, reader, graph, inspector, ⌘K, deep links | ✅ built and exercised in a browser; 24 renderer tests |\n| **P4 — Publish the personal vault** | Point `--vault` at the real vault, read the report, commit the snapshot | ⏸ **waiting on Pierce** — §7 |\n| **P5 — Keep it true** | `--public` in the local rebuild loop (rule 7); the bottle porthole (C7) repointed at `/vault/` | ⏸ after P4 |\n\n**Deliberate change from the drafted plan.** P3 was to be built against a throwaway fixture and P4 was to wire\nthe nav. Instead the first *real* snapshot is the site's own `docs/` folder — 11 notes that were already public\nin this repo — and the nav, sitemap and `llms.txt` are wired now. It gives the same \"judge it before anything\nprivate ships\" property a fixture would have, but the page is honest and useful today rather than a mock, and\nswapping the source is one flag. The irreversible step — committing personal notes — is still untaken.\n\n**Found while building, both worth knowing:**\n\n- **`/atlas/` had never deployed** (§1) — the reason P0 existed, confirmed by `git check-ignore`.\n- **`PNGraphify` counted `[[links]]` written inside code spans.** A note *documenting* the link syntax was\n  inventing edges, and inflating the unresolved count with the vault's own documentation about itself. Fixed in\n  the shared engine with tests, so `/notes/` and `/atlas/` get it too: on the `docs/` snapshot it took the\n  unresolved count from 6 to 0.\n\n**Not built, on purpose:** tree virtualisation past ~200 rows and the graph's \"largest connected component\"\ncontrol (§5). Eleven notes need neither; both become real work the moment P4 lands a few hundred.\n\n**The commit in P4 is the irreversible one.** Everything before it is reversible with `git checkout`. That is\nthe whole reason the phases are cut here: P3 builds the entire visual against fixtures, so the \"good visual\"\ncan be looked at, judged and rejected *before* a single real note is public.\n\nNot in scope: editing from `/vault/` (read-only by design — writes belong in Obsidian), live sync (§2's\nboundary), search indexing beyond client-side substring match, and any change to `/notes/`.\n\n---\n\n## 7. Open questions for Pierce\n\n1. **Does the first `--report` change the answer?** If it lists 300 daily notes of three sentences each, is\n   that still \"publish the whole vault\", or does it become \"publish `docs/` plus what I'd hand a colleague\"?\n   Worth agreeing now that the report is allowed to change the decision.\n2. **Third parties.** Client and colleague names appear in the vault. Publish, initial, or omit?\n3. **Is the stepping stone actually the destination?** `/vault/` currently publishes the site's own `docs/`\n   folder, and it reads well — \"here is how this site is built, in the open\" is a coherent page in its own\n   right, and it carries no privacy question at all. Is P4 still what you want, or is this it?\n4. **Nav pressure.** Seven top-level links is a lot. Does `/vault/` sit under a \"Notes\" grouping with\n   `/notes/`, or stand on its own?\n\nSource for all four: Claude, drafting this plan, 2026-09-08.\n","tags":["systems"],"created":"2026-09-08T15:14:10.602Z","updated":"2026-09-08T15:14:10.602Z"},{"path":"docs/BLUEPRINT.md","folder":"docs","title":"BLUEPRINT — piersonnorris.com","body":"# BLUEPRINT — piersonnorris.com\n\nMaster build spec. Prepared by Claude (Cowork), 2026-08-23, from decisions made directly with Pierson. ChatGPT: read this fully before writing code. [[CONTENT — piersonnorris.com]] holds all copy; [[ASSET TRACKER — feature spec (public-repo-safe edition)]] holds the dashboard feature.\n\nAnything tagged `[OPEN]` is an unmade decision — ask Pierson, don't guess.\n\n---\n\n## 1. Mission\n\nA personal site that answers \"who is Pierson Norris?\" for two audiences at once:\n\n1. **Humans** — recruiters, companies, collaborators. Clean, dark, fast, credible.\n2. **Machines** — AI crawlers and agents (GPTBot, ClaudeBot, PerplexityBot, Google) that increasingly answer \"who is X?\" on people's behalf. The site is deliberately structured so machines parse it accurately: semantic HTML, JSON-LD, llms.txt, explicit crawler permissions.\n\nSecondary mission: a **Tools hub** for things Pierson builds for himself, starting with a gated live portfolio dashboard. The site should read as proof-of-work, not just a resume.\n\n## 2. Locked decisions\n\n| Decision | Value |\n|---|---|\n| Stack | Plain HTML/CSS/JS. No framework, no build step, no npm for the site itself. |\n| Hosting | GitHub Pages, repo `piersonnorris.github.io`, branch `main`, root. `.nojekyll` present. |\n| Domain | `piersonnorris.github.io` now; `piersonnorris.com` attached later (build all URLs relative; put absolute URLs only in the crawl kit files, and update them at domain switch — see §7). |\n| Brand | Dark + sleek. Dark ground; the eye goes to what's bright. Full system in §4. |\n| Blog | Not in v1. No placeholder page. Structure must make adding one later trivial. |\n| Asset tracker | Password-gated only. Real numbers never in the repo or the public build. See [[ASSET TRACKER — feature spec (public-repo-safe edition)]]. |\n| Analytics | Privacy-friendly, no cookie banner (GoatCounter or Plausible). `[OPEN]` which one — build without it; it's one script tag added later. |\n\n## 3. Audiences drive structure\n\nEvery page carries its content in semantic HTML (`<main>`, `<article>`, `<section>`, `<h1>`–`<h3>` in strict order, `<time datetime>`, `<address>`, `<nav aria-label>`). No div-soup, no text baked into images, no content that only exists after JS runs (JS may *enhance*, never *carry*, the core content — the tracker page is the one exception and it's gated anyway).\n\n## 4. Design system\n\nCommitted single-theme dark design (this is the brand, not a dark mode).\n\n### Color tokens\n\n```css\n:root{\n  --bg:#0e1116;        /* page ground — graphite, cool near-black */\n  --panel:#151b23;     /* raised cards */\n  --panel2:#1a222d;    /* hover / second raise */\n  --line:#232d39;      /* hairline borders */\n  --line2:#2e3947;     /* stronger borders */\n  --ink:#e9edf3;       /* primary text */\n  --mut:#8b95a5;       /* secondary text */\n  --dim:#5c6675;       /* metadata */\n  --acc:#ffb224;       /* THE bright accent — Terminal Amber [OPEN: Pierson may switch to #4CC9F0 ice cyan or #B6F09C volt; only this token changes] */\n  --acc-soft:rgba(255,178,36,.12);\n  --good:#45c26b;      /* success/positive only, never decorative */\n}\n```\n\nRules: one accent, spent sparingly (links, active nav, key numbers, hover states, timeline nodes). Never large filled amber areas. Body background always `var(--bg)` explicitly. Selection color: accent bg, dark text.\n\n### Typography\n\nGoogle Fonts: `Archivo` (700/800) for display, `IBM Plex Sans` (400/450/500/600) for body, `IBM Plex Mono` (400/500) for dates, labels, chips, and data.\n\n```html\n<link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css2?family=Archivo:wght@600;700;800&family=IBM+Plex+Sans:wght@400;450;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap\">\n```\n\nScale: mono meta 11–12px w/ letter-spacing +0.1em uppercase · body 15.5px/1.62 · h3 17–18px · h2 24px · h1 clamp(34px, 6vw, 54px), Archivo 800, letter-spacing −0.02em. Running text max ~65ch. Tabular numerals (`font-variant-numeric: tabular-nums`) anywhere digits align.\n\n### Layout\n\nMax-width 880px centered column, 24px side padding, generous vertical rhythm (sections ~64px apart). Cards: `--panel` bg, 1px `--line` border, 6px radius. Mobile-first; single breakpoint at 640px is usually enough. No horizontal page scroll ever — wide content gets its own `overflow-x:auto` container.\n\n### Voice\n\nConfident, terse, concrete. Numbers over adjectives (\"50+ clients, five-figure first summer\" beats \"successful business\"). No corporate filler, no exclamation marks.\n\n## 5. Site architecture\n\n```\n/                       index.html          Home\n/about/                 index.html          About\n/experience/            index.html          Experience (the 2023–2027 timeline)\n/projects/              index.html          Projects\n/tools/                 index.html          Tools hub\n/tools/tracker/         index.html          Asset tracker (encrypted artifact — see spec)\n/contact/               index.html          Contact\n/assets/                css/site.css, js/site.js, img/…, resume/pierson-norris-resume.pdf\nrobots.txt  llms.txt  sitemap.xml  404.html  .nojekyll\n```\n\nFolder-per-page (`/about/` not `about.html`) so URLs stay clean and extension-free on Pages. One shared stylesheet; per-page CSS only if genuinely needed. Nav identical on every page, current page marked with `aria-current=\"page\"` + accent underline. Footer on every page: name, email, LinkedIn `[OPEN: more socials TBD]`, \"Built by hand — plain HTML, no framework\" (that line is on-brand).\n\n`404.html` at root (GitHub Pages picks it up automatically): dark, terse, links home.\n\n## 6. Page specs (copy lives in CONTENT.md)\n\n**Home** — The elevator pitch page. H1 name, one-line identity, the 2–3 sentence pitch (this is what AI snippets will lift — it must stand alone), then three compact proof cards (True North · Screencastify · Tools) each linking deeper, then a quiet contact line. No giant hero animation; confidence through restraint.\n\n**About** — The fuller story in first person, ~250–350 words, from CONTENT.md §2. Optional photo `[OPEN]`. Ends with what he's looking toward (the 2027 line, once Pierson supplies it).\n\n**Experience** — The timeline, 2023 → 2027, rendered as a vertical rail with year markers (mirror the Build HQ page's structure: year label column, entries as cards, amber nodes). Include the \"before Elon\" prologue as a muted intro block `[OPEN: Pierson may cut it]`. Each entry: role/title, mono date range, 1–3 sentence body with bolded numbers, optional takeaway line with accent left-border. Resume PDF download button at top right of the page `[OPEN: PDF to be produced — build the button pointing at /assets/resume/pierson-norris-resume.pdf and it will drop in]`. Skills grid at the bottom (structured `<ul>`, one `<li>` per skill group — prime crawler content).\n\n**Projects** — Card grid (1-col mobile / 2-col desktop): True North digital infrastructure, the Asset Tracking system, Screencastify internship tooling (the story is the project), this website itself. Each card: title, mono period, 2-sentence description, outcome numbers where they exist, link (live site, or the Tools page, or Experience anchor).\n\n**Tools** — The hub. Intro sentence (\"Things I build for myself; some are public, some are locked\"), then a card per tool. v1 has one: **Portfolio Tracker** — description of what it does + a screenshot with SAMPLE data + a \"locked\" chip + link to /tools/tracker/. Layout must make a second tool card a copy-paste job.\n\n**Contact** — Email (mailto), LinkedIn, GitHub profile `[OPEN: other socials TBD]`. No contact form in v1 (forms need a backend or third party; a mailto link is honest and zero-maintenance). Mono-styled links, one per line, `<address>` element.\n\n## 7. AI-crawlability kit\n\nStarters for `robots.txt` and `llms.txt` are already in the repo root — extend, don't replace. At custom-domain switch, update absolute URLs in: JSON-LD (`url`), `sitemap.xml`, `llms.txt`, OG tags, and add the `CNAME` file.\n\n**JSON-LD** — On Home and About, in `<head>`:\n\n```html\n<script type=\"application/ld+json\">\n{\n  \"@context\": \"https://schema.org\",\n  \"@type\": \"Person\",\n  \"name\": \"Pierson Norris\",\n  \"alternateName\": \"Pierce Norris\",\n  \"url\": \"https://piersonnorris.github.io/\",\n  \"email\": \"mailto:norrispierce506@gmail.com\",\n  \"jobTitle\": \"Founder & Owner, True North Services LLC\",\n  \"affiliation\": [\n    {\"@type\": \"Organization\", \"name\": \"True North Services LLC\"},\n    {\"@type\": \"CollegeOrUniversity\", \"name\": \"Elon University\"}\n  ],\n  \"description\": \"<the Home elevator pitch, verbatim>\",\n  \"knowsLanguage\": [\"English\", \"Chinese (Mandarin, near-fluent)\"],\n  \"knowsAbout\": [\"Small business operations\", \"Sales and local marketing\", \"Finance and markets\", \"AI workflow automation\", \"Web tools\"],\n  \"sameAs\": [\"<LinkedIn URL>\", \"<GitHub URL>\"]\n}\n</script>\n```\n\nValidate with Google's Rich Results Test before calling M3 done.\n\n**Per-page `<head>`** — unique `<title>` (\"Page — Pierson Norris\") and meta description; OG + Twitter card tags (og:title, og:description, og:type=profile on Home, og:image once a share image exists `[OPEN]`); `<link rel=\"canonical\">`; `<html lang=\"en\">`.\n\n**sitemap.xml** — all six public pages, referenced from robots.txt. The gated tracker page is NOT in the sitemap and carries `<meta name=\"robots\" content=\"noindex\">` in its (unencrypted wrapper) head.\n\n**llms.txt** — keep in sync with site content at every milestone; it is the machine-readable executive summary.\n\n## 8. Asset tracker (summary — full spec in ASSET_TRACKER_SPEC.md)\n\nPipeline: scheduled GitHub Action → fetches the Google Sheet via service account (key in Actions Secrets) → normalizes to JSON → renders the dashboard template → encrypts page+data with StatiCrypt using `TRACKER_PASSWORD` (also a Secret) → commits ONLY the encrypted `/tools/tracker/index.html`. The public repo and public site never contain readable holdings. The Tools hub shows a sample-data screenshot instead. Build this LAST (M4) — the site must not wait on it.\n\n## 9. Milestones\n\n**M0 — Live scaffold** *(already done if the coming-soon page is up)* Repo pushed, Pages enabled, placeholder serving.\n\n**M1 — Shell + Home.** `site.css` with the full design system; shared nav/footer; finished Home. ✔ Accept: Home matches §4/§6, valid HTML (W3C validator), keyboard-navigable, no horizontal scroll at 360px width, Lighthouse ≥95 accessibility.\n\n**M2 — Content pages.** About, Experience (timeline + skills), Projects, Contact, 404. ✔ Accept: all copy verbatim from CONTENT.md (`[OPEN]` items visibly absent, not faked); timeline readable on mobile; every internal link works.\n\n**M3 — Crawl kit.** JSON-LD both pages, full head/OG treatment, sitemap.xml, llms.txt + robots.txt finalized. ✔ Accept: Rich Results Test passes; sitemap fetches; every page has unique title + description.\n\n**M4 — Tracker.** Per ASSET_TRACKER_SPEC.md: Action runs green on schedule + manual dispatch; page decrypts with the password; sample-data screenshot on Tools hub. ✔ Accept: zero secrets in repo history; a wrong password shows the StatiCrypt prompt again, not content; `noindex` present.\n\n**M5 — Polish + launch.** Favicon (dark-ground \"PN\" mark), 404 styled, GoatCounter/Plausible when chosen, run the full QA list below, then Pierson shares the URL.\n\n## 10. Definition of done (run at M5, spot-check every milestone)\n\n- Every page: valid HTML, unique title/description, h1 exactly once, no console errors.\n- Mobile 360px → desktop 1440px: no layout breaks, no sideways scroll.\n- All links resolve (internal relative, external `rel=\"noopener\"`, mailto correct).\n- Lighthouse: Performance ≥90, Accessibility ≥95, SEO ≥95 on Home + Experience.\n- JSON-LD validates; robots.txt + sitemap.xml + llms.txt mutually consistent.\n- Grep the repo for `secrets`, `BEGIN PRIVATE KEY`, the password — zero hits.\n- The site reads correctly with JS disabled (tracker excepted).\n\n## 11. Standing rules for ChatGPT\n\n1. Copy comes from CONTENT.md verbatim; `[OPEN]` means ask Pierson.\n2. Complete files with exact paths; explain each step in plain English first.\n3. One milestone at a time; commit/push walkthrough after each.\n4. No secrets in chat, repo, or client code — ever.\n5. No new dependencies, frameworks, or build steps without Pierson's explicit yes.\n","tags":["4CC9F0","B6F09C"],"created":"2026-09-08T15:14:10.603Z","updated":"2026-09-08T15:14:10.603Z"},{"path":"docs/CONTENT.md","folder":"docs","title":"CONTENT — piersonnorris.com","body":"# CONTENT — piersonnorris.com\n\nSingle source of truth for every claim on the site. Compiled by Claude from Pierson's Drive (resumes, LLC documents, the Screencastify final deck, the asset-tracking spec) and direct decisions with Pierson, 2026-08-23. Timeline section rewritten 2026-09-05 from the True North operations framework, priority roadmap, all-hands deck, and head-manager training deck, plus the Screencastify final presentation.\n\nRules: use this copy verbatim (light grammatical smoothing allowed, meaning-changes not). `[OPEN]` = missing or unconfirmed — ask Pierson; never invent. Where numbers appear, they came from Pierson's own documents.\n\n---\n\n## 1. Identity\n\n- **Public name:** Pierson Norris (goes by Pierce in person — \"Pierson Norris\" is the searchable, professional identity and appears in H1s, titles, JSON-LD).\n- **One-line identity (Home, under H1):** Founder, operator, and builder — business on the ground, AI in the workflow.\n- **Elevator pitch (Home + JSON-LD description, verbatim):**\n  > Pierson Norris is a Finance & Accounting student at Elon University (class of 2027) who builds businesses and the systems that run them. He founded True North Services LLC, a Chicago North Shore exterior-services company he started at 18 and grew into a documented, trained operation; spent a summer as an AI intern at Screencastify mapping seven workflows across six departments and shipping three live automation fixes; and is the incoming president of Elon's AI & ET Club. This site is his profile, his portfolio, and his toolbox.\n\n  `[OPEN]` Pierson: confirm the club title line and the \"started at 18\" framing.\n- **Contact email:** norrispierce506@gmail.com `[OPEN: may swap for a cleaner public alias]`\n- **LinkedIn:** https://www.linkedin.com/in/pierson-norris-634954246 `[OPEN: claiming a cleaner custom URL is on his checklist — use whichever exists at build time]`\n- **GitHub:** `[OPEN: username pending confirmation]`\n- **Other socials:** `[OPEN: TBD by Pierson — an Instagram for the business (oaisis_exterior_cleaning) exists from the Oasis era]`\n- **Photo:** `[OPEN]`\n\n## 2. About (draft, first person, ~300 words)\n\n> I grew up on Chicago's North Shore doing the jobs that teach you how work actually happens — a Taco Bell register, moving furniture for a design firm, a one-month internship shadowing a CEO. The summer before college I started a power-washing and window-washing business with a crew of high-school and college kids. First summer: 50+ clients and five figures in revenue, sold with flyers, Nextdoor posts, and knocking on doors.\n>\n> At Elon I study Finance & Accounting with minors in Entrepreneurship and Mandarin Chinese. The business kept growing alongside the degree, and in 2026 it became True North Services LLC — a real company now, with an operations framework, a crew-training program I wrote myself, and a team that runs weekly ops calls without me in the room.\n>\n> The other thread is AI. In summer 2026 I was the AI intern at Screencastify, where I learned my favorite working method: don't start with the tool, start with someone's actual Tuesday. One written prompt got seven workflows mapped across six departments in 48 hours, and every single one had the same failure — a person acting as the API between systems that don't talk. We shipped three fixes; one cut a 4–8 hour weekly finance task to under 15 minutes.\n>\n> Now I run True North, trade and track my own portfolio (since 2020), and serve as incoming president of Elon's AI & ET Club. I build my own tools — the locked one on this site tracks my holdings across five platforms, live from a spreadsheet.\n>\n> `[OPEN — closing line: the 2027 headline. What Pierson is aiming at after graduation.]`\n\n## 3. Experience timeline (2023 → now)\n\nRendered newest-first at `/experience/`. The timeline **starts at 2026** (the top) and runs back to 2023. Per Pierson, 2026-09-05: **no 2027 entries** — that year hasn't happened, so it is not on the page.\n\n**2026** *(nine entries, newest first)*\n\n- **Incoming President, Elon AI & Emerging Tech Club** — 2026, ongoing. `[OPEN: exact title, term start/end, one line of agenda]`\n- **AI Intern, Screencastify** — Jun 9 – Aug 7, 2026 (nine weeks). One three-round workflow-mapping prompt dropped in a shared folder; department leaders mapped their own work (trigger, middle, hand-offs, friction, root cause) and returned **7 workflows across 6 departments in 48 hours**, with **one meeting added**. All seven shared one failure: *a person acting as the API between systems that don't talk.* Three fixes shipped — Finance weekly AR collections (**4–8 hrs/week → under 15 minutes**), Product-marketing Monday update email (**~30 min/week plus the rework loop, gone**), Engineering sprint-review recording (a hard-cutoff policy and one saved filter — **no tool build at all**). Combined build time: one afternoon. Separately, a **76-post** distribution audit reframed marketing as **under-concentrated, not under-worked**, and proposed the *Content Waterfall* — one ungated anchor asset repurposed into eight placements, with a single named distribution owner. *Takeaway: the method is the résumé line — find the problem in someone's actual Tuesday, get sign-off, build it with them, ship it.*\n- **Built Castify OS — three internal AI modules, live** — Jun 15 – Jul 31, 2026. Idea (Jun 15) → first agent skill (Jun 18) → build spec (Jun 29) → shipped (Jul 7) → first live client (Jul 9) → third module in beta (Jul 31). **Proposal Generator** (sales, live): deal context in, client-ready branded PDF + editable .docx out, **30 min → 5 min per proposal**. **Outbound Email Agent** (marketing, live): reads pending contacts from a sheet, researches, drafts into Gmail — *approval-gated by design, it never auto-sends*. **Traffic & AI Referral Analytics** (marketing, beta): two brands and two sites in one view, with AI-assistant referrals broken out as their own channel class. *Takeaway: AI doesn't replace the workflow. It removes the part of the workflow nobody wanted to do.*\n- **Ran True North's first company all-hands** — Jul 17, 2026, co-presented with the operations lead. Nine parts: the quote process end to end (respond as True North and capture name + address → price face to face with your reasoning → log it in the quote form → send a formal quote → book it and brief the crew), a live site walkthrough, a LinkedIn team-spotlight program, a written marketing do's-and-don'ts (before *and* after on every post, same angle, natural light, teammates credited, never a client's address), and a referral incentive of a **$10 minimum** per client — paid only once the client is landed *and* the job is completed.\n- **Built True North's digital infrastructure** — May – Aug 2026. Hand-built company site (standalone HTML, Netlify, custom domain) at truenorthservicesllc.netlify.app; lead-capture form mapped to service categories; a post-quote form feeding a responses sheet so close rate is a visible number; 90+ photo marketing library; SMS-automation scoping.\n- **Wrote the crew training program** — May 2026. Head-manager service training: power washing, paver-sand reapplication, and window washing, each a six-step field procedure with its own do's and don'ts. *Takeaway: the training is the quality control — you can't inspect your way to a standard you never wrote down.*\n- **Put the company on a documented operating system** — Apr – Jun 2026. One path per lead: lead → **Form 1** quote scheduling & assignment → appointment → **Form 2** quote result & pricing (scope, before photos, price, labor/materials estimate, payment terms, close probability) → approval → job → **Form 3** end-of-job report (actual hours, materials, after photos, property condition, damage, upsells, equipment) → invoice → paid. All three feed one master sheet: leads, quotes, closed jobs, scheduling, payments, applicants, referral partners, manager performance, equipment, weekly scorecard. *Takeaway: no form, no distribution. The paperwork is the control.*\n- **Incorporated True North Services LLC** — Apr – May 2026. LLC filing, operations & risk framework, written priority roadmap, and a manager-contractor model (managers are independent contractors who own crews, scheduling, and execution risk). Team of **four-plus**, operations lead running weekly ops calls. Economics: of gross, **20%** company / **5%** backend / **5%** lead fee / **70%** manager. Floors: **$100** minimum job, **30%+** manager margin target, pricing-floor check before quoting. Liability: jobs over **$500** require a signed client agreement; managers sign a scope-and-indemnification agreement; before/after photos on property-sensitive jobs; card fees passed through flat. Service-boundary matrix: direct — power washing, window washing, weeding, mulching, bush trimming, staining, pavers, gravel paths, seasonal cleanups; referral-only — roofing, plumbing, electrical, large tree work, licensed trades; anything else is approval-only. Cash: bank transfer first, cash second, card third; revenue lands in the LLC account, distributions biweekly once documentation, disputes, and refunds settle. *Takeaway: from hustle to institution — docs, training, and structure that run without you in the room.*\n- **Asset tracking system** — 2026, ongoing. Five-platform portfolio tracker in Google Sheets, monthly-tab automation, gated live dashboard on this site.\n\n**2025**\n- **Manager & equity partner, Student Maintenance LLC** — spring 2025. Brought on as manager with an ownership stake: on-site quoting, closing leads, marketing content, sales-team leadership. `[OPEN: include on the public site at all? If yes, confirm dates + how to describe the stake.]`\n- `[OPEN: junior-year highlights]`\n\n**2024**\n- **Business + portfolio, year two** — ran the cleaning business back-to-back summers while carrying the dual major; self-managed portfolio active since 2020.\n- `[OPEN: what defined 2024 — clubs, coursework, the summer story?]`\n\n**2023**\n- **Started at Elon University** — Aug 2023. Finance & Accounting dual major; minors in Entrepreneurship and Mandarin Chinese (near-fluent). `[OPEN: confirm still accurate]`\n- **Founded Oasis Exterior Cleaning** — May 2023. Power washing + window washing, Chicago North Shore. 50+ clients, five-figure revenue (~$12k profit) in the first summer, +68% customer growth from hand-built local marketing (flyers, Nextdoor, Instagram, Facebook), managing a crew of high-school and college students. *Takeaway: sold a service door to door and ran a crew before most people pick a major.* `[OPEN: is Oasis→True North one continuous story (rebrand) or two chapters?]`\n\n**Prologue — before Elon (muted block)** `[OPEN: keep or cut]`\nFirst W-2 at Taco Bell (2021) · mover/assistant at Inspired Interiors (2021–23) · one-month internship under Gemini Builds It CEO Courtney Wright (spring 2023) · trading his own portfolio since Aug 2020 · New Trier High School, class of 2023.\n\n### 3a. True North — open naming question\n\nPierson's own documents use three names for the same entity. Resolve before the About and Projects pages ship:\n\n- **True North Services LLC** — the all-hands deck (Jul 17, 2026), the live site, the Drive folder, the structure doc's filename. **This is what the site currently uses.**\n- **True North Maintenance LLC** — the Operations & Risk Framework and the Priority To-Do List (both Apr 2026).\n- **North Shore Services LLC** — the heading inside `True_North_Services_Structure 2.docx`.\n\n`[OPEN: Pierson — which is the name on the filing?]`\n\n### 3b. Screencastify — publication boundary\n\nKept **off** the public site deliberately: the first live client's name, Screencastify's internal channel performance figures (total and average view counts), and the names of their internal vendor tools. The page describes what Pierson built and the time saved, not his former employer's private numbers. `[OPEN: Pierson — confirm, or say which of these he has clearance to name.]`\n\n## 4. Skills (Experience page grid — structured list, crawler-friendly)\n\n- **Operations & leadership** — crew hiring and training programs, documented ops frameworks, weekly ops cadence, manager-contractor structuring, client management (50+ clients).\n- **Sales & local marketing** — door-to-door and referral sales, in-person quoting, Nextdoor/Meta local campaigns, lead-capture funnels, content standards.\n- **Finance & markets** — Finance & Accounting dual major; unit economics, pricing floors and margin structures; active self-directed investor since 2020 (equities, options, crypto — five platforms).\n- **AI & automation** — workflow mapping and diagnosis, prompt-built internal tools, agent modules with human approval gates, Google Sheets/Apps Script automation, Sheets API, N8N exploration.\n- **Web** — hand-built HTML/CSS sites (True North on Netlify; this site), form-to-sheet plumbing, static deploys.\n- **Languages** — English; Mandarin Chinese (near-fluent).\n\n## 5. Projects (Projects page cards)\n\n1. **True North Services — digital infrastructure** (2026, live) — company site, lead capture, photo library, ops docs. Link: True North site URL `[OPEN: confirm final URL]`.\n2. **Portfolio Asset Tracker** (2026, live + locked) — Google Sheet → GitHub Action → encrypted dashboard; five platforms, monthly history. Links to /tools/.\n3. **Screencastify AI internship tooling** (2026) — the 7-workflows/48-hours diagnosis method + three shipped fixes. Links to Experience entry.\n4. **piersonnorris.com** (2026) — this site: plain HTML, dark system, AI-crawlable by design, spec'd with Claude, built with ChatGPT. Repo link once public.\n\n## 6. Tools page\n\nIntro: \"Things I build for myself. Some are public, some are locked.\"\nCard — **Portfolio Tracker**: \"Live view of my holdings across five platforms — SoFi, Webull, Robinhood, Gemini, GoMining — pulled from the spreadsheet I actually maintain. Locked; sample view below.\" + sample-data screenshot + locked chip → /tools/tracker/.\n\n## 7. Numbers that may appear on the site (all from Pierson's own docs)\n\n50+ clients · five-figure first-summer revenue (~$12k profit) · +68% customer growth · 7 workflows / 6 departments / 48 hours / 1 meeting added · 3 shipped fixes · 4–8 hrs/week → under 15 minutes · ~30 min/week saved on the Monday email · 76 posts audited · 3 Castify OS modules live · 30 min → 5 min per proposal · 9 weeks idea-to-platform · 5 platforms tracked · team of 4+ · 20/5/5/70 revenue split · $100 minimum job · 30%+ manager margin target · $500 signed-agreement threshold · $10 minimum referral incentive.\n\n**Never on the site:** portfolio dollar values, share counts, individual holdings, the spreadsheet ID, any credential. (Tracker data exists only behind the password.) Also off the site: Screencastify client names, their internal channel performance figures, and their internal vendor tool names (see §3b); True North client names, addresses, and revenue figures.\n","tags":[],"created":"2026-09-08T15:14:10.603Z","updated":"2026-09-08T15:14:10.603Z"},{"path":"docs/OBSIDIAN_SYNC.md","folder":"docs","title":"Obsidian connectivity — graph view + vault sync","body":"# Obsidian connectivity — graph view + vault sync\n\nStatus: **shipped 2026-09-07** (ROADMAP R15). Two halves that meet in the middle: a graph that shows how notes connect, and a pipeline that gets the *real* vault in front of it.\n\n---\n\n## 1. The graph — `assets/js/notes-graph.js`\n\n`PNGraphify` mounts inside the notes UI on both `/notes/` (general vault) and the tracker's Obsidian tab (stock vault), behind a **Graph** button in the toolbar.\n\nDeliberately split in two:\n\n| Half | What it is | Tested |\n|---|---|---|\n| `build(notes, {tags, missing})` | Pure. Notes → `{nodes, links, stats}`. No DOM, no layout, no randomness. | Yes — `tools/tracker/notes-graph.test.js` |\n| `mount(el, opts)` | Force-directed SVG: pan, zoom, node dragging, neighbourhood highlight, click-to-open. | No — left to the browser |\n\n### The judgement calls `build()` makes\n\nThese are the decisions the picture then draws confidently, so they are the part worth writing down:\n\n- **A link is a resolved `[[wikilink]]`, once.** `A → B` and `B → A` are one connection, not two. `[[Note|alias]]` and `[[Note#heading]]` resolve to `Note`, matching Obsidian. Resolution is case-insensitive; a note linking to itself is not a link.\n- **A tag is not a link.** Tag nodes are drawn (toggleable) and tags are counted, but sharing `#macro` does not connect two notes and does not rescue either from being an orphan. Tagging is a weaker signal than linking and conflating them would flatter the vault.\n- **An unresolved link is not a link either.** A note whose only bracket points at a note that doesn't exist is still an orphan. That is exactly why orphans and unresolved links are two separate numbers.\n- **Data blobs are not writing.** Notes carrying a `noteType` (the tracker's calendar and project-board storage) are excluded — they would dominate the picture.\n- **Density** is `links × 2 / notes`: average links per note, the single number for whether the vault is a web or a pile.\n\n### Two known implementation traps\n\n- **Layout runs synchronously before the first paint.** Nodes are positioned by a `transform` on their `<g>`, and `requestAnimationFrame` does not fire at all in a background tab — so a graph that waited for the first animation frame rendered the entire vault stacked at `0,0`. The sim settles ~90 steps up front, paints, auto-fits the camera to the node bounds, and only then animates the remainder.\n- **Focus rings.** `.pg-node:focus-visible` restates its own ring for the same specificity reason `.pnchart-graph .vg-node` had to (ROADMAP U2): a two-class selector outranks the generic `[tabindex]:focus-visible` rule regardless of source order.\n\n`PNGraphify` is separate from `PNCharts.graph`, which stays what it was — the tracker Projects tab's projects-and-AI-thoughts map.\n\n---\n\n## 2. The sync — `tools/obsidian-sync.js`\n\n```bash\nnode tools/obsidian-sync.js --vault \"C:\\path\\to\\Vault\"        # → private/notes/obsidian-vault.json\nnode tools/obsidian-sync.js --vault \"…\" --list --dry-run      # report only, writes nothing\nnode tools/obsidian-sync.js --help\n```\n\n| Flag | Effect |\n|---|---|\n| `--vault <dir>` | The vault folder. Falls back to `OBSIDIAN_VAULT`, then `private/.obsidian-vault-path` (one line, gitignored). |\n| `--out <file>` | Bundle destination. Default `private/notes/obsidian-vault.json`. |\n| `--scope general\\|stocks` | Which vault the bundle is meant for. |\n| `--no-body` | Index only: titles, tags and paths, no note text. |\n| `--list` | Print orphan and unresolved titles. **Off by default** — the script prints counts, never note content. |\n| `--stdout` / `--dry-run` | Print the bundle / report and write nothing. |\n\nIt walks the folder for `.md`, skipping every dot-directory (`.obsidian`, `.trash`, `.git`). Parsing is **not** reimplemented: it calls the site's own `PNVault.fromMarkdown`, so a note read here and a note imported through the browser end up identical. The only thing layered on top is block-style YAML (`tags:\\n  - a`) plus `alias`/`aliases`, which Obsidian writes and the browser importer never had to handle.\n\nOutput is one `pn-vault-bundle` JSON: `{format, version, scope, generatedAt, source, count, notes[]}`, each note carrying its vault-relative `path`.\n\n### Getting a bundle into a vault\n\n**Route A — Import button (`/notes/`, no build).** Pick the one `.json` file instead of hand-selecting hundreds of `.md` files. `PNVault.importBundle()` **upserts** by `sourcePath`, so re-running the sync after editing in Obsidian updates notes in place; the old `.md` path only ever *added*, which made repeat imports useless. `source_path` is written into exported frontmatter too, so the identity survives a full round trip. Nothing is ever deleted: a note removed from the vault folder stays in the browser until you delete it.\n\n**Route B — baked into the encrypted build (tracker).** Drop the same bundle at `private/tracker/obsidian-vault.json` and `tools/tracker/build.js` (`loadObsidianVault`) seals it into the payload as `obsidianVault`, exactly like `googleEvents`. The tracker's Obsidian tab then shows a banner offering a one-click merge. Merging is a button and never automatic — it overwrites notes that came from the same vault paths, and silently rewriting a vault on unlock would be a surprising thing for a page to do. Malformed or missing input degrades to `null`; the build never fails over vault data.\n\n---\n\n## 3. The public route — `--public` (added 2026-09-08, R17)\n\nThe third destination for a bundle, after the Import button and the encrypted build: a committed file the\npublic `/vault/` page reads. Design and reasoning live in [[BACKSTAGE — the vault, in public, with no gate]]; the mechanics are here.\n\n```bash\nnode tools/obsidian-sync.js --vault \"<dir>\" --report                  # what WOULD publish. Writes nothing.\nnode tools/obsidian-sync.js --vault \"<dir>\" --public --prefix notes   # → assets/data/vault-public.js\n```\n\n| Flag | Effect |\n|---|---|\n| `--report` | Every note that would publish — path, word count, title — plus scrubber hits. No note text. |\n| `--public` | Writes the public bundle. Default `assets/data/vault-public.js`. |\n| `--label <name>` | What the page calls the snapshot. |\n| `--prefix <dir>` | Nests published paths under a folder, so the tree has something to draw. |\n| `--waive <ids>` | Skip named scrub rules for this run. Printed loudly and recorded in the bundle. |\n| `--no-link-paths` | Leave inline path references alone instead of resolving them into wikilinks. |\n\nThree things are worth knowing before running it against a real vault:\n\n- **Default is publish, and the opt-outs are Pierce's.** `publish: false` in frontmatter, a `#private` tag, or a\n  `nopublish/` folder. That default was his decision (2026-09-08), taken over two narrower scopes.\n- **The scrubber refuses; it never redacts.** Dollar amounts, share counts, addresses, phone numbers,\n  key-shaped tokens, plus literal terms from `private/.publish-blocklist` (gitignored — a committed list of\n  names you must never publish is itself a published list of those names). A hit prints `path:line`, never the\n  matched text, and stops the write. The fix belongs in the vault.\n- **Output is `.js`, not `.json`.** It loads with a `<script>` tag, exactly like `atlas-data.js`, so the page\n  works over `file://` with no fetch and no CORS. A `.json` path is still honoured if you ask for one.\n\nA publish-time transform runs on the way out: an inline-code path pointing at another published note\n(`` `docs/CONTENT.md` ``) becomes a `[[wikilink]]`. Vaults link with brackets; the site's own docs link by\nwriting a path, and both are the author saying \"this connects to that\". Only the exact inline-code form is\nrewritten — guessing inside prose or fenced blocks would invent edges nobody wrote. The count lands in the\nbundle as `linkedPaths` so the page can say it happened.\n\nThe first published snapshot is the site's own `docs/` folder, which was already public in this repo. The\npersonal vault is not published; that step is P4 in the plan and waits on Pierce.\n\n## Known boundary\n\nSame as [[Private Portfolio Calendar]] says about Google Calendar, and for the same reason: **this is a snapshot, not live sync.**\n\nThe site is static, served from GitHub Pages. It has no server, no OAuth, and no way to read a folder on disk — not from the browser, not from CI. `obsidian-sync.js` runs on the machine that has the vault, outside the site's own code. Refreshing means running it again and re-importing (or rebuilding). GitHub Actions cannot do this pull: the vault lives on Pierce's disk, not behind a portable credential.\n\nNote text never enters the repo. The bundle path is gitignored, and Route B's copy exists only inside the AES-256-GCM payload.\n","tags":[],"created":"2026-09-08T15:14:10.603Z","updated":"2026-09-08T15:14:10.603Z"},{"path":"docs/PORTFOLIO_CALENDAR_PLAN.md","folder":"docs","title":"Private Portfolio Calendar","body":"# Private Portfolio Calendar\n\n## Phase 1 status\n\nThe first calendar phase is implemented as a dedicated password-gated tab inside the portfolio tracker.\n\n### Included now\n\n- Full six-week month grid with previous, next, and today navigation.\n- Confirmed dividend ex-dates and payment dates derived from included stock positions.\n- Private reminders, earnings dates, portfolio reviews, and tax deadlines.\n- Create, edit, and delete flows for private events.\n- Day detail panel and a rolling 90-day agenda.\n- Summary cards for the current month, next 30 days, dividend dates, and private plans.\n- Filters for ex-dividend dates, payments, earnings, and private plans.\n- Per-event **Add to Google** links.\n- A private `.ics` export for the current filtered calendar.\n- A direct link to the user's signed-in Google Calendar.\n- Encrypted device-local persistence through the existing stock vault.\n- Persistent dividend-date overrides so manual calendar corrections survive reloads.\n- Obsidian Markdown export/import for private calendar records.\n- **(2026-09-07) Real Google Calendar events, pulled one-way.** `tools/tracker/build.js` reads `private/tracker/google-calendar.json` (gitignored) and bakes it into the payload as `googleEvents`, exactly like `dividendCalendar`. That snapshot is produced by a Claude session with a Google Calendar connector — the source file never comes from the static site itself. See \"Known boundary\" below for what this is and isn't.\n- **(2026-09-07) A compact calendar under the Projects board.** The kanban board's own tab now shows a read-only month grid (board target dates + dividends + private plans + Google events) directly beneath the board, so you don't have to switch tabs to see what's coming up. Editing and export still live on the full Calendar tab.\n\n## Privacy model\n\n```text\nTracker password\n      |\n      v\nEncrypted portfolio payload ----> confirmed dividend dates\n      |\n      v\nEncrypted stock vault ----------> reminders / earnings / reviews / tax dates\n      |\n      +---- explicit user action ----> Google Calendar event link or .ics export\n```\n\nNothing is sent to Google automatically. Opening **Add to Google** places that single event in a Google Calendar draft. Downloading `.ics` keeps the file local until the user chooses where to import it.\n\nThe tracker password protects the entire calendar surface. User-created calendar records are additionally stored as AES-GCM ciphertext in the browser's stock vault. Plaintext calendar data appears only while unlocked or in an export the user explicitly requests.\n\n## Data model\n\n```json\n{\n  \"id\": \"calendar-example\",\n  \"date\": \"2026-09-15\",\n  \"title\": \"Quarterly allocation review\",\n  \"kind\": \"review\",\n  \"symbol\": \"\",\n  \"notes\": \"Check target weights and unpriced assets.\",\n  \"source\": \"private\",\n  \"readonly\": false\n}\n```\n\nDividend events use the same normalized shape but are marked `source: \"dividend\"` and `readonly: true`; they are edited from the Dividend tab so there is one source of truth.\n\n## Accuracy rules\n\n- Only dates explicitly present in the encrypted research payload or entered by the user appear on the calendar.\n- The tracker does not invent future dividend dates from payout frequency.\n- Included/excluded position scope controls which dividend events appear.\n- Manual dividend corrections are encrypted and merged over the build-time research values.\n- Calendar exports use all-day events and a one-day exclusive end date for broad calendar compatibility.\n- Filters affect the grid, metrics, agenda, and exported `.ics` file together.\n\n## Known boundary\n\nThis phase uses safe Google Calendar handoff links, standards-based `.ics` export, and — as of 2026-09-07 — a one-way, session-pulled snapshot of real events. None of that is the same as live sync: the static site itself holds no Google credentials and makes no calls to Google at any point, in the browser or in CI. The snapshot in `private/tracker/google-calendar.json` is produced entirely outside the site's own code, by a Claude session that happens to have a Google Calendar connector authorized for that chat; refreshing it means running that pull again and rebuilding, not something the site or a scheduled workflow can trigger itself.\n\nAutomatic two-way Google Calendar synchronization (write-back, or the tracker refreshing its own snapshot on a schedule) would still require Google OAuth credentials, token storage, revocation handling, and a private backend — tracked as **R9b** in [[ROADMAP — piersonnorris.com]], and still parked for the same reason: no long-lived Google credentials belong on a public static site.\n\n## Validation checklist\n\n- Calendar helper syntax and tracker application syntax parse successfully.\n- Month generation always returns 42 day cells.\n- Date-window filtering is deterministic.\n- `.ics` output includes valid all-day start and end dates.\n- Calendar metadata survives Obsidian Markdown export/import.\n- Private calendar events and dividend overrides persist only inside the encrypted stock vault.\n- Filtered event types remain visually distinct without relying on color alone.\n- Calendar controls are native buttons, links, and form controls for keyboard access.\n\n## Next phases\n\n### Phase 2: Project task board — implemented\n\nThe private goals board is now available in the tracker's **Projects** tab. It includes Backlog, Planned, In progress, Blocked, and Complete columns; outcome, milestones, next action, target date, dependencies, related portfolio/calendar links; and an encrypted Obsidian export. See [PORTFOLIO_TASKBOARD_PLAN.md](PORTFOLIO_TASKBOARD_PLAN.md).\n\n### Phase 3: Data connections\n\nEvaluate a read-only Robinhood connection or compatible MCP, then normalize its positions and transactions into the tracker. Keep account authorization outside Obsidian; Obsidian should receive encrypted/exported summaries and research notes, never brokerage credentials or reusable access tokens.\n","tags":[],"created":"2026-09-08T15:14:10.603Z","updated":"2026-09-08T15:14:10.603Z"},{"path":"docs/PORTFOLIO_TASKBOARD_PLAN.md","folder":"docs","title":"Private Portfolio Project Board","body":"# Private Portfolio Project Board\n\n## Phase 2 status\n\nThe project task board is implemented as a dedicated password-gated **Projects** tab inside the portfolio tracker.\n\n## Included now\n\n- Five workflow stages: Backlog, Planned, In progress, Blocked, and Complete.\n- Summary metrics for active, due-soon, blocked, and completed goals.\n- Goal creation and editing with a target outcome and concrete next action.\n- Milestones and dependencies, entered one per line for easy scanning.\n- Optional target dates with overdue highlighting.\n- Optional links back to tracker views, calendar views, or trusted web resources.\n- Left/right movement controls for advancing or revising a goal's stage.\n- Seeded roadmap goals for the calendar, task board, Google Calendar sync, and a future read-only brokerage connection.\n- Device-local AES-GCM persistence through the existing stock-note vault.\n- Obsidian Markdown export/import with structured JSON frontmatter and a readable five-section board.\n\n## Privacy model\n\n```text\nTracker PIN\n    |\n    v\nEncrypted stock-note vault\n    |\n    +---- project goals / outcomes / milestones / blockers / next actions\n    |\n    +---- explicit export ----> Obsidian Markdown\n```\n\nThe public site contains the board interface and non-sensitive starter roadmap only. Personal project edits stay encrypted in browser storage. The exported Markdown becomes readable only when the user explicitly downloads it into a private Obsidian vault.\n\n## Goal model\n\n```json\n{\n  \"id\": \"goal-example\",\n  \"title\": \"Read-only brokerage connection\",\n  \"status\": \"backlog\",\n  \"outcome\": \"Import positions without trading permission.\",\n  \"nextAction\": \"Compare supported read-only providers.\",\n  \"targetDate\": \"\",\n  \"milestones\": [\"Connector comparison\", \"Security review\"],\n  \"dependencies\": [\"Approved provider\"],\n  \"relatedLink\": \"#positions\"\n}\n```\n\n## Validation checklist\n\n- All five stages are represented in a horizontally scrollable board.\n- Cards can move only within the defined stage sequence.\n- Invalid goals and unsupported links are normalized before storage.\n- An intentionally empty saved board is not repopulated with starter goals.\n- Goal metadata survives Obsidian Markdown export/import.\n- Personal goal content is absent from plaintext browser storage.\n- The task board is inaccessible until the portfolio payload is unlocked.\n\n## Phase 3 boundary\n\nThe task board tracks the future brokerage-integration work but does not connect an account. Any Robinhood-compatible integration must be read-only, must keep reusable credentials and tokens outside Obsidian, and must receive a separate security review before implementation.\n","tags":[],"created":"2026-09-08T15:14:10.604Z","updated":"2026-09-08T15:14:10.604Z"},{"path":"docs/ROADMAP.md","folder":"docs","title":"ROADMAP — piersonnorris.com","body":"# ROADMAP — piersonnorris.com\r\n\r\nThe shared task list for the three people building this site: **Pierce** (owner, decisions, secrets), **Claude** (gated tooling, docs, Drive research, build pipeline), and **ChatGPT** (public pages per [[BLUEPRINT — piersonnorris.com]], stock-update workflow). Read this first; it says what's in flight, what's next, and who owns it.\r\n\r\nSync rule: this file is canonical. The same list is mirrored to `TASKS.md` (Pierce's local Obsidian vault note, untracked) and seeded into the tracker's **Projects** board (`assets/js/taskboard.js → seed()`), which is also what `/island/` reads — so a card added there shows up in three places automatically. When a task changes state, update this file and the mirror you touched — whoever commits next reconciles the third.\r\n\r\nUpdated: 2026-09-07 (later) — R15 shipped: an Obsidian connectivity pass in two halves — a vault **graph view** (`PNGraphify`) on `/notes/` and the tracker’s Obsidian tab that counts orphans and unresolved `[[wikilinks]]` and lets you fix both from the picture, and `tools/obsidian-sync.js`, which reads a real Obsidian vault folder into an importable bundle (upserts by vault path, so re-syncing updates instead of duplicating) with a `build.js` seam to bake it into the encrypted tracker payload. See [[Obsidian connectivity — graph view + vault sync]]. Earlier 2026-09-07 — R9 moved from parked to mostly-done: a session-side Google Calendar connector now feeds real events into the encrypted tracker (one-way, snapshot + rebuild, not live two-way sync — that still needs an OAuth backend), the Projects tab grew a compact \"main calendar\" under the board, and the to-do board (private + `/island/`) now sorts by workflow stage then target date instead of raw insertion order. Previous update 2026-09-06 — R13/R14 shipped (chart sidebar + notes, then V2 customization: candles, RSI/MACD, sort, remembered prefs) with a new `charts.test.js`; §5 PN Tasks added (Pierce's open-questions queue + idea backlog); §3a: 14 of the 22-item UI backlog built same-day, 8 deliberately deferred (each says why).\r\n\r\n---\r\n\r\n## 1. Now — in flight\r\n\r\n### R1. Robinhood MCP connection — **Pierce**, then Claude\r\nPierce is setting up the Robinhood MCP on his side. Once connected, Claude wires it into the price seam that already exists: `PNPrices.resolve()` prefers a `quotes` map baked into the encrypted payload (`tools/tracker/build.js` emits `quotes: {}` today). The goal: scheduled builds carry real prices so the tracker values itself with **zero** browser API calls.\r\n- Blocked on: Pierce's backend setup.\r\n- Never: brokerage credentials in the repo, in Obsidian, or in chat. Read-only data only. Claude does not execute trades or transfers under any setup.\r\n\r\n### R2. Tracker refresh secrets in GitHub Actions — **Pierce**\r\n`.github/workflows/refresh-tracker.yml` exists but cannot run until repo secrets are set: `TRACKER_PASSWORD` (the tracker PIN), `GOOGLE_SERVICE_ACCOUNT_JSON`, `SHEET_ID`. Until then the tracker is rebuilt locally with `node tools/tracker/build.js --local-snapshot`.\r\n- Done when: the scheduled workflow runs green and commits a refreshed `tools/tracker/index.html`.\r\n\r\n### R9. Google Calendar — ✅ one-way pull done 2026-09-07, two-way still parked\r\nThe static site still has no OAuth of its own and never will without a private backend (unchanged boundary — see [[Private Portfolio Calendar]] \"Known boundary\"). What changed: Claude has a session-side Google Calendar connector, so it pulled Pierce's real primary-calendar events (confirmed, next ~3 months, 130 events) straight from Google, normalized them, and saved the snapshot to `private/tracker/google-calendar.json` (gitignored, never committed). `tools/tracker/build.js` now has a `loadGoogleEvents()` seam — same shape as `loadCalendar()` for dividend dates — that bakes that snapshot into the encrypted payload as `googleEvents`. The tracker's Calendar tab merges them in (new \"Google\" filter, its own pill/day-event color) alongside dividend and private events.\r\n- **New: a \"main calendar\" under the to-do board.** The Projects tab now has a compact month grid directly beneath the kanban board (`renderBoardCalendar()`, `#board-calendar`) combining board target dates + dividend dates + private plans + the baked Google events — its own month anchor so paging it never disturbs the full Calendar tab. Clicking a day jumps to the full tab with that date selected; editing/export still only happen there.\r\n- **Also: the to-do board now sorts.** `PNTaskboard.sortGoals()` orders by workflow stage (Backlog → Planned → In progress → Blocked → Complete), then target date (soonest first, undated last), then title. Applied to `/island/`'s flat \"All hands\" list (previously raw seed order) and to card order within each column on the private board (previously insertion order).\r\n- **Island's own calendar stays public-safe.** `/island/` got its own small \"tide chart\" — a month grid of the board's own `targetDate`s (already public text on each plank) — not a copy of the private/Google data. No portfolio or personal-calendar content reaches the public page.\r\n- **Known boundary, unchanged:** this is a manual refresh, not live sync. Re-pulling means a Claude session calling the connector again and re-running `--local-snapshot`; there is no way for GitHub Actions or the static site itself to do this pull, since the connector is tied to a chat session, not a portable credential. True two-way sync is still exactly the OAuth-backend project described below.\r\n- Verified: `calendar.test.js`/`taskboard.test.js` pass, rebuilt with `--local-snapshot`, grepped the output for plaintext holdings and for any of the 130 fetched event titles — zero hits outside the encrypted blob.\r\n\r\n### R3. Investment handoff file — ✅ done 2026-09-06\r\nSeptember 2026 (48 holdings) confirmed by Pierce and sealed into the tracker alongside August. Month switcher and value-over-time chart both show two points now. Reopens automatically next time a new month lands: refresh `private/STOCK_HANDOFF.md` from the Sheet (or run the `stock-portfolio-update` skill with screenshots), create the new tab, rebuild.\r\n\r\n### R13. Chart-tab stock sidebar + inline outlook notes — ✅ done 2026-09-06\r\nThe Charts tab's stock picker moved from a horizontal chip row to a left-hand vertical list; each row gets a notes icon that inline-expands a private per-ticker \"outlook\" note (thesis, what you're watching) without leaving the chart. Reuses the same ticker-level note `openTicker()`/the Positions-tab `.notebtn` already created — one note per ticker, not per platform lot — so it's the same note wherever you reach it from, and it still exports to Obsidian. New `PNNotes.getTickerNote()` / `saveTickerNote()` in `assets/js/notes-ui.js`; sidebar + inline-expand markup in `tools/tracker/index.template.html` (`renderChartSidebar()`). Verified: template/inline-script parse clean, `calendar.test.js` + `taskboard.test.js` still pass, rebuilt with `--reuse-payload` and grepped the output for real holdings numbers — zero plaintext hits.\r\n- Next: Pierce click-tests it for real (the note field, symbol switching, mobile stacking below 760px) — flagged as an open item in **§5 PN Tasks**.\r\n- Also kicked off: a \"Version 2 — customization\" scope in [[Portfolio Stock Chart Plan]] — now largely built, see R14.\r\n\r\n### R14. Chart V2 — customization — ✅ done 2026-09-06\r\nFour of the six drafted V2 options shipped: **candlestick chart type** (falls back to the line when a provider returns closes only), **RSI 14 and MACD 12/26/9 sub-panels** stacked under the volume strip (this is what R12 called \"chart indicators v2\"), **sidebar A–Z / Value sort**, and **remembered display prefs** (type, oscillators, sort, range — `pn.chart.prefs`, per browser, display-only). Sidebar note markers came with it. Indicator maths (`ema`/`rsi`/`macd`) live in `assets/js/prices.js`; `charts.js` stays a renderer that receives aligned arrays.\r\n- **New test file:** `tools/tracker/charts.test.js` — alignment, RSI bounds, MACD's `hist === macd − signal` identity, and sign behaviour on rising/falling/flat series. BLUEPRINT §10 asked for chart fixtures and there were none. It earned its keep immediately: it caught a wrong expectation of mine (Wilder smoothing oscillates either side of 50 on an alternating series rather than converging on it) and, separately, a real bug where the remembered range was read before `CHART_RANGES` was initialised and silently fell back to 3M every load.\r\n- **Underwater theme** (added the same day, Pierce's ask): an opt-in Charts skin — ocean ground with caustics and bubbles, an open treasure chest presiding over the KPI row, and a hoard panel below it carrying gold at 17% opacity behind semi-translucent cells of the data the KPI row can't fit (including tracked cost basis and P/L per symbol when the vault is unlocked). CSS + inline SVG only; `prefers-reduced-motion` respected. Fits the site's existing nautical streak (`/island/`, the ship-in-a-bottle idea in §5).\r\n- `charts.test.js` also now guards the panel markup itself: it pulls the real `$('stockchart').innerHTML` expression out of the template, evaluates it for both themes, and walks the `<div>` depth — an unbalanced tag there would silently wreck the tab's layout with nothing to catch it. Verified the guard fails when a closing tag is removed, rather than trusting that it would.\r\n- Deliberately left open: manual drag-to-reorder the sidebar, and per-stock accent colors — reasons in [[Portfolio Stock Chart Plan]].\r\n- Still needs Pierce's eyes on the real thing: candles at each range, both oscillators on at once, the underwater theme, and the sidebar under 760px.\r\n\r\n### R4. Content decisions — **Pierce** (blocking Claude + ChatGPT)\r\nOpen questions that block copy on the public pages (details in [[CONTENT — piersonnorris.com]] `[OPEN]` markers):\r\n1. Club title: still \"incoming president\" or president now? (five files change together)\r\n2. The LLC's legal name — docs disagree: True North **Services** vs **Maintenance** vs North Shore Services (see CONTENT.md §3a).\r\n3. Name Ryan Cravens publicly, or keep \"operations lead\"?\r\n4. Oasis → True North: one continuous story or two chapters?\r\n5. Keep Student Maintenance LLC (2025) on the public site?\r\n6. Keep the \"before Elon\" prologue?\r\n7. Anything to add for the thin 2024/2025 years?\r\n\r\n## 2. Next — ready to build\r\n\r\n### R5. Live prices — **mostly done 2026-09-05**\r\nPierce supplied a Twelve Data key; it now lives in `private/.twelvedata-key` (gitignored) and `build.js` bakes a spot price for every holding symbol into the encrypted payload (chunked 8/minute for the free tier — a local build takes ~4 minutes). All value charts work with zero browser API calls. Remaining:\r\n- **Pierce:** paste the same key into the unlocked tracker's **Price source** panel once — that's what powers the Charts tab's *history* fetches (EMA lines on real candles), which happen client-side.\r\n- **Pierce, with R2:** add the key as the optional `TWELVEDATA_API_KEY` repo secret so scheduled CI builds bake fresh quotes too.\r\n\r\n### R6. Dividend research refresh — **Claude**, quarterly\r\n`private/tracker/dividend-calendar.json` holds researched ex/pay dates for ET, VDE, VZ, UPS, SGOV, NEE, VST, BOTZ. Dates go stale each quarter — re-verify against issuer IR pages, update the JSON, rebuild. Always recheck with the issuer before trading around a date.\r\n\r\n### R7. Public pages M2/M3 — **ChatGPT** (copy from CONTENT.md only)\r\nAbout, Projects, Contact, Tools hub, 404, `sitemap.xml`, per-page OG tags. Blocked partly on R4. Rules: copy verbatim from [[CONTENT — piersonnorris.com]], `[OPEN]` means ask Pierce, no frameworks, no build steps, keep `llms.txt` in sync.\r\n\r\n### R17. Backstage — the vault in public, no gate — ✅ P0–P3 done 2026-09-08, P4 waiting on Pierce\r\nFull plan and current status in [[BACKSTAGE — the vault, in public, with no gate]]; the tool half is written up in [[Obsidian connectivity — graph view + vault sync]] §3. Pierce asked to \"view the website back end Obsidian, no gate to get in, and a good visual,\" and chose **the whole personal vault, ungated** over two narrower scopes.\r\n\r\n- **The gate was never the work.** `/notes/` has no server behind it — the vault is browser-local, PIN-derived AES, on one machine, so removing the PIN would publish nothing. Making a vault publicly viewable means *committing a snapshot of it into this repo* and building a reader. That is a one-way door, which is why the phases are cut where they are.\r\n- **Shipped:** `/vault/` (\"Backstage\"), read-only and ungated — folder tree, rendered Markdown, backlinks/outgoing/unresolved inspector, per-note table of contents, ⌘K palette over notes *and* headings, `#path/to/note.md` deep links, and Reader / Graph / Timeline. `/notes/` and its PIN are untouched: one is a writing surface, the other a reading surface.\r\n- **The publish pipe:** `tools/obsidian-sync.js` gained `--report`, `--public`, `--label`, `--prefix`, `--waive` and `--no-link-paths`. Default is publish; a note opts out with `publish: false`, a `#private` tag or a `nopublish/` folder. The scrubber **refuses and prints `path:line`** rather than silently redacting, and named terms come from a gitignored `private/.publish-blocklist`. 26 tests in `tools/tracker/vault-publish.test.js`.\r\n- **New: a Markdown renderer.** `assets/js/markdown.js` (`PNMarkdown`), ~250 lines, no dependency, **escape-first** — every character is escaped before a tag is emitted, so raw HTML in a note is shown rather than executed. Headings, lists, task boxes, tables, fences, quotes, Obsidian callouts, wikilinks. Images are described, never fetched; `javascript:`/`data:` hrefs render as text. 24 tests.\r\n- **Visual:** Slate command center (Version 1 in `notes/visual-options/`), which **closes R10** — built over the atlas's own three-pane shell (`atlas.css`) rather than a second copy of it.\r\n- **What is actually published: the site's own `docs/` folder** — 11 notes already public in this repo. Deliberate: it exercises the whole pipe and makes the look judgeable *before* anything private ships. Pointing it at the real vault is one flag, and it waits on Pierce (§5, and [[BACKSTAGE — the vault, in public, with no gate]] §7).\r\n- **Two bugs found on the way.** (1) `atlas/index.html` was **gitignored and had never deployed** — the allowlist never got an `!/atlas/` pair, so R16 was linked from six pages and served a 404. Fixed, and `/vault/` allowlisted with it. (2) `PNGraphify` counted `[[links]]` written inside code spans, so a note documenting the syntax invented edges and inflated the unresolved count; fixed in the shared engine with tests, which took the `docs/` snapshot from 6 unresolved to 0. `/notes/` and `/atlas/` get the fix too.\r\n- **Also fixed:** `.atlas-head::before`'s glow bled 25% past the viewport and scrolled the page sideways at desktop widths. `overflow-x:clip` on both head sections.\r\n- **Not built on purpose:** tree virtualisation and the graph's largest-connected-component control. Eleven notes need neither; both become real work if P4 lands a few hundred.\r\n\r\n### R16. Knowledge atlas — `/atlas/` — ✅ done 2026-09-07\r\nA public, third Obsidian-shaped surface, and the first one that is **not** a vault: `/atlas/` is a three-pane workspace over 25 curated notes on what Pierce has learned and where. Explorer (domains → notes, plus source and tag filters), reader (frontmatter block, evidence chips, inline `[[wikilinks]]`, backlinks inspector), and three views — Reader, **Graph**, Timeline. ⌘K focuses search; `#note-id` deep-links a note.\r\n\r\n- **The graph is real.** It is `PNGraphify` — the same engine `/notes/` uses — fed the atlas notes, so every edge is an actual `[[wikilink]]` in the copy above it, not a decorative drawing. Tag nodes are toggled off on mount (33 tags against 25 notes buries the structure); the engine's own toggle puts them back. Current shape: 25 notes, 30 links, 2.4 per note, **0 orphans, 0 unresolved** — the status bar and the graph's stat strip count links the same way (unique undirected pairs) so they cannot disagree on one page.\r\n- **Data, not a vault.** `assets/js/atlas-data.js` is a curated public file. Every claim traces to [[CONTENT — piersonnorris.com]] (§3 takeaways, §4 skills, §7 cleared numbers); note titles reuse Pierce's own documented wording where a takeaway exists. Nothing marked `[OPEN]` is asserted — Student Maintenance LLC is left out, and Oasis → True North is described as two dated chapters, not a rebrand. The §7 \"never on the site\" list is respected: no client names, portfolio values or former-employer figures.\r\n- **Wiring.** Nav entry on all six pages plus the tracker template, `sitemap.xml`, and an `llms.txt` line. Page-scoped `body.atlas-page main.wrap{max-width:1240px}` — a three-pane workspace does not fit the site's 900px reading column, and the header/footer keep the shared width.\r\n- **Still open:** the atlas is hand-maintained. When [[CONTENT — piersonnorris.com]] changes, `atlas-data.js` has to change with it — same rule as the taskboard seed.\r\n\r\n### R15. Obsidian connectivity — graph view + vault sync — ✅ done 2026-09-07\r\nTwo halves, one goal: make the vault’s links visible, and get the real vault in front of them. Full write-up in [[Obsidian connectivity — graph view + vault sync]].\r\n\r\n- **Graph view.** New `assets/js/notes-graph.js` (`PNGraphify`), behind a **Graph** button in the notes toolbar on both `/notes/` and the tracker’s Obsidian tab. Force-directed SVG with pan/zoom, node dragging, neighbourhood highlight and click-to-open. `build()` is a pure function and is unit-tested (`tools/tracker/notes-graph.test.js`); the SVG half is not. It is deliberately about connectivity: **orphans** (no `[[wikilink]]` either way) and **unresolved links** (brackets pointing at notes that don’t exist) are two separate counts, and clicking an unresolved node writes that note. Sharing a `#tag` is drawn but is *not* counted as a link — conflating the two would flatter the vault.\r\n- **Vault sync.** New `tools/obsidian-sync.js` reads a real vault folder (skipping `.obsidian`/`.trash`), reuses the site’s own `PNVault.fromMarkdown` rather than reimplementing the parser, adds block-style YAML tags and aliases, and writes a `pn-vault-bundle` JSON to `private/notes/obsidian-vault.json` (gitignored) plus a connectivity report. It prints counts, never note text, unless you pass `--list`.\r\n- **Import upserts now.** `PNVault.importBundle()` matches on each note’s vault-relative path (also written into exported frontmatter as `source_path`), so re-running the sync after editing in Obsidian updates notes in place. The old `.md` path only ever *added* and skipped duplicates, which made repeat imports useless. Nothing is ever deleted.\r\n- **Baked route.** `build.js` gained `loadObsidianVault()` — drop the bundle at `private/tracker/obsidian-vault.json` and it seals into the payload as `obsidianVault`, exactly like `googleEvents`; the tracker’s Obsidian tab then offers a one-click merge. The merge is a button, never automatic.\r\n- **Known boundary, same as R9:** a snapshot, not live sync. The static site can’t read a folder on disk and CI can’t run the pull; refreshing means re-running the script on the machine that has the vault. Note text never enters the repo.\r\n- **Not verified in a browser:** the tracker’s merge banner. It needs the real tracker PIN and a real build, so it is code-reviewed and its `build.js` seam is unit-tested, but nobody has clicked it. Everything on `/notes/` was exercised live.\r\n\r\n### R8. Résumé PDF — ✅ done\r\n`/assets/resume/pierson-norris-resume.pdf` exists and the Experience page's button now links straight to it (`download` attribute) instead of the \"pending\" placeholder span.\r\n\r\n## 3. Later — parked on purpose\r\n\r\n- **R9b. Two-way Google Calendar sync** — a private OAuth backend so the tracker could write back to Google (or refresh itself without a Claude session in the loop). The one-way pull (Claude connector → snapshot → bake at build time) shipped 2026-09-07 — see R9 above; this is only the write-back / self-refreshing half, and it stays parked for the same reason it always has: no long-lived Google credentials belong on a public static site.\r\n- **R10. Obsidian visual style** — pick a direction from `notes/visual-options/` (local exploration) and apply it to `/notes/` and the tracker's Obsidian tab.\r\n- **R11. Real launch hardening** — before promoting the site: revisit the template page's deliberate demo login (`tools/tracker/index.template.html`, fake by design \"for now\" per Pierce 2026-09-05), run the BLUEPRINT §10 definition-of-done list, attach the `piersonnorris.com` domain (CNAME + absolute-URL sweep).\r\n- **R12. Chart indicators v2** — candlesticks, RSI/MACD, and EMA-crossover flags on the *stock chart* (corrected 2026-09-06 — this previously said \"vault graph,\" which is a different feature entirely), only if Pierce actually uses v1. Dividend markers and normalized comparison mode (the plan's own \"recommended next milestone\") shipped 2026-09-06 — see [[Portfolio Stock Chart Plan]] Version 2 status. Portfolio-aggregate line, total-return toggle, and a benchmark line stay parked until a dated transaction ledger exists — faking one off today's share counts would misrepresent performance. **Closed out 2026-09-06 by R14**: candlesticks, RSI and MACD all shipped as part of the V2 customization pass. EMA-crossover *flags* (an explicit marker when two EMAs cross) are the only piece of the original R12 wording not built — reopen it only if the crossings are hard to spot by eye now that the overlays and oscillators are both there.\r\n\r\n## 3a. UI polish backlog — audited 2026-09-06, 15/22 built\r\n\r\nA full pass over the live site (Home, Experience, Notes, Tracker, Island), verified against the actual code rather than assumed — contrast ratios were measured, the favicon/skip-link/og:image gaps were grepped for, not guessed. All 22 mirror the taskboard seed (`assets/js/taskboard.js`), so they show up on both the private Projects board and the public `/island/` page. 14 were built the same day; 8 stayed in `backlog` on purpose (see each item).\r\n\r\n**Accessibility & standards**\r\n- ✅ **U1 — Fix low-contrast meta text.** `--dim` measured 3.25:1 on `--bg`, below the 4.5:1 WCAG AA floor. Bumped to `#7a8392` (4.94:1).\r\n- ✅ **U2 — Restore visible focus rings.** Correction made *while* fixing this: input/textarea/select were already fine (a later `:focus-visible` rule with equal specificity already wins the outline back in the cascade) — only `.pnchart-graph .vg-node:focus` was genuinely broken, because its two-class selector (0,0,3,0) outranks the generic `[tabindex]:focus-visible` rule (0,0,2,0) regardless of source order. Added `.vg-node:focus-visible` with its own ring.\r\n- ✅ **U3 — Add a skip-to-content link.** Added to all five pages + 404.\r\n- ✅ **U4 — Ship the favicon.** A bold green \"P\" mark, dark rounded-square ground, as an SVG (`assets/img/favicon.svg`) linked from every `<head>`.\r\n- 🔲 **U5 — Complete Open Graph + og:image.** Still open: a compliant og:image needs a real raster (PNG/JPG) asset — Facebook/Twitter's crawlers don't reliably render SVG og:images, and this session has no image-generation tool. Needs a dedicated design pass.\r\n\r\n**Navigation & information architecture**\r\n- ✅ **U6 — Mobile nav menu** (built 2026-09-06, its own pass). Hamburger toggle (`assets/js/nav.js`) shared across all six pages, collapsing `.navlinks` under 760px into a push-down panel. Caught a real trap: Experience defines its own unconditional `.navlinks{display:flex}` which — equal specificity, later in the cascade than site.css — would have silently beaten a site.css-only fix, so it got its own matching override. Verified at true desktop width (toggle hidden) and mobile (opens, closes, auto-closes on link click) on all six pages including the real encrypted tracker.\r\n- 🔲 **U7 — One real icon set.** Deferred as a real design project; U15's empty-state icon is a first small step toward it.\r\n- ✅ **U8 — Wire the home feed to `updates.js`.** Re-scoped before building: `updates.js` tracks *website-engineering* changes, wrong subject matter for a career-facing homepage. Pinned an explicit sync-note comment to the real source of truth (the Experience page / CONTENT.md §3) instead.\r\n- 🔲 **U9 — Site-wide \"last updated\" stamp.** Re-scoped: `updates.js` is the wrong source here too, for the same reason as U8. Needs someone to decide what \"last updated\" should mean for a *public* page before anything gets built.\r\n\r\n**Tracker-specific UI**\r\n- ✅ **U10 — Sticky first column** on wide platform tables (verified via computed style: `position:sticky`).\r\n- ✅ **U11 — Show/hide toggle on the API-key field.**\r\n- 🔲 **U12 — Compact agenda view for the calendar on mobile.** Deferred: touching the calendar's rendering carries real regression risk; wanted its own focused pass.\r\n- 🔲 **U13 — Loading skeletons.** Deferred for time.\r\n- ✅ **U14 — Group the Charts tab's control rows** into three labeled groups (Stock / Range / Indicators).\r\n- ✅ **U15 — Friendlier empty states.** Checked all three named cases first: the Obsidian vault's blank state really was plain text (got an icon); the Calendar's per-day state and the board's per-column state were already dashed-box/friendly-copy — left those two alone.\r\n- ✅ **U16 — Row-hover highlight** across tracker tables.\r\n\r\n**Visual polish & consistency**\r\n- 🔲 **U17 — Consolidate duplicated component CSS.** Deferred: real but risky, needs its own careful pass rather than a rushed refactor.\r\n- ✅ **U18 — Subtle entrance animation** on Home's console tiles and the Experience timeline's year sections, via a shared `.enter-fade` keyframe respecting `prefers-reduced-motion`.\r\n- ✅ **U19 — Build the 404 page.**\r\n- ✅ **U20 — `theme-color` meta tag** on every page (island uses its own sky-blue).\r\n- 🔲 **U21 — One radius/shadow scale.** Actually counted it: 13 distinct border-radius values (2–16px) across the repo, not a small drift. Forcing them all onto the existing two tokens would visibly break small elements (a 3px nail dot doesn't want a 10px radius) — the real fix is a proper 4–5 step scale mapped by hand, not a blind find-replace. Deferred rather than done carelessly.\r\n- ✅ **U22 — Tie the Island page's look back to the brand** — added a soft ground shadow under the sign board so it reads as sitting in the sand.\r\n\r\n## 4. Standing rules (all three of us)\r\n\r\n1. **Secrets never in the repo.** No PIN, keys, tokens, holdings, or client data in tracked files, commit messages, or chat meant for the public build. `private/` is never allowlisted in `.gitignore`.\r\n2. **[[CONTENT — piersonnorris.com]] is the source of truth for public copy.** [[BLUEPRINT — piersonnorris.com]] for architecture. Don't invent facts; ask Pierce.\r\n3. **Plain HTML/CSS/JS.** No frameworks, npm dependencies, or build steps for the site itself (the tracker's Node build script is the one sanctioned build).\r\n4. **The tracker ships encrypted.** Only `tools/tracker/build.js` writes `tools/tracker/index.html`; verify no plaintext holdings after every build.\r\n5. **Board ↔ Obsidian ↔ this file stay in sync** (see sync rule at top).\r\n6. **Questions for Pierce get logged, not just asked in chat.** Anything Claude or ChatGPT can't resolve without Pierce goes in **§5 PN Tasks → Open questions** so it survives past one session.\r\n7. **Local rebuild + test loop:** `node tools/tracker/build.js --local-snapshot`, `node tools/tracker/calendar.test.js`, `node tools/tracker/taskboard.test.js`, `node tools/tracker/charts.test.js`, then click through the demo page.\r\n\r\n## 5. PN Tasks — Pierce's queue\r\n\r\nA dedicated inbox for anything that specifically needs Pierce: an open question Claude or ChatGPT can't resolve alone, or a feature idea that needs his call before anyone starts building. Standing rule (also in §4): **log it here as soon as it comes up — don't just ask in chat and let the answer evaporate.** Every entry — question or idea — carries a **Source** line (who raised it, where, when) so it's traceable later instead of a bare, unattributed bullet. Mirrors `TASKS.md` and the taskboard seed (`assets/js/taskboard.js`) like everything else in this file.\r\n\r\n### Open questions for Pierce\r\n\r\n- **Backstage (R17) — four calls before P4, the one irreversible step.** `/vault/` is live and ungated, but it is publishing the site's own `docs/` folder, not the personal vault. Before pointing it at the real one: run `node tools/obsidian-sync.js --vault \"<vault>\" --report`, read the list it prints, and answer these. (1) Is the first `--report` allowed to change the answer, if it turns out to be 300 short daily notes? (2) Client and colleague names appear in the vault — publish, initial, or omit? (3) Is the `docs/` snapshot a stepping stone, or is \"Backstage = how this site is built\" actually the better permanent page? (4) Seven top-level nav links is a lot — does `/vault/` group under Notes, or stand alone? Detail in [[BACKSTAGE — the vault, in public, with no gate]] §7.\r\n  **Source:** Claude, drafting the Backstage plan, 2026-09-08.\r\n\r\n### Idea backlog — needs Pierce's decision before anyone builds it\r\n\r\n- **Ship-in-a-bottle Easter egg** — 🔨 entrance built 2026-09-07, viewer still open. Pierce picked the placement himself (\"homepage and the to-do list\"), so the *entrance* half is built: `assets/js/bottle.js` + `assets/css/bottle.css` (`PNBottle.mount()`), one component mounted twice — under the Currently card on Home, and washed up in the sand on `/island/`, wearing each page's own palette via `--pnb-*` variables. Click it, the cork pops, the ship sails out, and a porthole dialog opens onto the vault's front door: a generated constellation sketch, a lamp saying whether a vault exists *on this device* (key-presence only — the same check the home console already does), and links into `/notes/` and the stock desk. Esc/scrim close it and focus returns to the bottle.\r\n  **Deliberately not built: the viewer itself.** The original idea had the ship open \"an embedded Obsidian-vault viewer.\" A *public* page cannot show one — the notes are encrypted behind a PIN and decrypt only in the owner's browser — so anything drawn there would have to be faked, which would misrepresent what the site does. The porthole says so in its own copy, and the real viewer is `/notes/`, one click away. Making *that* polished is still **R10**, and it is still Pierce's design call: pick a direction from `notes/visual-options/` (Slate command center / Paper canvas / Research studio).\r\n  **Source:** Pierce, Cowork chat with Claude, 2026-09-06 — his own words: \"reaching a visual obsidian with some [Easter] eggs on the website like a shipping [bottle] ... in addition to something like that on the home site too that looks small, but has impact.\" Placement decided by Pierce 2026-09-07.\r\n\r\n- **Chart V2 — the two options left on the table.** Four of the six drafted customizations shipped (R14). Still Pierce's call: **manual drag-to-reorder** the sidebar (A–Z and Value may already cover it), and **per-stock accent colors** (comparison mode already auto-assigns distinguishable ones). Neither gets built unless he says the current behaviour actually annoys him.\r\n  **Source:** Pierce, Cowork chat with Claude, 2026-09-06 — \"Start implementing V2.\"\r\n\r\n- **Click-test the chart V2 build.** Candles at each range, RSI and MACD both on at once, the A–Z/Value sort, and how the sidebar stacks under 760px. Claude verified it by tests and code review — the browser tooling was down this session, so nobody has actually *looked* at it yet.\r\n  **Source:** Claude flagged it, 2026-09-06.\r\n","tags":[],"created":"2026-09-08T15:14:10.604Z","updated":"2026-09-08T15:14:10.604Z"},{"path":"docs/SETUP_CHECKLIST.md","folder":"docs","title":"SETUP CHECKLIST — work through bit by bit","body":"# SETUP CHECKLIST — work through bit by bit\n\nMirrors the Build HQ page. Check items off here (or there) and tell Claude/ChatGPT what's done.\n\n## Phase 1 — this week (no code, ~45 min)\n\n- [ ] Confirm (or pick) the GitHub username — it becomes `username.github.io`; `piersonnorris` is ideal if free\n- [ ] Install GitHub Desktop (desktop.github.com) + VS Code (code.visualstudio.com)\n- [ ] Pick the public contact email (norrispierce506@gmail.com or a cleaner alias)\n- [ ] LinkedIn: claim a custom URL (currently `/pierson-norris-634954246`) + add Screencastify and True North entries\n- [ ] Choose the tracker password → password manager only; later into GitHub Secrets; never into any chat\n- [ ] React to the timeline (accent A/B/C, confirm chips, 2024/2025 gaps, the 2027 headline)\n\n## Phase 2 — Google Cloud (tracker prep, ~25 min; click-by-click in the private Drive spec)\n\n- [ ] Create a Google Cloud project (e.g. `pierce-asset-tracking`) + enable the Google Sheets API\n- [ ] Create a service account + download its JSON key (treat like a password)\n- [ ] Share the Asset Tracking sheet with the service-account email — Viewer, notify unchecked\n\n## Phase 3 — when the build starts (with ChatGPT)\n\n- [ ] Create the repo `piersonnorris.github.io` (public) + enable Pages (Settings → Pages → main / root)\n- [ ] Add Actions secrets: `GOOGLE_SERVICE_ACCOUNT_JSON`, `TRACKER_PASSWORD`, `SHEET_ID`\n- [ ] ChatGPT Project: upload the `docs/` files, paste `HANDOFF_PROMPT.md`\n\n## Later — post-launch\n\n- [ ] Buy piersonnorris.com (~$10–12/yr) → CNAME + DNS to Pages; update absolute URLs (blueprint §7)\n- [ ] Privacy-friendly analytics: GoatCounter (free) or Plausible — one script tag\n- [ ] Headshot for About (fresh, or Claude screens the May 2026 Drive photo set)\n- [ ] Install the monthly-tab Apps Script on the sheet (written; in the private spec)\n- [ ] Refresh the resume PDF → `/assets/resume/pierson-norris-resume.pdf` (Claude drafts it from CONTENT.md once timeline gaps close)\n","tags":[],"created":"2026-09-08T15:14:10.604Z","updated":"2026-09-08T15:14:10.604Z"},{"path":"docs/STOCK_CHART_PLAN.md","folder":"docs","title":"Portfolio Stock Chart Plan","body":"# Portfolio Stock Chart Plan\n\n## Outcome\n\nThe portfolio tracker should feel like a focused private market terminal: fast enough to check daily, clear enough to understand at a glance, and careful about keeping holdings and credentials out of the public repository.\n\nThe first implementation is now part of the gated **Charts** tab. It charts only included equity positions for the active month and does not expose stock symbols until the tracker has been unlocked.\n\n## Product principles\n\n1. **Private by default.** Holdings stay inside the encrypted tracker payload. API keys stay in browser storage and are never committed or sent to the build workflow.\n2. **Useful before complex.** Lead with price direction, volume, range return, and position context. Add comparison and attribution only when their calculation can be explained.\n3. **Honest data states.** Clearly distinguish live provider data, cached data, manually entered prices, loading, and unavailable history.\n4. **Portfolio-aware.** Show charts only for stocks the user owns and currently includes in the portfolio scope.\n5. **Accessible everywhere.** Pointer and keyboard inspection, screen-reader data tables, high-contrast states, and a mobile layout are required features.\n\n## Version 1 — implemented\n\n### Chart workspace\n\n- An owned-stock switcher derived from the active portfolio month.\n- A default selection based on the largest priced included equity position.\n- Range controls for 1 month, 3 months, 6 months, and 1 year.\n- Daily close-price line with a positive/negative area treatment.\n- Daily volume bars colored by up/down session when OHLC data is available.\n- A dashed reference at the first close in the selected range.\n- Pointer crosshair and detail tooltip for date, close, high, low, daily move, and volume.\n- Keyboard inspection with Left, Right, Home, and End.\n- Summary metrics for range return, high, low, average close, shares owned, and position value.\n- Responsive two-column metrics on small screens.\n- A disclosure arrow on every stock row that opens its private purchase journal.\n- Multiple dated buy-price lots with average buy, tracked cost basis, and tracked unrealized gain/loss.\n- Autosaved internal notes stored in the encrypted stock vault and included in Obsidian exports.\n\n### Data behavior\n\n- Twelve Data requests trading-session counts appropriate to each selected range.\n- Finnhub requests calendar windows large enough to cover the selected trading range.\n- History is cached separately by provider, symbol, and range for 15 minutes.\n- Expired history cache entries are pruned after 24 hours.\n- Manual pricing still supports portfolio valuation, but the history chart explains that a live market-data source is required.\n- Switching symbol or range ignores stale in-flight responses so an older request cannot overwrite the newest selection.\n\n### Security boundary\n\n```text\nEncrypted tracker payload\n        |\n        v\nUser unlocks tracker --> included equity symbols are derived in memory\n                                      |\nBrowser-only API key -----------------+\n                                      |\n                                      v\n                         read-only history request\n                                      |\n                                      v\n                         15-minute local cache --> SVG chart\n```\n\n- The public template contains no real holdings.\n- The browser sends a ticker to the configured provider only after unlock.\n- Market-data credentials are not brokerage credentials and must remain read-only.\n- The generated production tracker remains password protected; the source template remains a non-sensitive shell.\n- Purchase lots and internal notes are encrypted in the device-local stock vault; plaintext exists only while unlocked or when the user explicitly exports Markdown for Obsidian.\n\n## Version 2 — portfolio comparison\n\n### Status — 2026-09-06\n\nTwo pieces shipped, price-only, with no invented data:\n\n- **Dividend event markers**, on the single-stock chart. Confirmed ex-date/pay-date (from the researched private JSON) render as solid diamonds; because a price chart is backward-looking, \"estimated\" markers are projected *backward* from the confirmed pay date at the symbol's own stated frequency, filling in prior quarters inside the visible range — never a forward guess, never a date without support. Hover/focus each marker for its date via a native `<title>`.\n- **Multi-select comparison mode**, a \"Compare\" toggle next to the single-stock view. Up to 5 owned equities at once, each series independently normalized to 100 at the start of the selected range, with a legend and hover tooltip showing each symbol's return. `PNCharts.compare()` in `assets/js/charts.js`.\n\n### V1 addition — 2026-09-06\n\nThe stock switcher moved from a horizontal chip row to a left-hand vertical list (Pierce: \"an area to click right of a stock's name on the left with notes to add\"). Each row now carries a notes icon that inline-expands a private per-ticker outlook note — same note `openTicker()` already reaches from the Positions tab, just one more way in. See ROADMAP R13.\n\n### Version 2 — customization — built 2026-09-06\n\nPierce asked to start defining what \"the chart should be customizable\" means, then to implement it. Four of the six drafted options shipped; two are still open on purpose (below).\n\n**Shipped**\n\n- **Chart type** — Line (the existing close-price area) or **Candles** (OHLC bodies + wicks). Candles only engage when at least 60% of the returned points actually carry open/high/low; below that the chart falls back to the line rather than drawing a row of one-tick bodies, because some providers return closes only. In candle mode the axis widens to the highs and lows so wicks can't clip.\n- **Oscillator sub-panels** — **RSI 14** (0–100 scale with 30/50/70 guides) and **MACD 12/26/9** (histogram + MACD and signal lines around a zero line), each rendered as its own panel stacked under the volume strip. The SVG grows downward instead of squeezing the price plot; with neither enabled the geometry is exactly what it was. Both read out in the hover/keyboard tooltip alongside the EMAs. This is what R12 called \"chart indicators v2.\"\n- **Sidebar order** — an A–Z / Value toggle at the top of the stock list. Value sorts by current position value, falling back to alphabetical on ties and unpriced rows so the list never reshuffles at random.\n- **Remembered display prefs** — chart type, oscillator toggles, sort mode, and selected range persist per browser in `pn.chart.prefs`, so a re-lock doesn't reset the view. Display choices only; nothing about holdings leaves the encrypted payload.\n- **Sidebar note markers** — a ticker that already has a saved outlook note shows a small dot on its row (with a screen-reader equivalent), so you can see at a glance which stocks you've written about without opening each one.\n\n- **Underwater theme** — an opt-in skin for the Charts panel (Theme: Deep / Underwater). Ocean gradient ground with drifting caustic light and slow-rising bubbles; an open **treasure chest** presides over the KPI row, so the range figures read as what spilled out of it; and below it a **hoard panel** carries scattered gold at 17% opacity behind semi-translucent cells of the numbers the KPI row has no room for — shares held, position value, last close, average close, range high/low, which platforms hold it, and (when the vault is unlocked) tracked buys, average buy, and tracked P/L summed across that symbol's purchase journals. All CSS and inline SVG, drawn in the template, because the encrypted tracker ships as one self-contained file. Motion respects `prefers-reduced-motion`. It repaints; it never changes a number, and a figure with no source renders as an em dash rather than a guess.\n\n**Still open, deliberately**\n\n- **Manual drag-to-reorder** the sidebar — A–Z and Value cover the real need; drag ordering needs persisted per-symbol positions and touch/keyboard equivalents, which is its own scope rather than a bolt-on.\n- **Per-stock accent color** — the weakest of the six: comparison mode already assigns distinguishable colors automatically, so a manual override mostly risks two stocks ending up the same shade. Worth doing only if the auto-assignment actually annoys.\n\n**Indicator maths** live in `assets/js/prices.js` (`ema`, `rsi`, `macd`) and are covered by `tools/tracker/charts.test.js` — alignment (warm-up slots stay null, arrays stay input-aligned), the RSI 0–100 bound, MACD's `hist === macd − signal` identity, and sign behaviour on rising/falling/flat series. `charts.js` stays a pure renderer: the caller computes the series and passes aligned arrays in, exactly as EMA overlays already worked.\n\nDeliberately **not** built yet, because they need data this session doesn't have honestly:\n- **Portfolio aggregate line** and the **Price return / Total return toggle** — both require the dated transaction ledger below. Building either off \"today's shares, applied to the whole range\" would misrepresent performance, which this plan already flags as a risk.\n- **Benchmark line** — reasonable to add once comparison mode has real use; skipped for now to keep scope tight.\n\n### Features (original spec, for what's still open)\n\n- Portfolio aggregate line built from dated share counts and historical closing prices.\n- Optional benchmark line, initially the user's chosen broad-market ETF.\n- A toggle between **Price return** and **Total return**.\n\n### Data additions\n\nAdd a dated transaction model rather than assuming today's shares were owned for the full range:\n\n```json\n{\n  \"symbol\": \"SAMPLE\",\n  \"date\": \"2026-01-15\",\n  \"type\": \"buy\",\n  \"shares\": 2.5,\n  \"price\": 100.00\n}\n```\n\nUntil this history exists, any aggregate portfolio line must be labeled as a **current-holdings simulation**, not historical performance.\n\n## Version 3 — live session view\n\n### Features\n\n- Optional intraday ranges: 1D and 5D.\n- Last-updated timestamp and market-session state.\n- Automatic refresh while the page is open, with a user-visible pause control.\n- Watchlist separate from owned positions.\n- Price and percentage alerts stored locally first; external notifications require an explicit service integration.\n- Provider adapter interface so a future stock-data MCP or brokerage connector can supply the same normalized quote/history shape.\n\n### Guardrails\n\n- Respect the provider's free-tier request and rate limits.\n- Back off after provider errors; never retry continuously.\n- Stop polling when the tab is hidden or the tracker is locked.\n- Never request trading permissions for a read-only tracker.\n\n## Version 4 — portfolio intelligence\n\n### Features\n\n- Contribution to gain/loss by position.\n- Realized versus unrealized return after transactions are available.\n- Dividend income overlay and yield-on-cost.\n- Allocation drift against user-set targets.\n- Volatility, drawdown, and concentration indicators with plain-language explanations.\n- Exportable monthly snapshot for the Obsidian vault using the existing encrypted sync path.\n\nThese calculations should not ship until transaction history, splits, dividends, fees, and corporate actions reconcile against the source data.\n\n## Provider strategy\n\nThe UI consumes one normalized history record regardless of provider:\n\n```json\n{\n  \"date\": \"2026-08-21\",\n  \"label\": \"08-21\",\n  \"open\": 101.20,\n  \"high\": 104.00,\n  \"low\": 100.80,\n  \"close\": 103.50,\n  \"value\": 103.50,\n  \"volume\": 1234567\n}\n```\n\nThe provider adapter owns authentication, response mapping, rate-limit errors, and unsupported-symbol handling. The chart renderer never receives an API key and never depends on provider-specific fields.\n\n## UI states\n\nEvery chart state must have an explicit visual treatment:\n\n| State | Required behavior |\n|---|---|\n| Locked | Render no holdings, symbols, prices, or provider requests. |\n| No equities | Explain that only included stock positions are charted. |\n| Setup needed | Link the user conceptually to Price source and explain read-only keys. |\n| Loading | Keep the selected ticker/range visible and show what is being requested. |\n| Ready | Show data source, range, session count, and cache duration. |\n| Empty history | Identify the symbol and suggest changing provider or range. |\n| Provider error | Preserve the controls and show a concise provider message. |\n| Stale cache | In a future release, show the cached timestamp and offer a manual refresh. |\n\n## Accessibility and responsive requirements\n\n- Chart SVG has an accessible title and description.\n- The same daily close series is available in a visually hidden table.\n- Stock and range controls expose selected state with `aria-pressed`.\n- Tooltips are available through both pointer and keyboard interaction.\n- Color never carries the only indication of gain or loss; signed numbers remain visible.\n- Focus rings must remain visible against the dark background.\n- At 320 CSS pixels wide, controls may scroll horizontally but the page must not overflow.\n- Reduced-motion preferences must be respected if chart transitions are introduced later.\n\n## Validation plan\n\n### Automated checks\n\n- Parse-check `assets/js/prices.js`, `assets/js/charts.js`, and the template's inline application script.\n- Build the tracker with fabricated sample input and verify that the encrypted output contains no sample plaintext after encryption.\n- Test provider response mapping with successful, empty, malformed, and rate-limited fixtures.\n- Test each range's cache key and the 15-minute expiration boundary.\n- Test stale-response protection by changing ticker and range before the first request resolves.\n- Test range return, high, low, and average calculations with fixed fixtures.\n- Test purchase-journal encryption and Markdown export/import round trips without duplicated generated tables.\n\n### Manual checks\n\n- Unlock with valid and invalid passwords.\n- Verify that no market request happens before unlock in the network panel.\n- Check all four ranges for at least two owned stocks.\n- Inspect the first, middle, and last point with pointer and keyboard.\n- Verify positive, negative, flat, missing-volume, and one-point histories.\n- Check mobile widths, long ticker lists, zoom at 200%, and high-contrast focus.\n- Confirm that excluding a portfolio removes its stocks from the chart selector.\n\n## Acceptance criteria\n\nVersion 1 is complete when:\n\n- The Charts tab displays an interactive price-and-volume chart for every included equity symbol returned by the active portfolio month.\n- Range controls load the intended amount of history and cannot be overwritten by stale requests.\n- Summary numbers agree with the returned history series.\n- No holdings or API key appear in committed public source or build logs.\n- The chart remains usable with keyboard navigation and at mobile widths.\n- Loading, setup, empty, and error states are understandable without opening developer tools.\n\n## Risks and mitigations\n\n| Risk | Mitigation |\n|---|---|\n| Free API quota is exhausted | Cache history, request only the active symbol/range, and avoid background polling in v1. |\n| A provider changes its response | Keep normalization in the provider adapter and cover it with fixtures. |\n| Historical portfolio return is misleading | Do not claim performance until dated transactions exist; label simulations clearly. |\n| Password protection is mistaken for server security | Keep real data encrypted at build time and document that the static host sees ciphertext. |\n| Real ticker names leak through analytics or logs | Do not log symbols and keep analytics off gated stock surfaces. |\n| Small-screen charts become unreadable | Preserve the full-width plot, reduce surrounding chrome, and allow control strips to scroll. |\n\n## Recommended next milestone\n\nAfter Version 1 has been used for a few weeks, prioritize dividend markers and normalized comparison mode. They add the most insight without requiring brokerage write access. Historical portfolio-performance claims should wait for a reliable transaction ledger.\n","tags":[],"created":"2026-09-08T15:14:10.604Z","updated":"2026-09-08T15:14:10.604Z"},{"path":"docs/STOCK_UPDATE_PROMPT.md","folder":"docs","title":"STOCK UPDATE PROMPT — paste into ChatGPT (vision-capable model, e.g. GPT-4o) to update the Asset Tracking Sheet from screenshots","body":"# STOCK UPDATE PROMPT — paste into ChatGPT (vision-capable model, e.g. GPT-4o) to update the Asset Tracking Sheet from screenshots\n\nNo real holdings live in this file — it's a reusable template. Pierce fills in the \"Starting context\" section each time he runs it, then uploads screenshots of each platform's app as ChatGPT asks for them. ChatGPT has no write access to the Sheet; it hands back a block formatted to paste directly into the grid — no reformatting, no \"split text to columns\" needed.\n\n(Claude/Cowork can also run this same walkthrough directly with photos, using the \"stock-portfolio-update\" skill — this file is for running it in ChatGPT instead.)\n\n---\n\n## Paste everything below this line into ChatGPT as the first message\n\nYou're helping Pierson Norris (goes by Pierce) update the \"Stock flows\" tab of his private \"Asset Tracking\" Google Sheet from screenshots of his brokerage/crypto apps. You do not have write access to his Google Sheet — your job is to extract the numbers from images he uploads and hand him back a block formatted to paste directly into the Sheet's grid.\n\n### The five platforms, in this exact order\n1. Sofi\n2. Webull\n3. Robinhood\n4. Gemini\n5. GoMining\n\n### How this works — go one platform at a time\nFor each platform above, in order:\n1. Ask Pierce to upload one or more screenshots of that platform's app/site showing his current positions — or tell you \"no change\" if nothing's different since last time.\n2. If he uploads image(s): read every position visible — ticker or label, share/coin count or dollar amount, and anything in the notes worth keeping (e.g. \"auto-invest sub-account,\" \"option contract,\" \"crypto combined value ~$X\"). Read numbers carefully — decimals matter (share counts like 12.34 or 0.5678 are normal, not typos) — and if a number is genuinely unreadable or ambiguous, ask Pierce rather than guessing.\n3. Show him what you extracted for that platform as a quick table and get his confirmation or correction before moving on.\n4. Once confirmed, move to the next platform. If he says \"no change,\" carry forward that platform's numbers unchanged from the starting context below (ask him to supply it if it's missing).\n\nDon't skip ahead or batch platforms — go through the list one at a time.\n\n### Output format — TAB-separated, paste-ready (must match exactly; this also feeds an automated parser later)\nUse a real TAB character between fields, never commas or spaces — that's what lets Google Sheets auto-split the paste into columns instead of dumping it into one cell.\n\nEach platform section, one row per line:\n```\n<Platform>[TAB]Amount[TAB]Unit[TAB]Notes\n<Label/ticker>[TAB]<number>[TAB]Shares|Units|USD[TAB]<notes or blank>\n...\n```\n- Unit is exactly one of: `Shares` (equity/ETF share counts), `Units` (crypto coin counts), `USD` (already a dollar figure — cash, options, robo sub-accounts, \"total value\" style lines).\n- Never sum or total a section — mixed units can't be added together. No totals row, ever.\n- Leave the Notes field blank (but keep the tab before it) rather than guessing when nothing is stated.\n\nAfter all five platforms are confirmed, output the complete tab as ONE code block, in platform order, with a single blank line between each platform's section — the exact shape of a \"<Month> <Year>\" tab in the Sheet (e.g. \"September 2026\"). Start the block with two title rows (each its own line, no tabs needed):\n```\nStock flows — <Month> <Year> update\nDictated by Pierce on <today's date>. Share counts are as stated; dollar totals need current market prices to compute.\n```\nTell Pierce exactly how to use it: open the new (or current) month's tab in the Sheet, click cell A1, paste once — Sheets will drop every field into its own cell and every row into its own line automatically, because it's tab-delimited.\n\n### Starting context (Pierce: paste this in right after this prompt, before the first upload)\n[PASTE the most recent confirmed tab content here — or point ChatGPT at private/STOCK_HANDOFF.md's tables if that's more current. This is what lets ChatGPT know what \"no change\" means per platform.]\n\n### Ground rules\n- This is real financial data — never suggest posting it anywhere public, and don't repeat the numbers back in a form meant to be shared outside this chat.\n- Ask, don't assume, whenever a screenshot is blurry, cropped, or a number could be read two ways.\n- If Pierce uploads a screenshot for the wrong platform (e.g. a Robinhood screenshot while you're on the Webull step), point it out rather than silently filing it under the current step.\n- When finished, remind Pierce this output goes into the Google Sheet, not the website repo — the repo's private/STOCK_HANDOFF.md and the site's password-gated tracker just consume whatever the Sheet has next time either one gets refreshed.\n","tags":[],"created":"2026-09-08T15:14:10.605Z","updated":"2026-09-08T15:14:10.605Z"}]};
