/* Generated by tools/obsidian-sync.js --public. Do not edit by hand. */
window.PNVaultPublic = {"format":"pn-vault-public","version":1,"label":"Site docs","generatedAt":"2026-09-15T21:06:44.136Z","linkedPaths":37,"waived":["money"],"count":9,"notes":[{"path":"docs/BACKSTAGE_PLAN.md","folder":"docs","title":"BACKSTAGE — the vault, in public, with no gate","body":"# BACKSTAGE — the vault, in public, with no gate\n\n**Status: P0–P3 built 2026-09-08. P4 (publishing the personal vault) is waiting on Pierce.**\nDrafted and executed the same day, for ROADMAP **R17**.\n\n> **Where this stands.** `/vault/` is live, ungated, and reads a real published snapshot — but the snapshot it\n> currently carries is the site's own `docs/` folder, every file of which was already public in this repo. That\n> was deliberate: it exercises the whole pipe end to end and makes the visual judgeable **before** a single\n> private note is committed. Pointing it at the personal vault is one command (`--vault \"<vault>\"`), and it\n> should not be run until §7's questions are answered — see P4 in §6.\nDecision on record: Pierce, 2026-09-08 — *\"view the website back end Obsidian, no gate to get in, and a good visual.\"*\nAsked which content, he picked **the whole personal vault, ungated** over the two narrower options.\n\n---\n\n## 0. The thing to understand before anything else\n\n**\"No gate\" is not a PIN you delete. The vault is not on the website today.**\n\n`/notes/` is not a viewer pointing at a vault on a server. It is browser-local storage: notes live in\nIndexedDB in *one* browser, sealed with AES-256-GCM under a PBKDF2 key derived from the PIN, and the PIN\nprotects data that never left the machine. Delete the PIN prompt and a stranger opening `/notes/` sees an\nempty page, because there was never anything on the server for them to see. Pierce's own vault sits on his\ndisk; the only way it reaches the browser today is the Import button.\n\nSo the real work is the opposite of removing a lock: **publishing a copy of the vault into the repo** and\nbuilding a reader for it. Every hard question in this plan follows from that one sentence — once the notes\nare in a public GitHub repo they are public permanently, including in git history, including anything a\nlater scrub removes.\n\nSecond thing to know: **the repo's `.gitignore` denies everything by default** (`*`, then an allowlist),\nbecause this folder doubles as Pierce's working vault. That is the single control that has kept the notes\noff the internet for the life of the project. It is also, right now, silently eating a finished feature —\nsee §1.\n\n---\n\n## 1. Standing bug this plan must fix first\n\n`atlas/index.html` — the knowledge atlas built 2026-09-07 (R16), wired into the nav, `sitemap.xml` and\n`llms.txt` on every page — **is gitignored and has never deployed.**\n\n```\n$ git check-ignore -v atlas/index.html\n.gitignore:7:*    atlas/index.html\n```\n\n`/assets/**` is allowlisted, so `atlas.css`, `atlas-data.js` and `atlas-ui.js` show up as untracked and\nwould commit fine; the page they exist for does not. Six pages currently link to a live 404. The allowlist\nnever got an `!/atlas/` pair, the way `!/island/` and `!/notes/` have. The same trap will swallow `/vault/`.\n\n**Fix before anything else ships:** add `!/atlas/` + `!/atlas/index.html` to `.gitignore`, commit the four\natlas files together, confirm `/atlas/` loads on Pages. One line of fix, five minutes of verification.\n\n---\n\n## 2. Current connectors — what already exists\n\nEverything below is built and working. The new surface should add exactly one connector, not replace these.\n\n| # | Connector | Direction | Where it lives | Gate | Reaches the public web? |\n|---|---|---|---|---|---|\n| C1 | **Vault engine** — `PNVault`: `fromMarkdown` / `toMarkdown` / `importBundle`, wikilink resolution, backlinks | in-browser | `assets/js/vault.js` | PIN | No |\n| C2 | **Notes UI** — reader, tag filter, editor, backlinks panel | in-browser | `assets/js/notes-ui.js`, `/notes/` | PIN | No |\n| C3 | **Graph engine** — `PNGraphify.build()` (pure, unit-tested) + `mount()` (force-directed SVG) | in-browser | `assets/js/notes-graph.js` | none itself | Yes, via C6 |\n| C4 | **Vault sync** — walks a real vault folder, calls C1's own parser, emits one `pn-vault-bundle` JSON | disk → file | `tools/obsidian-sync.js` | runs on Pierce's machine | No — output is gitignored |\n| C5 | **Baked route** — `loadObsidianVault()` seals a bundle into the encrypted tracker payload. *Moved to the private `stock-trackers` repo, 2026-09-15.* | file → build | `build.js` in `stock-trackers` | tracker PIN | No (ciphertext only) |\n| C6 | **Atlas** — 25 curated public notes, three-pane workspace, real link graph, timeline | static file | `assets/js/atlas-data.js`, `/atlas/` | **none** | Intended to — blocked by §1 |\n| C7 | **Bottle porthole** — Easter-egg door reporting whether a vault exists *on this device* | in-browser | `assets/js/bottle.js` | key-presence check only | Yes (the door, not the notes) |\n\nTwo facts worth pulling out of that table:\n\n- **C6 is the template.** The atlas already proves the exact shape this plan needs: a public JSON data file,\n  an ungated three-pane Obsidian-style workspace, and a graph that is the *real* link graph of the content\n  above it rather than a decoration. What C6 lacks is a real vault behind it — it is 25 notes typed by hand\n  from [[CONTENT — piersonnorris.com]].\n- **C4 is the missing half.** It already reads the real vault, correctly, using the site's own parser. Its\n  output has simply never been allowed to leave the machine. Publishing is a new *mode* on C4, not a new\n  script.\n\n**Boundary that does not move:** the site is static, on GitHub Pages, with no server and no OAuth. CI cannot\npull the vault — the vault is on Pierce's disk, not behind a portable credential. Publishing is a snapshot,\nproduced by a command he runs, exactly like the Google Calendar pull (R9) and the tracker build.\n\n---\n\n## 3. Where this goes\n\n**A new public surface at `/vault/`, titled \"Backstage\".** Not a change to `/notes/`.\n\n```mermaid\nflowchart LR\n  V[\"Obsidian vault<br/>(Pierce's disk)\"] -->|\"C4 obsidian-sync.js\"| B[\"private/notes/<br/>obsidian-vault.json<br/>gitignored\"]\n  V -->|\"NEW: --public + review gate\"| P[\"assets/data/<br/>vault-public.json<br/>committed\"]\n  B -->|\"Import button\"| N[\"/notes/<br/>PIN, browser-local, editable\"]\n  B -->|\"C5 build.js\"| T[\"stock-trackers repo<br/>private, sealed payload\"]\n  P --> W[\"/vault/ — Backstage<br/>public, read-only, no gate\"]\n  A[\"/atlas/ — curated public\"] -.->|\"same shell\"| W\n  C3[\"C3 PNGraphify\"] -.-> W\n```\n\nThree reasons it is a separate page and not an ungated `/notes/`:\n\n1. **`/notes/` is a writing surface; `/vault/` is a reading surface.** Editing, deleting and exporting only\n   make sense against your own browser storage. A public reader has no business offering them, and merging\n   the two means every future change to either has to reason about which mode it is in.\n2. **The PIN on `/notes/` is still doing real work** — it guards the *editable* local copy, drafts included,\n   on a laptop that gets opened in public. Nothing about publishing a snapshot argues for removing it.\n3. **Two audiences, two front doors.** `/atlas/` answers \"what has he learned\"; `/vault/` answers \"how does\n   he actually work\". Nav gets both; the bottle porthole (C7) finally gets a real destination to open onto,\n   which is what the original Easter-egg idea wanted and could not have.\n\nNaming: **Backstage**, at `/vault/`. \"Backstage\" says *this is the working material behind the finished\npages* without promising it is polished, and it does not collide with the atlas's \"curated\" framing.\n\n---\n\n## 4. The part that decides whether this is safe\n\nPierce's answer was \"the whole vault\", and this plan builds for that. But **\"publish the whole vault\" and\n\"publish it without reading it first\" are different decisions**, and only the first one has been made.\n\nOne flag, plainly, then out of the way: a public repo is permanent and indexed. [[CONTENT — piersonnorris.com]] §7 keeps a\n\"never on the site\" list — client names and addresses, portfolio values and share counts, Screencastify\ninternal figures and vendor names — and `Portfolio Access.md` and the daily notes are exactly where those\nlive today. Publishing without a pass over the text would put on a career-facing site the one category of\nthing the site has deliberately kept off it for its whole life. Third parties (clients, colleagues) also\nappear in those notes without having chosen to.\n\nSo the plan keeps the scope Pierce chose and adds one gate that is **his**, not the reader's:\n\n- **Default is publish.** Whole vault, as decided. No allowlist to maintain, no note quietly missing.\n- **`publish: false` in frontmatter opts a note out** — one line, in Obsidian, where he is already standing.\n  Same for a `#private` tag; a `nopublish/` folder excludes a whole tree.\n- **The scrubber refuses on a hit, it does not silently redact.** Regexes for dollar amounts, street\n  addresses, phone numbers, and the literal names on the [[CONTENT — piersonnorris.com]] §7 list. A hit fails the run and\n  prints `file:line`, so the fix happens in the vault rather than in a filter nobody remembers.\n- **`--report` before the first publish is mandatory, and it is a human read.** Every note that would go\n  public, listed with title, path and word count. Pierce reads that list top to bottom, once. After that,\n  incremental runs report only what is new or changed.\n- **Nothing lands until he says so.** The publish command writes the file; committing it is a separate,\n  deliberate act.\n\nIf the first `--report` comes back and the vault is mostly daily working notes, the honest recommendation\nmay change to \"publish `docs/` plus the notes you'd hand a colleague\". That is a call to make *with the\nreport in hand*, not now.\n\n---\n\n## 5. The visual\n\nDirection: **Slate command center** — Version 1 in `notes/visual-options/`, the dense, dark, keyboard-first\none. It is the closest of the three to real Obsidian, it inherits the site's existing dark palette instead\nof fighting it, and it is the only one of the three that comfortably holds a file tree, a reader, a graph\nand an inspector at once. This also finally closes **R10**, parked on exactly this choice since 2026-09-06.\n\nLayout — the three-pane workspace shell, reused rather than rewritten. It was written for the atlas and lived in\n`atlas.css` until R20 rebuilt `/atlas/` as a night sky; it now lives in `assets/css/workspace.css`, and `/vault/` is\nits only caller:\n\n```\n┌──────────────────────────────────────────────────────────────────────┐\n│  Backstage   ⌕ search…            [ Read ] [ Graph ] [ Timeline ]    │  ← C6's wbar\n├──────────┬────────────────────────────────────────┬──────────────────┤\n│ VAULT    │  # Weekly review                       │  LINKED          │\n│ ▾ docs   │  ─────────────────────────             │  ← 4 backlinks   │\n│   BLUE…  │  Rendered markdown: headings, lists,   │  → 6 outgoing    │\n│   ROAD…  │  code, tables, callouts, and           │  ⚠ 1 unresolved  │\n│ ▾ daily  │  [[wikilinks]] that actually resolve.  │                  │\n│   2026…  │                                        │  TAGS  #systems  │\n│ ▸ stocks │                                        │  local graph ▨   │\n├──────────┴────────────────────────────────────────┴──────────────────┤\n│  47 notes · 112 links · 2.4 per note · 3 orphans · 1 unresolved       │\n└──────────────────────────────────────────────────────────────────────┘\n```\n\nWhat makes it read as Obsidian rather than as a docs site:\n\n- **A real folder tree**, collapsible, mirroring vault paths. The atlas groups by domain, which is wrong for\n  a vault — folders are how Pierce actually organises it.\n- **The graph is the graph.** `PNGraphify` (C3), the same engine `/notes/` and `/atlas/` use, so every edge\n  is a `[[wikilink]]` in the text above it. Tag nodes off on mount, toggleable back. Local-graph mode in the\n  inspector: the selected note plus its immediate neighbourhood.\n- **The inspector earns the third pane** — backlinks, outgoing links, unresolved links, tags. Unresolved\n  links stay visible rather than hidden; on a public page they are honest, and they were R15's whole point.\n- **⌘K command palette**, cursor keys to move, Enter to open, Esc to close. The atlas already binds ⌘K to\n  search; this extends that convention instead of inventing a second one.\n- **Deep links.** `/vault/#docs/BLUEPRINT.md` opens that note. Shareable, and the bottle porthole and the\n  Experience page can point at specific notes.\n- **Honest heavy and empty states.** A vault of a few hundred notes is not 25: the tree virtualises past\n  ~200 rows, and the graph gets a \"showing the largest connected component\" control rather than a hairball.\n\nReused as-is: `site.css` tokens, `notes.css` (`.pg-*` graph styles), the atlas's `.workspace` grid, `nav.js`,\nthe skip link, focus rings (U1/U2/U3), `theme-color`, favicon.\n\n**One genuinely new piece of code: a Markdown renderer.** There isn't one. `/notes/` and `/atlas/` escape the\nbody text and linkify `[[brackets]]` — fine for short curated notes, wrong for [[BLUEPRINT — piersonnorris.com]]. It needs\nheadings, lists, fenced code, tables, blockquotes, inline emphasis and links, callouts and task checkboxes,\nin ~200 lines of plain JS with **escape-first** ordering (escape, then structure — never the reverse), no\ndependency, per standing rule 3. This is the piece most likely to be underestimated.\n\n---\n\n## 6. Moving plan\n\nFive phases. Each one ends somewhere it is safe to stop.\n\n| Phase | What | Status |\n|---|---|---|\n| **P0 — Unblock** | `.gitignore` allowlist for `/atlas/` (and `/vault/`); commit the atlas files; verify it loads | ✅ done — `git check-ignore` was hiding `atlas/index.html`; the page now loads with 25 notes, 30 links, 0 orphans |\n| **P1 — Look before publishing** | `--report`: every note that *would* publish, with path and size, no text | ✅ built. Run against `docs/`, it caught 7 dollar amounts — all cleared-for-publication figures, so `money` was waived deliberately and the bundle records the waiver |\n| **P2 — The publish pipe** | `--public`: `publish:false` / `#private` / `nopublish/` exclusions, refusing scrubber, `--prefix`, `--waive`, writes `assets/data/vault-public.js` | ✅ built, 26 tests in `tools/tests/vault-publish.test.js` |\n| **P3 — The reader** | `/vault/`, `vault-ui.js`, `vault.css`, `markdown.js`, folder tree, reader, graph, inspector, ⌘K, deep links | ✅ built and exercised in a browser; 24 renderer tests |\n| **P4 — Publish the personal vault** | Point `--vault` at the real vault, read the report, commit the snapshot | ⏸ **waiting on Pierce** — §7 |\n| **P5 — Keep it true** | `--public` in the local rebuild loop (rule 7); the bottle porthole (C7) repointed at `/vault/` | ⏸ after P4 |\n\n**Deliberate change from the drafted plan.** P3 was to be built against a throwaway fixture and P4 was to wire\nthe nav. Instead the first *real* snapshot is the site's own `docs/` folder — 11 notes that were already public\nin this repo — and the nav, sitemap and `llms.txt` are wired now. It gives the same \"judge it before anything\nprivate ships\" property a fixture would have, but the page is honest and useful today rather than a mock, and\nswapping the source is one flag. The irreversible step — committing personal notes — is still untaken.\n\n**Found while building, both worth knowing:**\n\n- **`/atlas/` had never deployed** (§1) — the reason P0 existed, confirmed by `git check-ignore`.\n- **`PNGraphify` counted `[[links]]` written inside code spans.** A note *documenting* the link syntax was\n  inventing edges, and inflating the unresolved count with the vault's own documentation about itself. Fixed in\n  the shared engine with tests, so `/notes/` and `/atlas/` get it too: on the `docs/` snapshot it took the\n  unresolved count from 6 to 0.\n\n**Not built, on purpose:** tree virtualisation past ~200 rows and the graph's \"largest connected component\"\ncontrol (§5). Eleven notes need neither; both become real work the moment P4 lands a few hundred.\n\n**The commit in P4 is the irreversible one.** Everything before it is reversible with `git checkout`. That is\nthe whole reason the phases are cut here: P3 builds the entire visual against fixtures, so the \"good visual\"\ncan be looked at, judged and rejected *before* a single real note is public.\n\nNot in scope: editing from `/vault/` (read-only by design — writes belong in Obsidian), live sync (§2's\nboundary), search indexing beyond client-side substring match, and any change to `/notes/`.\n\n---\n\n## 7. Open questions for Pierce\n\n1. **Does the first `--report` change the answer?** If it lists 300 daily notes of three sentences each, is\n   that still \"publish the whole vault\", or does it become \"publish `docs/` plus what I'd hand a colleague\"?\n   Worth agreeing now that the report is allowed to change the decision.\n2. **Third parties.** Client and colleague names appear in the vault. Publish, initial, or omit?\n3. **Is the stepping stone actually the destination?** `/vault/` currently publishes the site's own `docs/`\n   folder, and it reads well — \"here is how this site is built, in the open\" is a coherent page in its own\n   right, and it carries no privacy question at all. Is P4 still what you want, or is this it?\n4. **Nav pressure.** Seven top-level links is a lot. Does `/vault/` sit under a \"Notes\" grouping with\n   `/notes/`, or stand on its own?\n\nSource for all four: Claude, drafting this plan, 2026-09-08.\n","tags":["systems"],"created":"2026-09-15T21:06:44.118Z","updated":"2026-09-15T21:06:44.118Z"},{"path":"docs/BLUEPRINT.md","folder":"docs","title":"BLUEPRINT — piersonnorris.com","body":"# BLUEPRINT — piersonnorris.com\n\nMaster build spec. Prepared by Claude (Cowork), 2026-08-23, from decisions made directly with Pierson. ChatGPT: read this fully before writing code. [[CONTENT — piersonnorris.com]] holds all copy. (The dashboard feature and its spec, `ASSET_TRACKER_SPEC.md`, moved to the private `stock-trackers` repo on 2026-09-15 — see §8.)\n\nAnything tagged `[OPEN]` is an unmade decision — ask Pierson, don't guess.\n\n---\n\n## 1. Mission\n\nA personal site that answers \"who is Pierson Norris?\" for two audiences at once:\n\n1. **Humans** — recruiters, companies, collaborators. Clean, dark, fast, credible.\n2. **Machines** — AI crawlers and agents (GPTBot, ClaudeBot, PerplexityBot, Google) that increasingly answer \"who is X?\" on people's behalf. The site is deliberately structured so machines parse it accurately: semantic HTML, JSON-LD, llms.txt, explicit crawler permissions.\n\nSecondary mission: a **Tools hub** for things Pierson builds for himself. (It was designed around a gated live portfolio dashboard, which now lives in its own private repo instead — see §8.) The site should read as proof-of-work, not just a resume.\n\n## 2. Locked decisions\n\n| Decision | Value |\n|---|---|\n| Stack | Plain HTML/CSS/JS. No framework, no build step, no npm for the site itself. |\n| Hosting | GitHub Pages, repo `piersonnorris.github.io`, branch `main`, root. `.nojekyll` present. |\n| Domain | `piersonnorris.github.io` now; `piersonnorris.com` attached later (build all URLs relative; put absolute URLs only in the crawl kit files, and update them at domain switch — see §7). |\n| Brand | Dark + sleek. Dark ground; the eye goes to what's bright. Full system in §4. |\n| Blog | Not in v1. No placeholder page. Structure must make adding one later trivial. |\n| Asset tracker | Not on this site. Moved to the private `stock-trackers` repo on 2026-09-15; real numbers never in this repo or the public build. |\n| Analytics | Privacy-friendly, no cookie banner (GoatCounter or Plausible). `[OPEN]` which one — build without it; it's one script tag added later. |\n\n## 3. Audiences drive structure\n\nEvery page carries its content in semantic HTML (`<main>`, `<article>`, `<section>`, `<h1>`–`<h3>` in strict order, `<time datetime>`, `<address>`, `<nav aria-label>`). No div-soup, no text baked into images, no content that only exists after JS runs (JS may *enhance*, never *carry*, the core content — the tracker page is the one exception and it's gated anyway).\n\n## 4. Design system\n\nCommitted single-theme dark design (this is the brand, not a dark mode).\n\n### Color tokens\n\n```css\n:root{\n  --bg:#0e1116;        /* page ground — graphite, cool near-black */\n  --panel:#151b23;     /* raised cards */\n  --panel2:#1a222d;    /* hover / second raise */\n  --line:#232d39;      /* hairline borders */\n  --line2:#2e3947;     /* stronger borders */\n  --ink:#e9edf3;       /* primary text */\n  --mut:#8b95a5;       /* secondary text */\n  --dim:#5c6675;       /* metadata */\n  --acc:#ffb224;       /* THE bright accent — Terminal Amber [OPEN: Pierson may switch to #4CC9F0 ice cyan or #B6F09C volt; only this token changes] */\n  --acc-soft:rgba(255,178,36,.12);\n  --good:#45c26b;      /* success/positive only, never decorative */\n}\n```\n\nRules: one accent, spent sparingly (links, active nav, key numbers, hover states, timeline nodes). Never large filled amber areas. Body background always `var(--bg)` explicitly. Selection color: accent bg, dark text.\n\n### Typography\n\nGoogle Fonts: `Archivo` (700/800) for display, `IBM Plex Sans` (400/450/500/600) for body, `IBM Plex Mono` (400/500) for dates, labels, chips, and data.\n\n```html\n<link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css2?family=Archivo:wght@600;700;800&family=IBM+Plex+Sans:wght@400;450;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap\">\n```\n\nScale: mono meta 11–12px w/ letter-spacing +0.1em uppercase · body 15.5px/1.62 · h3 17–18px · h2 24px · h1 clamp(34px, 6vw, 54px), Archivo 800, letter-spacing −0.02em. Running text max ~65ch. Tabular numerals (`font-variant-numeric: tabular-nums`) anywhere digits align.\n\n### Layout\n\nMax-width 880px centered column, 24px side padding, generous vertical rhythm (sections ~64px apart). Cards: `--panel` bg, 1px `--line` border, 6px radius. Mobile-first; single breakpoint at 640px is usually enough. No horizontal page scroll ever — wide content gets its own `overflow-x:auto` container.\n\n### Voice\n\nConfident, terse, concrete. Numbers over adjectives (\"50+ clients, five-figure first summer\" beats \"successful business\"). No corporate filler, no exclamation marks.\n\n## 5. Site architecture\n\n```\n/                       index.html          Home\n/about/                 index.html          About\n/experience/            index.html          Experience (the 2023–2027 timeline)\n/projects/              index.html          Projects\n/tools/                 index.html          Tools hub\n/contact/               index.html          Contact\n/assets/                css/site.css, js/site.js, img/…, resume/pierson-norris-resume.pdf\nrobots.txt  llms.txt  sitemap.xml  404.html  .nojekyll\n```\n\nFolder-per-page (`/about/` not `about.html`) so URLs stay clean and extension-free on Pages. One shared stylesheet; per-page CSS only if genuinely needed. Nav identical on every page, current page marked with `aria-current=\"page\"` + accent underline. Footer on every page: name, email, LinkedIn `[OPEN: more socials TBD]`, \"Built by hand — plain HTML, no framework\" (that line is on-brand).\n\n`404.html` at root (GitHub Pages picks it up automatically): dark, terse, links home.\n\n## 6. Page specs (copy lives in CONTENT.md)\n\n**Home** — The elevator pitch page. H1 name, one-line identity, the 2–3 sentence pitch (this is what AI snippets will lift — it must stand alone), then three compact proof cards (True North · Screencastify · Tools) each linking deeper, then a quiet contact line. No giant hero animation; confidence through restraint.\n\n**About** — The fuller story in first person, ~250–350 words, from CONTENT.md §2. Optional photo `[OPEN]`. Ends with what he's looking toward (the 2027 line, once Pierson supplies it).\n\n**Experience** — The timeline, 2023 → 2027, rendered as a vertical rail with year markers (mirror the Build HQ page's structure: year label column, entries as cards, amber nodes). Include the \"before Elon\" prologue as a muted intro block `[OPEN: Pierson may cut it]`. Each entry: role/title, mono date range, 1–3 sentence body with bolded numbers, optional takeaway line with accent left-border. Resume PDF download button at top right of the page `[OPEN: PDF to be produced — build the button pointing at /assets/resume/pierson-norris-resume.pdf and it will drop in]`. Skills grid at the bottom (structured `<ul>`, one `<li>` per skill group — prime crawler content).\n\n**Projects** — Card grid (1-col mobile / 2-col desktop): True North digital infrastructure, the Asset Tracking system, Screencastify internship tooling (the story is the project), this website itself. Each card: title, mono period, 2-sentence description, outcome numbers where they exist, link (live site, or the Tools page, or Experience anchor).\n\n**Tools** — The hub. Intro sentence (\"Things I build for myself; some are public, some are locked\"), then a card per tool. `[OPEN]` which cards: v1 was designed around a **Portfolio Tracker** card, and that tool left the site on 2026-09-15 (§8). Layout must make each tool card a copy-paste job.\n\n**Contact** — Email (mailto), LinkedIn, GitHub profile `[OPEN: other socials TBD]`. No contact form in v1 (forms need a backend or third party; a mailto link is honest and zero-maintenance). Mono-styled links, one per line, `<address>` element.\n\n## 7. AI-crawlability kit\n\nStarters for `robots.txt` and `llms.txt` are already in the repo root — extend, don't replace. At custom-domain switch, update absolute URLs in: JSON-LD (`url`), `sitemap.xml`, `llms.txt`, OG tags, and add the `CNAME` file.\n\n**JSON-LD** — On Home and About, in `<head>`:\n\n```html\n<script type=\"application/ld+json\">\n{\n  \"@context\": \"https://schema.org\",\n  \"@type\": \"Person\",\n  \"name\": \"Pierson Norris\",\n  \"alternateName\": \"Pierce Norris\",\n  \"url\": \"https://piersonnorris.github.io/\",\n  \"email\": \"mailto:norrispierce506@gmail.com\",\n  \"jobTitle\": \"Founder & Owner, True North Services LLC\",\n  \"affiliation\": [\n    {\"@type\": \"Organization\", \"name\": \"True North Services LLC\"},\n    {\"@type\": \"CollegeOrUniversity\", \"name\": \"Elon University\"}\n  ],\n  \"description\": \"<the Home elevator pitch, verbatim>\",\n  \"knowsLanguage\": [\"English\", \"Chinese (Mandarin, near-fluent)\"],\n  \"knowsAbout\": [\"Small business operations\", \"Sales and local marketing\", \"Finance and markets\", \"AI workflow automation\", \"Web tools\"],\n  \"sameAs\": [\"<LinkedIn URL>\", \"<GitHub URL>\"]\n}\n</script>\n```\n\nValidate with Google's Rich Results Test before calling M3 done.\n\n**Per-page `<head>`** — unique `<title>` (\"Page — Pierson Norris\") and meta description; OG + Twitter card tags (og:title, og:description, og:type=profile on Home, og:image once a share image exists `[OPEN]`); `<link rel=\"canonical\">`; `<html lang=\"en\">`.\n\n**sitemap.xml** — all six public pages, referenced from robots.txt. A page that should not be indexed (today, `/notes/`) stays out of the sitemap and carries `<meta name=\"robots\" content=\"noindex\">`.\n\n**llms.txt** — keep in sync with site content at every milestone; it is the machine-readable executive summary.\n\n## 8. Asset tracker — moved out (2026-09-15)\n\nThe portfolio dashboard is no longer part of this site. It lived at `/tools/tracker/` (PIN-sealed at first, open from 2026-09-08) and on 2026-09-15 moved to its own **private** repo, `piersonnorris/stock-trackers`, together with its spec, build script, tests and scheduled refresh workflow. A private repo is what lets it run with no password. Nothing on this site links to it, and holdings appear nowhere here. The old files remain in this repo's git history.\n\n## 9. Milestones\n\n**M0 — Live scaffold** *(already done if the coming-soon page is up)* Repo pushed, Pages enabled, placeholder serving.\n\n**M1 — Shell + Home.** `site.css` with the full design system; shared nav/footer; finished Home. ✔ Accept: Home matches §4/§6, valid HTML (W3C validator), keyboard-navigable, no horizontal scroll at 360px width, Lighthouse ≥95 accessibility.\n\n**M2 — Content pages.** About, Experience (timeline + skills), Projects, Contact, 404. ✔ Accept: all copy verbatim from CONTENT.md (`[OPEN]` items visibly absent, not faked); timeline readable on mobile; every internal link works.\n\n**M3 — Crawl kit.** JSON-LD both pages, full head/OG treatment, sitemap.xml, llms.txt + robots.txt finalized. ✔ Accept: Rich Results Test passes; sitemap fetches; every page has unique title + description.\n\n**M4 — Tracker.** Out of this site's scope since 2026-09-15; the tracker and its acceptance criteria live in the private `stock-trackers` repo (§8).\n\n**M5 — Polish + launch.** Favicon (dark-ground \"PN\" mark), 404 styled, GoatCounter/Plausible when chosen, run the full QA list below, then Pierson shares the URL.\n\n## 10. Definition of done (run at M5, spot-check every milestone)\n\n- Every page: valid HTML, unique title/description, h1 exactly once, no console errors.\n- Mobile 360px → desktop 1440px: no layout breaks, no sideways scroll.\n- All links resolve (internal relative, external `rel=\"noopener\"`, mailto correct).\n- Lighthouse: Performance ≥90, Accessibility ≥95, SEO ≥95 on Home + Experience.\n- JSON-LD validates; robots.txt + sitemap.xml + llms.txt mutually consistent.\n- Grep the repo for `secrets`, `BEGIN PRIVATE KEY`, the password — zero hits.\n- The site reads correctly with JS disabled.\n\n## 11. Standing rules for ChatGPT\n\n1. Copy comes from CONTENT.md verbatim; `[OPEN]` means ask Pierson.\n2. Complete files with exact paths; explain each step in plain English first.\n3. One milestone at a time; commit/push walkthrough after each.\n4. No secrets in chat, repo, or client code — ever.\n5. No new dependencies, frameworks, or build steps without Pierson's explicit yes.\n","tags":["4CC9F0","B6F09C"],"created":"2026-09-15T21:06:44.119Z","updated":"2026-09-15T21:06:44.119Z"},{"path":"docs/BOTTLE_SHOWCASE.md","folder":"docs","title":"SHIP IN A BOTTLE — the Obsidian showcase","body":"# SHIP IN A BOTTLE — the Obsidian showcase\n\n**Status 2026-09-08: phases A, B and C done. `PNObservatory` is real code. D, E and F remain; E is blocked on\nR19's lane labels.** ROADMAP **R18**.\nComponent: `assets/js/observatory.js` + `assets/css/observatory.css`.\nLocal routes: `vault/concepts/index.html` (the five directions), `vault/concepts/observatory/` (the phase-A\ncomp), `vault/concepts/observatory/live/` (the live component).\nCompanion: [[ISLAND TO ISLAND — the work, laid out as a chart]] (R19) — direction 02's chart, pointed at the work history instead.\n\n> **Pierce, 2026-09-08:** *\"i like 1 … before this starts add in to refine the visual and build it out because\n> this is a whole home page ui update.\"*\n>\n> Two things follow from that sentence and they change the shape of this doc. **The Bottle Observatory is not a\n> sub-page** — it is the home page's new front. And **the first phase is refinement, not construction**: the\n> preview in `vault/concepts/` is a one-fifth-scale sketch, and sketching is not designing. Building straight\n> from it would bake in proportions nobody has looked at full-size.\n\n---\n\n## 0. What this is for, stated plainly\n\nPierce, 2026-09-08: *\"the end goal is to have an amazing in-a-bottle Obsidian visual that looks impressive …\nit also should open in one click and have no gate … the goal of this is to have a crazy looking UI to showcase\nthe use of Obsidian, no other purposes.\"*\n\nSo the brief is narrower than everything else on this site, and that is a feature:\n\n- **The only job is to look extraordinary** while showing real notes and real `[[links]]`. Not to inform, not\n  to convert, not to be a second `/notes/`.\n- **One click from the bottle. No gate.** Pierce's reasoning: only he links to it, and it can be gated at any\n  time later. That is a decision about *his own* published notes, and it is reversible in one commit — unlike\n  what gets committed, which is not.\n- **Nothing to do with stocks.** See §1.\n\nThe one thing that must stay true underneath: **the structure has to be real.** A drawn constellation that\nisn't the actual link graph is a screensaver, and it would undercut the exact thing being showcased.\n\n---\n\n## 1. Separation from the stock page — done 2026-09-08\n\nObsidian and the portfolio touched in two places. Only one was a real coupling:\n\n| Where | What it was | Now |\n|---|---|---|\n| `assets/js/bottle.js` porthole | Two buttons: *Open the vault* → `/notes/`, and ***Stock desk* → `/tools/tracker/#stocknotes`** | ✅ Stock link removed. Buttons are now `/vault/` (primary) and `/notes/`. The bottle is the Obsidian door and nothing else's. |\n\n**Update 2026-09-10 (ROADMAP R23).** The porthole is no longer a flat dotted sketch that sat there breathing. A jar stands on the ground, nine fireflies rise to the nine points of the constellation, and each point lights as its firefly lands — the same story `/atlas/` tells when you open a jar. The bottle on the page also gained seven fireflies drifting in its glass, so it moves before anyone clicks it. **What the drawing means is unchanged**: still generated, still not a graph of anyone's notes, and the copy under it still says so. §0's brief — *\"the only job is to look extraordinary\"* — is what this was measured against.\n| Tracker's **Obsidian tab** | The *stock notes* vault — per-ticker notes — lives inside the tracker | Left alone, deliberately. Those notes are about holdings; they belong next to the holdings. It is a different vault, not this one. |\n\nThe rule going forward: **the showcase never links to the tracker, and the tracker never links to the\nshowcase.** They share the `PNVault` engine and `PNGraphify`; they share no surface and no navigation.\n\n---\n\n## 2. The five directions\n\nAll five share the same spine — the existing `PNBottle` entrance, the published `vault-public.js` snapshot,\nand `PNGraphify` for real link structure. They differ in what the vault *is* once you are inside.\n\n### 01 · The Bottle Observatory — *most on-theme*\n\nThe bottle is the page. Full-viewport ocean, one enormous bottle, and the vault living **inside the glass** as\na drifting constellation: every dot a note, every line a real `[[wikilink]]`, the ship tacking between them.\n\nPop the cork and the camera flies **through** the glass — the bottle doesn't disappear, it becomes the vignette\nat the edge of the screen, so you are permanently inside it. Notes unfurl as message-in-a-bottle scrolls.\nGraph view is the default state, not a tab; reading a note dims the constellation and keeps its neighbours lit.\nUnresolved links drift past as corked empty bottles.\n\n- **Sells it:** glass that behaves like glass (specular sweep, refraction offset near the curve, caustics on the\n  sea floor); one continuous camera with no page transition ever; zoom *is* the navigation.\n- **Hard:** the through-the-glass zoom must hold 60fps or the trick dies. Text legibility inside a curved,\n  tinted, moving container.\n- **Cost:** high. Reuses `PNBottle` and `PNGraphify` almost entirely.\n\n### 02 · The Cartographer's Table — *most legible*\n\nTop-down onto a desk. The vault is an ocean chart: every note an **island**, every folder an **archipelago**,\nevery `[[wikilink]]` an inked shipping lane. The bottle lies on the chart as the launcher; it rolls, uncorks,\nand the chart unrolls out of it across the screen. Reading a note unfurls a scroll *over* the map.\n\n- **Sells it:** texture — real paper grain, plate-mark edges, ink bleed, brass instruments. The metaphor is\n  load-bearing: orphan islands have no lane to them, and unresolved links are the edge of the known world,\n  *hic sunt dracones*, drawn in. Zoom out far enough and the chart simply *is* the graph.\n- **Hard:** island shapes must be generated *from* the data or it's a picture, not a map. Serif-on-parchment has\n  to stay WCAG-legible — U1 was fought over exactly this.\n- **Cost:** high. The most new art, the least new engine work.\n\n### 03 · The Descent — *most cinematic*\n\nYou throw the bottle in and follow it down. A single continuous descent: light fades, pressure marks tick past,\nnotes hang in the dark as bioluminescent life joined by glowing filaments. Scrolling is diving; there is no\nsecond page. **Depth is time** — recently-edited notes float near the surface, the oldest sit on the sea floor,\nso the Timeline stops being a list and becomes the act of descending.\n\n- **Sells it:** godrays, marine snow and parallax — three cheap effects that together read as expensive. Scroll-as-dive\n  is genuinely novel *and* honest. Kinship with the tracker's underwater skin without sharing a page with it.\n- **Hard:** scroll-hijacking is the fastest way to make a page feel broken; needs a real escape hatch. Deep means\n  dark, which is a contrast fight for body text.\n- **Cost:** medium-high. Highest risk of \"impressive once, annoying twice\".\n\n### 04 · The Orrery in Glass — *most unusual*\n\nThe bottle holds a working orrery. Each of the six domains is a **sun**; every note is a body orbiting the\ndomain it belongs to. `[[Wikilinks]]` are resonance lines drawn between bodies that shouldn't otherwise touch,\nso a cross-domain link is visibly, structurally strange. The cork lifts and the armillary rings expand out of\nthe bottle's neck to fill the screen, still turning. Clicking a body slows the machine and swings it into an\nastrolabe panel. **Orbital period maps to recency** — a stale note visibly crawls.\n\n- **Sells it:** nobody has seen a vault look like this. Brass and starfield is a palette the site has never used,\n  so it is instantly its own place. Motion carries meaning rather than decorating.\n- **Hard:** real 3D-ish maths in CSS/SVG with no library — the hardest build of the five. Constant motion behind\n  text is a reading and accessibility problem and needs a decisive pause.\n- **Cost:** highest. Furthest from anything already built.\n\n### 05 · Vault OS — *cheapest, ships fastest*\n\nThe anti-skeuomorphic one. The bottle shatters into a phosphor terminal that **boots the vault in front of\nyou** — mounting the snapshot, resolving links, reporting counts, all real numbers from `vault-public.js`. The\nboot log is the loading screen and it takes about a second because it is genuinely doing that work. Then the\nASCII graph **morphs** into the real SVG `PNGraphify` graph, box-drawing characters sliding into nodes. That\nsingle transition is the whole trick.\n\n- **Sells it:** it's fast — text, glow, scanlines, no heavy art, and it looks intentional on any screen.\n  Keyboard-first matches how a vault is actually used, and `⌘K` already exists. Reuses `/vault/` wholesale.\n- **Hard:** the ASCII→SVG morph is the only genuinely novel piece; cut it and this becomes ordinary. Terminal\n  aesthetics are common, so the ship has to keep it personal.\n- **Cost:** low-medium. Could ship in one session.\n\n---\n\n## 2a-0. Phase A — done 2026-09-08. What the comp decided.\n\nComp: `vault/concepts/observatory/index.html` (local only). Full viewport, real bottle geometry, real type\nsizes, and **13 real notes / 16 real links read straight out of `assets/data/vault-public.js`** — not\nplaceholder dots. Three states and two depths, switchable; desktop and phone.\n\nEverything below was decided by looking at it at full size. Half of it contradicts what the plan assumed,\nwhich is exactly what phase A was for.\n\n### Two findings that change the build\n\n**1. \"Inside\" is not a zoom of \"sealed\". It is a second composition.**\nThe plan said \"the camera flies through the glass\" and I built that first: push the viewBox in until you are\npast the surface. At full size it fails badly — the nodes inflate to beach balls and four notes of thirteen\nare left on screen. **The vault disappears at the exact moment you arrive in it.**\n\nThe fix is that inside has its own layout: the same notes re-laid across the whole viewport at readable\nscale, labelled, with the glass demoted to a rim vignette and two curvature arcs. That reads correctly and it\nis the better picture.\n\nBut it means **phase C is not a camera push — it is a morph between two layouts**, every node travelling its\nown path from its place in the bottle to its place in the room. That is materially more work than the plan\nassumed, and finding it now cost nothing.\n\n**2. The desktop composition does not survive portrait.**\nOn a 390 × 844 frame the copy, the bottle and the Currently card all want the middle third and land on top of\neach other. Phone is therefore a **third composition, not a reflow**: the scene stops filling the frame and\nbecomes a horizontal band, copy above it, card below, status pinned. It works, and it is still the weakest of\nthe three states. The stronger answer — standing the bottle upright on its base for portrait — is a different\ndrawing, and it belongs in phase B.\n\n### Answered, and now settled\n\n- **The name sits outside the glass.** The first comp let the copy column run to 42vw and the bottle went\n  straight through a 72px `<h1>`. The copy column is now hard-capped at `min(40ch, 36vw)` and the glass starts\n  after it. Non-negotiable in the build.\n- **The bottle lies on its side, base left, cork right.** The first geometry drew a vertical neck on a\n  horizontal body — a hybrid that read as a submarine. Cork on the right also points the \"pull me\" affordance\n  *away* from the headline instead of into it.\n- **The Currently card survives, bottom-right, over the glass.** Phase E's open question, answered: it is the\n  one block a recruiter needs in five seconds, and at `backdrop-filter: blur(8px)` it reads as floating in\n  front of the bottle rather than fighting it.\n- **Depth, not light/dark.** `site.css` has *zero* `prefers-color-scheme` rules — the site is dark-only, so\n  \"both themes\" was the wrong axis in the plan. The two variants that mean something are **Deep** and\n  **Dusk**: same composition, different water. Corrected here.\n- **Nodes never enter the shoulder or the neck**, where the clip slices them and the curvature is tightest.\n  Layout is a seeded, deterministic scatter inside the body only — a comp that reshuffles on reload cannot be\n  judged, and neither can a home page that does.\n- **Two marks make it read as glass**, not ten: one specular sweep across the shoulder and one rim light on\n  the base curve. Everything else was noise.\n\n### Still open after phase A\n\n- **The console tiles** (Asset tracker, Obsidian notes, What's moved lately, True North, Screencastify) have\n  nowhere to go in this composition yet. That is now R19's job — see §2a-1.\n- **The `Obsidian — stock desk` tile on today's home page** violates the separation rule the moment the home\n  page *becomes* the showcase. It has to move to the tracker or go. Flagged, not yet decided.\n- **Node count.** Thirteen is comfortable; a few hundred silts the glass up completely. Phase D needs a cap\n  plus a largest-connected-component view before the personal vault (R17 P4) is ever pointed at this.\n\n**Verdict: 01 holds up at full size.** The fallback to Vault OS is not needed. Proceed to phase B — with C\nre-scoped from \"camera\" to \"layout morph\".\n\n## 2a-0b. Phase B — done 2026-09-08. And C came with it.\n\n**`assets/js/observatory.js` + `assets/css/observatory.css`** — production files, not comp scaffolding.\n`PNObservatory.mount(el, {data, orientation, depth, onOpen})`. Scratch route:\n`vault/concepts/observatory/live/`. Phase F is now a swap, not a rewrite.\n\n**Phase C shipped with it.** The morph was supposed to be its own risky phase; it turned out to be ~40 lines\nonce the two layouts existed, so it is built: opening runs one `requestAnimationFrame` loop that walks every\nnode from its place in the glass to its place in the room, redrawing edges each frame. **The frame-rate worry\nwas unfounded at this scale** — 13 transforms and 17 line segments. What keeps it that way is phase D's node\ncap, which is now load-bearing rather than nice-to-have.\n\n### What's in it\n\n- **Two compositions, not one with breakpoints.** `LANDSCAPE` lies the bottle on its side (base left, cork\n  right); `PORTRAIT` stands it on its base. Each carries its own viewBox, silhouette, node field, cork travel,\n  glint path and furniture position. An orientation flip **rebuilds** rather than reflows — phase A's finding,\n  encoded in the data structure rather than remembered in a comment.\n- **The glass: three stops and two marks.** Bright where the curve turns away at the top, nearly clear through\n  the belly, bright again at the base where the floor reflects up; one travelling glint on the shoulder and\n  one rim light. Everything beyond that read as noise at full size.\n- **Ambient life that costs nothing:** three swaying light shafts, two drifting caustic lines on the floor,\n  rising bubbles, a bobbing ship with a faint reflection, and pulses that run down the links — every one an\n  opacity, a transform or a `stroke-dashoffset`, so none of them touch layout.\n- **The structure is real.** Edges are `[[wikilinks]]` actually written in the notes, code spans stripped, so\n  the counts here and on `/vault/` agree by construction.\n- **Reduced motion is a path, not a switch:** the morph becomes an instant placement and every ambient loop\n  stops. The page still works; it just stops moving.\n\n### Three bugs worth writing down\n\n- **Every node came out the same colour.** `seedOf` used `h * 16777619`, which overflows 2^53 — JS silently\n  drops the low bits, so every seed was a multiple of 4 and `seed % 4` picked colour 0 thirteen times out of\n  thirteen. It looked deliberate, which is why it survived a screenshot. `Math.imul` fixes it.\n- **CSS animation beats the SVG `transform` attribute.** Drift and bob were applied to the same elements the\n  script positions, so a node snapped to the viewBox origin the instant its animation delay elapsed — the ship\n  was already sitting at `left: -75px`. Position and animation now live on separate nested groups. This is a\n  rule for anything else that animates SVG here.\n- **The ship stayed for the inside state**, becoming a twenty-foot schooner moored in the middle of the\n  constellation. The ship and waterline are what you see *through* the glass, so they fade with it.\n\n### Still open\n\n- One portrait label (`PORTFOLIO_TASKBOARD_PLAN`) still crowds the right edge at the widest filenames.\n  Truncation belongs with phase D's node cap.\n- Nothing has been looked at on real hardware yet — only this machine's browser.\n\n## 2a-0c. Phase D — spec. Written 2026-09-08, not built.\n\nTwo things, one of them new. Pierce, 2026-09-08: *\"add in D in addition add a button to find groups for\nobsidian and labeling them.\"*\n\n### D1 — the node cap and the largest connected component\n\nPhase C is cheap at 13 notes and 17 edges. It will not be at 300. The morph walks every node every frame, so\ncost is linear in node count and the glass silts up visually long before it stutters.\n\n- **Cap the drawn set.** Default ~90 nodes, ranked by degree then recency, with an honest readout: *\"showing\n  90 of 312 — ranked by connections.\"* Never silently truncate; a vault that looks smaller than it is\n  misrepresents the thing being showcased.\n- **Largest-connected-component view.** A toggle that drops everything not reachable from the biggest cluster.\n  Orphans stay *counted* in the readout even when hidden — R15's whole argument was that hiding them flatters\n  the vault.\n- This is what makes R17's P4 (pointing `/vault/` at the personal vault) safe to do later.\n\n### D2 — \"Find groups\": clustering, and labels that are earned\n\nA button in the inside state. Press it and the constellation resolves into its actual communities: each\ncluster tinted, given a soft hull, and named.\n\n**Detection.** Label propagation over the `[[wikilink]]` graph — every node starts in its own group, then\nrepeatedly adopts the most common group among its neighbours until stable. Chosen over modularity methods\nbecause it is ~30 lines, has no tuning knob to fake, and runs in milliseconds at this size. **Iteration order\nmust be seeded**, not `Object.keys` order, or the same vault clusters differently on every load and the\nfeature becomes a random-colour generator.\n\n**Labelling — the part that has to stay honest.** A label is *derived*, never invented, in this order:\n\n1. **Shared folder.** If ≥60% of a cluster lives in one folder, that folder is the name (`docs/`, `daily/`).\n2. **Shared tag.** Else if ≥60% carry one tag, that's the name (`#systems`).\n3. **The hub.** Else the title of the cluster's most-linked note, phrased as *\"around BLUEPRINT\"*.\n4. **Nothing.** Else — **\"unnamed group · 6 notes\"**, and it stays unnamed.\n\nRule 4 is the important one. The tempting version of this feature reads the notes and writes a clever theme\nfor each blob. That would be the page inventing a claim about Pierce's thinking that no data supports, on a\npage whose entire premise is that the structure is real. **If the vault has not earned a name for a cluster,\nthe cluster does not get one.**\n\n**Drawing it.** Convex hull per cluster, expanded and rounded, filled at very low alpha in the cluster's hue,\nlabel set on the hull's top edge. Singletons get no hull — a circle around one dot is noise. Clusters keep\nthe existing palette; tinting is a hue *shift* per group so a node's identity colour survives.\n\n**State.** Grouping is a toggle, off by default: the ungrouped constellation is the honest default view, and\ngroups are an interpretation laid over it. Off returns to exactly the previous positions — no re-layout,\nbecause a button that scrambles the picture reads as a bug.\n\n**Testing.** `build`-style split, same as `PNGraphify`: a pure `groups(notes)` returning\n`{clusters, labels, stats}` with unit tests (a known two-cluster fixture, a fully-connected fixture that must\nreturn one group, an all-orphans fixture that must return none, and a determinism check that the same input\ngives the same output twice). The drawing half is left to the browser.\n\n## 2a-1. R19 is the data layer for the rest of the page\n\nPierce, 2026-09-08: *\"add to the note that R19 will be started as the data for the home page when its updates.\"*\n\nRecorded, and it resolves the biggest thing phase A left open. The Observatory owns the first screen; it does\nnot own the whole page, and the console tiles below it currently have no home in the new composition.\n\n**R19 becomes that content.** `voyage-data.js` — the islands, dates, inscriptions and lane labels traced to\n[[CONTENT — piersonnorris.com]] §3 — is not just the Experience chart's data file; it is **the data layer the home page reads for\neverything below the fold**. The work section stops being six hand-written tiles and becomes the voyage,\nrendered compactly.\n\nWhat that buys:\n\n- **One source, two surfaces.** Experience gets the full chart, the home page gets the strip, and both read\n  the same file — the same rule `atlas-data.js` already follows, so a chapter is added once.\n- **Phase F stops being cosmetic.** It was \"fold in the strip\"; it is now \"the home page's content layer lands\".\n- **Sequencing:** R19's data file is built **when the home page is updated**, i.e. inside this pass, not after\n  it. The chart *rendering* can still follow on Experience, but the data has to exist by phase E, because\n  phase E is what replaces the tiles.\n\nConsequence for the phase table: **E now depends on R19's `voyage-data.js`**, and R19's lane labels — which\nneed Pierce's own words — become a blocker for phase E rather than a nice-to-have afterwards.\n\n## 2a. The build, now that 01 is picked\n\nScope is **the home page**, not a new URL: the Observatory becomes what you land on at `/`. That raises the\nstakes — this is the first thing a recruiter sees, so \"looks impossible\" and \"loads fast, reads clearly, works\non a phone\" have to both be true. The phases are cut so the expensive, irreversible-feeling decisions happen\nwhile everything is still cheap to throw away.\n\n| Phase | What | Status / why it's cut here |\n|---|---|---|\n| **A — Refine the visual** | Direction 01 taken from a one-fifth-scale sketch to a **full-size, full-viewport comp**: real geometry, real node count from `vault-public.js`, real type, three states, two depths, desktop and phone. | ✅ **done 2026-09-08** — findings in §2a-0. Verdict: 01 holds up; proceed. |\n| **B — The glass** | The bottle as a real component, plus the upright-bottle portrait composition. | ✅ **done 2026-09-08** — `assets/js/observatory.js` + `assets/css/observatory.css`. §2a-0b. |\n| **C — The layout morph** | Every node walks its own path between the two layouts; glass resolves into a rim vignette. | ✅ **built with B** — ~40 lines once both layouts existed. Frame-rate worry unfounded at 13 nodes. **Unverified on real hardware.** |\n| **D — The constellation** | **D1** node cap + largest-connected-component view. **D2** a *Find groups* button: label-propagation clustering with labels derived from folder, tag or hub — never invented. Spec in §2a-0c. | Next. The cap is what stops R17 P4 from silting the glass up; the grouping is what makes it read as a vault rather than a starfield. |\n| **E — The home page around it** | The nav, the footer, the contact route, and **the console tiles replaced by R19's voyage data** (§2a-1). | Most likely to be underestimated. A gorgeous page that buries the contact details is a worse home page. **Blocked on R19's lane labels.** |\n| **F — Ship it** | Swap `/` over, keep the old hero on a fallback route until it has been looked at on real hardware. | Was \"fold in R19\"; R19 moved into E, so F is the cutover. |\n\n**Ends of the line, stated now:** if phase C can't hold frame rate on a mid-range laptop, the fallback is a\ncross-fade between the two layouts rather than a per-node morph, and the page is still good. Phase A already\ncleared its own bar — 01 held up full-size, so the re-pick branch is closed and **05 Vault OS is no longer on\nthe table**.\n\n**What is not in scope:** changing what `/vault/` does (it stays the useful reading surface, and the Observatory\nlinks to it), touching the tracker, or publishing the personal vault (still R17's parked P4).\n\n## 3. The pick — 01, and what the others are still for\n\n**01, The Bottle Observatory — chosen 2026-09-08.** It was the recommendation and it is the pick: the only one\nwhere the bottle stays the subject the whole way through, and the graph it needs is already built.\n\nThe other four are not discarded, they are shelved with a purpose:\n\n- **02** is now **R19**, doing a different job entirely — see [[ISLAND TO ISLAND — the work, laid out as a chart]].\n- **05, Vault OS** is the **fallback**. If phase A says 01 doesn't hold up full-size, this is what ships instead;\n  it is the only direction whose failure mode is \"fine\" rather than \"broken\".\n- **03** and **04** stay on the shelf. 04 in particular is worth revisiting if the site ever wants a second\n  showpiece, because nothing about it overlaps with 01.\n\nThree things hold regardless: real link structure from `PNGraphify`, the `PNBottle` entrance, and\n`prefers-reduced-motion` honoured throughout — this is built out of motion, and it has to survive that being\nswitched off.\n\n---\n\n## 4. Open\n\n- ~~Which direction.~~ **Answered 2026-09-08: 01.**\n- ~~Where it lives.~~ **Answered: the home page itself**, not a new URL. `/vault/` stays the reading surface and\n  the Observatory links to it.\n- **What it shows.** Right now `vault-public.js` is the site's own `docs/` folder. A showcase of \"how I use\n  Obsidian\" is more convincing over the *personal* vault, which is R17's parked P4 — same four questions. Worth\n  deciding before phase D, since the node count changes the composition.\n- **How much of today's home page survives** (phase E). Needs Pierce's call on what must stay plainly readable\n  outside the metaphor — at minimum the contact route, probably the Currently card.\n- **Does the R19 strip land in this pass** (phase F) or immediately after?\n","tags":[],"created":"2026-09-15T21:06:44.121Z","updated":"2026-09-15T21:06:44.121Z"},{"path":"docs/CONTENT.md","folder":"docs","title":"CONTENT — piersonnorris.com","body":"# CONTENT — piersonnorris.com\n\nSingle source of truth for every claim on the site. Compiled by Claude from Pierson's Drive (resumes, LLC documents, the Screencastify final deck, the asset-tracking spec) and direct decisions with Pierson, 2026-08-23. Timeline section rewritten 2026-09-05 from the True North operations framework, priority roadmap, all-hands deck, and head-manager training deck, plus the Screencastify final presentation.\n\nRules: use this copy verbatim (light grammatical smoothing allowed, meaning-changes not). `[OPEN]` = missing or unconfirmed — ask Pierson; never invent. Where numbers appear, they came from Pierson's own documents.\n\n---\n\n## 1. Identity\n\n- **Public name:** Pierson Norris (goes by Pierce in person — \"Pierson Norris\" is the searchable, professional identity and appears in H1s, titles, JSON-LD).\n- **One-line identity (Home, under H1):** Founder, operator, and builder — business on the ground, AI in the workflow.\n- **Elevator pitch (Home + JSON-LD description, verbatim):**\n  > Pierson Norris is a Finance & Accounting student at Elon University (class of 2027) who builds businesses and the systems that run them. He founded True North Services LLC, a Chicago North Shore exterior-services company he started at 18 and grew into a documented, trained operation; spent a summer as an AI intern at Screencastify mapping seven workflows across six departments and shipping three live automation fixes; and is the incoming president of Elon's AI & ET Club. This site is his profile, his portfolio, and his toolbox.\n\n  `[OPEN]` Pierson: confirm the club title line and the \"started at 18\" framing.\n- **Contact email:** norrispierce506@gmail.com `[OPEN: may swap for a cleaner public alias]`\n- **LinkedIn:** https://www.linkedin.com/in/pierson-norris-634954246 `[OPEN: claiming a cleaner custom URL is on his checklist — use whichever exists at build time]`\n- **GitHub:** `[OPEN: username pending confirmation]`\n- **Other socials:** `[OPEN: TBD by Pierson — an Instagram for the business (oaisis_exterior_cleaning) exists from the Oasis era]`\n- **Photo:** `[OPEN]`\n\n## 2. About (draft, first person, ~300 words)\n\n> I grew up on Chicago's North Shore doing the jobs that teach you how work actually happens — a Taco Bell register, moving furniture for a design firm, a one-month internship shadowing a CEO. The summer before college I started a power-washing and window-washing business with a crew of high-school and college kids. First summer: 50+ clients and five figures in revenue, sold with flyers, Nextdoor posts, and knocking on doors.\n>\n> At Elon I study Finance & Accounting with minors in Entrepreneurship and Mandarin Chinese. The business kept growing alongside the degree, and in 2026 it became True North Services LLC — a real company now, with an operations framework, a crew-training program I wrote myself, and a team that runs weekly ops calls without me in the room.\n>\n> The other thread is AI. In summer 2026 I was the AI intern at Screencastify, where I learned my favorite working method: don't start with the tool, start with someone's actual Tuesday. One written prompt got seven workflows mapped across six departments in 48 hours, and every single one had the same failure — a person acting as the API between systems that don't talk. We shipped three fixes; one cut a 4–8 hour weekly finance task to under 15 minutes.\n>\n> Now I run True North, trade and track my own portfolio (since 2020), and serve as incoming president of Elon's AI & ET Club. I build my own tools — one of them tracks my holdings across five platforms, live from a spreadsheet.\n>\n> `[OPEN — closing line: the 2027 headline. What Pierson is aiming at after graduation.]`\n\n## 3. Experience timeline (2023 → now)\n\nRendered newest-first at `/experience/`. The timeline **starts at 2026** (the top) and runs back to 2023. Per Pierson, 2026-09-05: **no 2027 entries** — that year hasn't happened, so it is not on the page.\n\n**2026** *(nine entries, newest first)*\n\n- **Incoming President, Elon AI & Emerging Tech Club** — 2026, ongoing. `[OPEN: exact title, term start/end, one line of agenda]`\n- **AI Intern, Screencastify** — Jun 9 – Aug 7, 2026 (nine weeks). One three-round workflow-mapping prompt dropped in a shared folder; department leaders mapped their own work (trigger, middle, hand-offs, friction, root cause) and returned **7 workflows across 6 departments in 48 hours**, with **one meeting added**. All seven shared one failure: *a person acting as the API between systems that don't talk.* Three fixes shipped — Finance weekly AR collections (**4–8 hrs/week → under 15 minutes**), Product-marketing Monday update email (**~30 min/week plus the rework loop, gone**), Engineering sprint-review recording (a hard-cutoff policy and one saved filter — **no tool build at all**). Combined build time: one afternoon. Separately, a **76-post** distribution audit reframed marketing as **under-concentrated, not under-worked**, and proposed the *Content Waterfall* — one ungated anchor asset repurposed into eight placements, with a single named distribution owner. *Takeaway: the method is the résumé line — find the problem in someone's actual Tuesday, get sign-off, build it with them, ship it.*\n- **Built Castify OS — three internal AI modules, live** — Jun 15 – Jul 31, 2026. Idea (Jun 15) → first agent skill (Jun 18) → build spec (Jun 29) → shipped (Jul 7) → first live client (Jul 9) → third module in beta (Jul 31). **Proposal Generator** (sales, live): deal context in, client-ready branded PDF + editable .docx out, **30 min → 5 min per proposal**. **Outbound Email Agent** (marketing, live): reads pending contacts from a sheet, researches, drafts into Gmail — *approval-gated by design, it never auto-sends*. **Traffic & AI Referral Analytics** (marketing, beta): two brands and two sites in one view, with AI-assistant referrals broken out as their own channel class. *Takeaway: AI doesn't replace the workflow. It removes the part of the workflow nobody wanted to do.*\n- **Ran True North's first company all-hands** — Jul 17, 2026, co-presented with the operations lead. Nine parts: the quote process end to end (respond as True North and capture name + address → price face to face with your reasoning → log it in the quote form → send a formal quote → book it and brief the crew), a live site walkthrough, a LinkedIn team-spotlight program, a written marketing do's-and-don'ts (before *and* after on every post, same angle, natural light, teammates credited, never a client's address), and a referral incentive of a **$10 minimum** per client — paid only once the client is landed *and* the job is completed.\n- **Built True North's digital infrastructure** — May – Aug 2026. Hand-built company site (standalone HTML, Netlify, custom domain) at truenorthservicesllc.netlify.app; lead-capture form mapped to service categories; a post-quote form feeding a responses sheet so close rate is a visible number; 90+ photo marketing library; SMS-automation scoping.\n- **Wrote the crew training program** — May 2026. Head-manager service training: power washing, paver-sand reapplication, and window washing, each a six-step field procedure with its own do's and don'ts. *Takeaway: the training is the quality control — you can't inspect your way to a standard you never wrote down.*\n- **Put the company on a documented operating system** — Apr – Jun 2026. One path per lead: lead → **Form 1** quote scheduling & assignment → appointment → **Form 2** quote result & pricing (scope, before photos, price, labor/materials estimate, payment terms, close probability) → approval → job → **Form 3** end-of-job report (actual hours, materials, after photos, property condition, damage, upsells, equipment) → invoice → paid. All three feed one master sheet: leads, quotes, closed jobs, scheduling, payments, applicants, referral partners, manager performance, equipment, weekly scorecard. *Takeaway: no form, no distribution. The paperwork is the control.*\n- **Incorporated True North Services LLC** — Apr – May 2026. LLC filing, operations & risk framework, written priority roadmap, and a manager-contractor model (managers are independent contractors who own crews, scheduling, and execution risk). Team of **four-plus**, operations lead running weekly ops calls. Economics: of gross, **20%** company / **5%** backend / **5%** lead fee / **70%** manager. Floors: **$100** minimum job, **30%+** manager margin target, pricing-floor check before quoting. Liability: jobs over **$500** require a signed client agreement; managers sign a scope-and-indemnification agreement; before/after photos on property-sensitive jobs; card fees passed through flat. Service-boundary matrix: direct — power washing, window washing, weeding, mulching, bush trimming, staining, pavers, gravel paths, seasonal cleanups; referral-only — roofing, plumbing, electrical, large tree work, licensed trades; anything else is approval-only. Cash: bank transfer first, cash second, card third; revenue lands in the LLC account, distributions biweekly once documentation, disputes, and refunds settle. *Takeaway: from hustle to institution — docs, training, and structure that run without you in the room.*\n- **Asset tracking system** — 2026, ongoing. Five-platform portfolio tracker in Google Sheets, monthly-tab automation, feeding a private dashboard (on this site until 2026-09-15, when it moved to its own private repo; the site no longer links to it).\n\n**2025**\n- **Manager & equity partner, Student Maintenance LLC** — spring 2025. Brought on as manager with an ownership stake: on-site quoting, closing leads, marketing content, sales-team leadership. `[OPEN: include on the public site at all? If yes, confirm dates + how to describe the stake.]`\n- `[OPEN: junior-year highlights]`\n\n**2024**\n- **Business + portfolio, year two** — ran the cleaning business back-to-back summers while carrying the dual major; self-managed portfolio active since 2020.\n- `[OPEN: what defined 2024 — clubs, coursework, the summer story?]`\n\n**2023**\n- **Started at Elon University** — Aug 2023. Finance & Accounting dual major; minors in Entrepreneurship and Mandarin Chinese (near-fluent). `[OPEN: confirm still accurate]`\n- **Founded Oasis Exterior Cleaning** — May 2023. Power washing + window washing, Chicago North Shore. 50+ clients, five-figure revenue (~$12k profit) in the first summer, +68% customer growth from hand-built local marketing (flyers, Nextdoor, Instagram, Facebook), managing a crew of high-school and college students. *Takeaway: sold a service door to door and ran a crew before most people pick a major.* `[OPEN: is Oasis→True North one continuous story (rebrand) or two chapters?]`\n\n**Prologue — before Elon (muted block)** `[OPEN: keep or cut]`\nFirst W-2 at Taco Bell (2021) · mover/assistant at Inspired Interiors (2021–23) · one-month internship under Gemini Builds It CEO Courtney Wright (spring 2023) · trading his own portfolio since Aug 2020 · New Trier High School, class of 2023.\n\n### 3a. True North — open naming question\n\nPierson's own documents use three names for the same entity. Resolve before the About and Projects pages ship:\n\n- **True North Services LLC** — the all-hands deck (Jul 17, 2026), the live site, the Drive folder, the structure doc's filename. **This is what the site currently uses.**\n- **True North Maintenance LLC** — the Operations & Risk Framework and the Priority To-Do List (both Apr 2026).\n- **North Shore Services LLC** — the heading inside `True_North_Services_Structure 2.docx`.\n\n`[OPEN: Pierson — which is the name on the filing?]`\n\n### 3b. Screencastify — publication boundary\n\nKept **off** the public site deliberately: the first live client's name, Screencastify's internal channel performance figures (total and average view counts), and the names of their internal vendor tools. The page describes what Pierson built and the time saved, not his former employer's private numbers. `[OPEN: Pierson — confirm, or say which of these he has clearance to name.]`\n\n## 4. Skills (Experience page grid — structured list, crawler-friendly)\n\n- **Operations & leadership** — crew hiring and training programs, documented ops frameworks, weekly ops cadence, manager-contractor structuring, client management (50+ clients).\n- **Sales & local marketing** — door-to-door and referral sales, in-person quoting, Nextdoor/Meta local campaigns, lead-capture funnels, content standards.\n- **Finance & markets** — Finance & Accounting dual major; unit economics, pricing floors and margin structures; active self-directed investor since 2020 (equities, options, crypto — five platforms).\n- **AI & automation** — workflow mapping and diagnosis, prompt-built internal tools, agent modules with human approval gates, Google Sheets/Apps Script automation, Sheets API, N8N exploration.\n- **Web** — hand-built HTML/CSS sites (True North on Netlify; this site), form-to-sheet plumbing, static deploys.\n- **Languages** — English; Mandarin Chinese (near-fluent).\n\n## 5. Projects (Projects page cards)\n\n1. **True North Services — digital infrastructure** (2026, live) — company site, lead capture, photo library, ops docs. Link: True North site URL `[OPEN: confirm final URL]`.\n2. **Portfolio Asset Tracker** (2026, private) — Google Sheet → GitHub Action → dashboard; five platforms, monthly history. Lives in its own private repo since 2026-09-15, so the card has no link. `[OPEN: Pierson — keep this card on the Projects page at all?]`\n3. **Screencastify AI internship tooling** (2026) — the 7-workflows/48-hours diagnosis method + three shipped fixes. Links to Experience entry.\n4. **piersonnorris.com** (2026) — this site: plain HTML, dark system, AI-crawlable by design, spec'd with Claude, built with ChatGPT. Repo link once public.\n\n## 6. Tools page\n\nIntro: \"Things I build for myself. Some are public, some are locked.\"\nCard — **Portfolio Tracker**: removed 2026-09-15. The tracker moved to its own private repo and no longer has a page on this site. `[OPEN: Pierson — does the Tools hub still ship without it, and with which cards?]`\n\n## 7. Numbers that may appear on the site (all from Pierson's own docs)\n\n50+ clients · five-figure first-summer revenue (~$12k profit) · +68% customer growth · 7 workflows / 6 departments / 48 hours / 1 meeting added · 3 shipped fixes · 4–8 hrs/week → under 15 minutes · ~30 min/week saved on the Monday email · 76 posts audited · 3 Castify OS modules live · 30 min → 5 min per proposal · 9 weeks idea-to-platform · 5 platforms tracked · team of 4+ · 20/5/5/70 revenue split · $100 minimum job · 30%+ manager margin target · $500 signed-agreement threshold · $10 minimum referral incentive.\n\n**Never on the site:** the spreadsheet ID, any credential. Also off the site: portfolio dollar values, share counts and individual holdings — **everywhere, no exceptions**. The one exception there ever was, the open `/tools/tracker/` page Pierson chose on 2026-09-08, ended on 2026-09-15 when the tracker moved to its own private repo (`stock-trackers`). The figures it published remain in this public repo's git history. Also off the site: Screencastify client names, their internal channel performance figures, and their internal vendor tool names (see §3b); True North client names, addresses, and revenue figures.\n","tags":[],"created":"2026-09-15T21:06:44.121Z","updated":"2026-09-15T21:06:44.121Z"},{"path":"docs/ISLAND_TO_ISLAND.md","folder":"docs","title":"ISLAND TO ISLAND — the work, laid out as a chart","body":"# ISLAND TO ISLAND — the work, laid out as a chart\n\n**Status: direction picked 2026-09-08, not built.** ROADMAP **R19**.\nLive preview (local only): `vault/concepts/index.html`, direction 06.\nSibling: [[SHIP IN A BOTTLE — the Obsidian showcase]] (R18) — same ocean, different job.\n\n---\n\n## 0. What this is\n\nPierce, 2026-09-08, picking direction 01 for the bottle and then: *\"lets add as the md 2 for the layout of my\nwork ive done island to island with a visual.\"*\n\nSo direction 02's cartography stops competing with 01 and gets pointed at something else entirely: **the work\nhistory, as a voyage.** Every chapter is an island. The route between them is the thing that carried over.\n\nIt is not a second timeline. The Experience page already has one, and a timeline can only ever show **order**.\nA chart shows **relation** — what fed what — and that is the actual story: door-to-door at Oasis became a crew,\na crew became a documented company, the documented company became the method that worked inside somebody\nelse's business.\n\n---\n\n## 1. The islands\n\nChapters and dates come from [[CONTENT — piersonnorris.com]] §3 verbatim. Nothing here is new copy; it is existing copy\n**placed**. Each island's inscription is that chapter's own *Takeaway:* line where one exists.\n\n| # | Island | When | Inscription (from CONTENT.md §3) |\n|---|---|---|---|\n| 1 | **Self-directed portfolio** | 2020 — ongoing | The longest-running thing on the chart, and the only one still underway from the start |\n| 2 | **Oasis Exterior Cleaning** | May 2023 | *Sold a service door to door and ran a crew before most people pick a major* |\n| 3 | **Elon University** | Aug 2023 — | Finance & Accounting dual major; Entrepreneurship and Mandarin minors |\n| 4 | **True North Services LLC** | Apr 2026 — | *From hustle to institution — docs, training, and structure that run without you in the room* |\n| 5 | **Screencastify** | Jun 9 – Aug 7, 2026 | *The method is the résumé line — find the problem in someone's actual Tuesday, get sign-off, build it with them, ship it* |\n| 6 | **This site** | 2026 — | The tracker, the vault, the atlas — built in the open |\n| 7 | **Elon AI & Emerging Tech Club** | 2026, incoming | **Drawn as an outline, not a landfall** — charted, not yet reached |\n\n**Deliberately not on the chart:** Student Maintenance LLC. [[CONTENT — piersonnorris.com]] §3 marks it `[OPEN: include on the\npublic site at all?]`, and an open question does not get an island. The water stays empty rather than filled\nwith a guess — which is the same rule the atlas follows.\n\n**Oasis → True North** is drawn as **two islands with a lane between them**, not one landmass. §3 flags whether\nthat is a rebrand or two chapters as an open question; two islands is the reading that is true either way.\n\n---\n\n## 2. The lanes are the argument\n\nThis is the part that earns the whole layout. Each lane carries a label — what transferred, not merely what\ncame next:\n\n| Leg | What carried over |\n|---|---|\n| Portfolio → Oasis | Money is a system, not a windfall |\n| Oasis → Elon | Crew and door-to-door |\n| Elon → True North | Hustle → institution |\n| True North → Screencastify | The documented system |\n| Screencastify → This site | The workflow method, turned on my own work |\n| This site → *(ahead)* | — dashed; not sailed yet |\n\nA résumé bullet list physically cannot say any of that. A chart can say it in one glance, and it is all\ndefensible from §3.\n\n---\n\n## 2a. R19 is the home page's data layer — decided 2026-09-08\n\nPierce, 2026-09-08: *\"add to the note that R19 will be started as the data for the home page when its updates.\"*\n\nThis is a scope change and a good one. R18 phase A showed the Observatory owns the first screen but has no\nanswer for what sits below it — the six console tiles have nowhere to go in the new composition.\n\n**R19's data file becomes that answer.** `assets/js/voyage-data.js` is not only the Experience chart's source;\nit is **what the home page reads for everything below the fold**. The work section stops being six\nhand-maintained tiles and becomes the voyage, rendered compactly.\n\n- **One source, two surfaces.** Experience gets the full chart, the home page gets the strip, both read the\n  same file. Add a chapter once — the same rule `atlas-data.js` already follows.\n- **Sequencing: the data lands *inside* the home-page pass**, not after it. `voyage-data.js` must exist by\n  R18 **phase E**, because phase E is what replaces the tiles. The chart *rendering* on Experience can follow\n  later; the data cannot.\n- **Which promotes a blocker.** The lane labels below need Pierce's own words, and they are now on R18's\n  critical path rather than a nice-to-have afterwards.\n\n## 3. Where it goes\n\n**Primary: the Experience page**, replacing the top of it — the chart first, the existing year-by-year detail\nbelow it, unchanged. Clicking an island scrolls to that chapter, so the chart is a map *of* the page rather\nthan a substitute for it.\n\n**Secondary: a compact strip on the home page** — the route only, no labels, six islands wide, that links\nthrough. That is the piece that makes it part of R18's home-page pass rather than a separate errand.\n\nOpen question for Pierce: does the chart **replace** the vertical timeline on Experience, or sit above it?\nRecommendation is *above it* — recruiters scan, and the timeline is the scannable thing.\n\n---\n\n## 4. Build notes\n\n- **Placement comes from dates, not taste.** Longitude is time; latitude is free for layout, but the x-order\n  must be derived. If an island is placed by eye, this is a drawing, not a chart, and it will drift out of\n  date the first time a chapter is added.\n- **One data file**, `assets/js/voyage-data.js`, same shape and same rule as `atlas-data.js`: every field traces\n  to [[CONTENT — piersonnorris.com]], and when that changes, this changes. Islands, dates, inscriptions and lane labels all live\n  there — no copy in the markup.\n- **Island shapes are generated, not drawn**, from a seeded blob so they're stable between loads but not\n  hand-placed. Size maps to duration.\n- **Accessibility is the real risk.** Serif on parchment has to clear 4.5:1 (U1 was fought over exactly this),\n  every island needs a real focusable control, and the whole thing must degrade to the existing ordered list\n  under ~760px. **The chart is an enhancement; the list is the content.**\n- **Reuse:** the parchment treatment, compass rose and lane styling are shared with R18's ocean language, so\n  Home → Experience reads as one world rather than two themes bolted together.\n\n---\n\n## 5. Open\n\n1. **Above the timeline, or instead of it?** (recommendation: above)\n2. **Is the home-page strip in scope for R18's pass**, or a follow-up?\n3. **Lane labels** — the six above are drafted from §3. They are claims about your own career, so they need\n   your words, not mine, before they ship.\n","tags":[],"created":"2026-09-15T21:06:44.121Z","updated":"2026-09-15T21:06:44.121Z"},{"path":"docs/OBSIDIAN_SYNC.md","folder":"docs","title":"Obsidian connectivity — graph view + vault sync","body":"# Obsidian connectivity — graph view + vault sync\n\nStatus: **shipped 2026-09-07** (ROADMAP R15). Two halves that meet in the middle: a graph that shows how notes connect, and a pipeline that gets the *real* vault in front of it.\n\n---\n\n## 1. The graph — `assets/js/notes-graph.js`\n\n`PNGraphify` mounts inside the notes UI on `/notes/` (general vault), behind a **Graph** button in the toolbar. The tracker's Obsidian tab (stock vault) had one too; it moved with the tracker to the private `stock-trackers` repo on 2026-09-15.\n\nDeliberately split in two:\n\n| Half | What it is | Tested |\n|---|---|---|\n| `build(notes, {tags, missing})` | Pure. Notes → `{nodes, links, stats}`. No DOM, no layout, no randomness. | Yes — `tools/tests/notes-graph.test.js` |\n| `mount(el, opts)` | Force-directed SVG: pan, zoom, node dragging, neighbourhood highlight, click-to-open. | No — left to the browser |\n\n### The judgement calls `build()` makes\n\nThese are the decisions the picture then draws confidently, so they are the part worth writing down:\n\n- **A link is a resolved `[[wikilink]]`, once.** `A → B` and `B → A` are one connection, not two. `[[Note|alias]]` and `[[Note#heading]]` resolve to `Note`, matching Obsidian. Resolution is case-insensitive; a note linking to itself is not a link.\n- **A tag is not a link.** Tag nodes are drawn (toggleable) and tags are counted, but sharing `#macro` does not connect two notes and does not rescue either from being an orphan. Tagging is a weaker signal than linking and conflating them would flatter the vault.\n- **An unresolved link is not a link either.** A note whose only bracket points at a note that doesn't exist is still an orphan. That is exactly why orphans and unresolved links are two separate numbers.\n- **Data blobs are not writing.** Notes carrying a `noteType` (the tracker's calendar and project-board storage) are excluded — they would dominate the picture.\n- **Density** is `links × 2 / notes`: average links per note, the single number for whether the vault is a web or a pile.\n\n### Two known implementation traps\n\n- **Layout runs synchronously before the first paint.** Nodes are positioned by a `transform` on their `<g>`, and `requestAnimationFrame` does not fire at all in a background tab — so a graph that waited for the first animation frame rendered the entire vault stacked at `0,0`. The sim settles ~90 steps up front, paints, auto-fits the camera to the node bounds, and only then animates the remainder.\n- **Focus rings.** `.pg-node:focus-visible` restates its own ring for the same specificity reason `.pnchart-graph .vg-node` had to (ROADMAP U2): a two-class selector outranks the generic `[tabindex]:focus-visible` rule regardless of source order.\n\n`PNGraphify` is separate from `PNCharts.graph`, the tracker Projects tab's projects-and-AI-thoughts map, which left this repo with the tracker.\n\n---\n\n## 2. The sync — `tools/obsidian-sync.js`\n\n```bash\nnode tools/obsidian-sync.js --vault \"C:\\path\\to\\Vault\"        # → private/notes/obsidian-vault.json\nnode tools/obsidian-sync.js --vault \"…\" --list --dry-run      # report only, writes nothing\nnode tools/obsidian-sync.js --help\n```\n\n| Flag | Effect |\n|---|---|\n| `--vault <dir>` | The vault folder. Falls back to `OBSIDIAN_VAULT`, then `private/.obsidian-vault-path` (one line, gitignored). |\n| `--out <file>` | Bundle destination. Default `private/notes/obsidian-vault.json`. |\n| `--scope general\\|stocks` | Which vault the bundle is meant for. |\n| `--no-body` | Index only: titles, tags and paths, no note text. |\n| `--list` | Print orphan and unresolved titles. **Off by default** — the script prints counts, never note content. |\n| `--stdout` / `--dry-run` | Print the bundle / report and write nothing. |\n\nIt walks the folder for `.md`, skipping every dot-directory (`.obsidian`, `.trash`, `.git`). Parsing is **not** reimplemented: it calls the site's own `PNVault.fromMarkdown`, so a note read here and a note imported through the browser end up identical. The only thing layered on top is block-style YAML (`tags:\\n  - a`) plus `alias`/`aliases`, which Obsidian writes and the browser importer never had to handle.\n\nOutput is one `pn-vault-bundle` JSON: `{format, version, scope, generatedAt, source, count, notes[]}`, each note carrying its vault-relative `path`.\n\n### Getting a bundle into a vault\n\n**Route A — Import button (`/notes/`, no build).** Pick the one `.json` file instead of hand-selecting hundreds of `.md` files. `PNVault.importBundle()` **upserts** by `sourcePath`, so re-running the sync after editing in Obsidian updates notes in place; the old `.md` path only ever *added*, which made repeat imports useless. `source_path` is written into exported frontmatter too, so the identity survives a full round trip. Nothing is ever deleted: a note removed from the vault folder stays in the browser until you delete it.\n\n**Route B — baked into the encrypted build (tracker) — now in the `stock-trackers` repo.** Drop the same bundle at that repo's `private/tracker/obsidian-vault.json` and its `build.js` (`loadObsidianVault`) seals it into a `--sealed` payload as `obsidianVault`, exactly like `googleEvents`; an open build refuses both. The tracker's Obsidian tab then shows a banner offering a one-click merge. Merging is a button and never automatic — it overwrites notes that came from the same vault paths, and silently rewriting a vault on unlock would be a surprising thing for a page to do. Malformed or missing input degrades to `null`; the build never fails over vault data.\n\n---\n\n## 3. The public route — `--public` (added 2026-09-08, R17)\n\nThe third destination for a bundle, after the Import button and the encrypted build: a committed file the\npublic `/vault/` page reads. Design and reasoning live in [[BACKSTAGE — the vault, in public, with no gate]]; the mechanics are here.\n\n```bash\nnode tools/obsidian-sync.js --vault \"<dir>\" --report                  # what WOULD publish. Writes nothing.\nnode tools/obsidian-sync.js --vault \"<dir>\" --public --prefix notes   # → assets/data/vault-public.js\n```\n\n| Flag | Effect |\n|---|---|\n| `--report` | Every note that would publish — path, word count, title — plus scrubber hits. No note text. |\n| `--public` | Writes the public bundle. Default `assets/data/vault-public.js`. |\n| `--label <name>` | What the page calls the snapshot. |\n| `--prefix <dir>` | Nests published paths under a folder, so the tree has something to draw. |\n| `--waive <ids>` | Skip named scrub rules for this run. Printed loudly and recorded in the bundle. |\n| `--no-link-paths` | Leave inline path references alone instead of resolving them into wikilinks. |\n\nThree things are worth knowing before running it against a real vault:\n\n- **Default is publish, and the opt-outs are Pierce's.** `publish: false` in frontmatter, a `#private` tag, or a\n  `nopublish/` folder. That default was his decision (2026-09-08), taken over two narrower scopes.\n- **The scrubber refuses; it never redacts.** Dollar amounts, share counts, addresses, phone numbers,\n  key-shaped tokens, plus literal terms from `private/.publish-blocklist` (gitignored — a committed list of\n  names you must never publish is itself a published list of those names). A hit prints `path:line`, never the\n  matched text, and stops the write. The fix belongs in the vault.\n- **Output is `.js`, not `.json`.** It loads with a `<script>` tag, exactly like `atlas-data.js`, so the page\n  works over `file://` with no fetch and no CORS. A `.json` path is still honoured if you ask for one.\n\nA publish-time transform runs on the way out: an inline-code path pointing at another published note\n(`` `docs/CONTENT.md` ``) becomes a `[[wikilink]]`. Vaults link with brackets; the site's own docs link by\nwriting a path, and both are the author saying \"this connects to that\". Only the exact inline-code form is\nrewritten — guessing inside prose or fenced blocks would invent edges nobody wrote. The count lands in the\nbundle as `linkedPaths` so the page can say it happened.\n\nThe first published snapshot is the site's own `docs/` folder, which was already public in this repo. The\npersonal vault is not published; that step is P4 in the plan and waits on Pierce.\n\n## Known boundary\n\nSame as the portfolio calendar plan (now in the `stock-trackers` repo) says about Google Calendar, and for the same reason: **this is a snapshot, not live sync.**\n\nThe site is static, served from GitHub Pages. It has no server, no OAuth, and no way to read a folder on disk — not from the browser, not from CI. `obsidian-sync.js` runs on the machine that has the vault, outside the site's own code. Refreshing means running it again and re-importing (or rebuilding). GitHub Actions cannot do this pull: the vault lives on Pierce's disk, not behind a portable credential.\n\nNote text never enters the repo. The bundle path is gitignored, and Route B's copy exists only inside the AES-256-GCM payload.\n","tags":[],"created":"2026-09-15T21:06:44.122Z","updated":"2026-09-15T21:06:44.122Z"},{"path":"docs/ONE_GREAT_JAR.md","folder":"docs","title":"KNOWLEDGE ATLAS → ONE GREAT JAR","body":"# KNOWLEDGE ATLAS → ONE GREAT JAR\n\n**Status 2026-09-10: steps 1, 2 and 3 done. The jar is sealed, real, and it opens — the swarm pours\nout through the mouth and fills the scene, and seals back. Steps 4–7 remain; §9 questions 1 and 2 are\nanswered, 3 and 4 still need Pierce's call.**\nRedesign of `/atlas/` as an Obsidian showcase: the vault as fireflies in one jar.\n\nDecided by Pierce, 2026-09-10: direction **B**, one jar, real vault data, telescope + Ship's log kept,\nrich-but-steady motion, scope is the Atlas page only.\n\nBrief in his words: *\"the idea is to make this page showcase obsidian in a visualy stunning way.\"*\n\nComponent: `assets/js/atlas-jar.js` + `assets/css/jar.css` + `assets/js/vault-groups.js`.\nScratch route: `vault/concepts/jar/` (local only, same as the Observatory's comps).\nCompanion: [[SHIP IN A BOTTLE — the Obsidian showcase]] (R18) — the home page's bottle, which this must not duplicate.\n\n---\n\n## 0. The one rule\n\nThe swarm is the real `[[wikilink]]` graph, drawn by `PNGraphify` from real notes. A pretty swarm that\nisn't the actual structure is a screensaver, and it undercuts the only thing the page exists to prove.\nSame rule the Bottle Observatory holds itself to.\n\n## 1. What this is not\n\n`/` (home) is becoming the **Bottle Observatory** — a bottle on an ocean, notes as a constellation behind\nglass. This page must not read as a second version of that. The separation, stated so it can be checked:\n\n| | Bottle Observatory (`/`) | Jar (`/atlas/`) |\n|---|---|---|\n| Object | bottle on its side, sea | mason jar standing on a plank, night |\n| Notes are | still stars behind glass | **living fireflies** — they move on their own |\n| Structure shown as | drawn lines | **clusters** — the vault's actual communities |\n| The trick | glass, and the morph out of it | **the swarm organising itself** |\n| Job | first impression of Pierce | how Pierce actually uses Obsidian |\n\nAlso inherited: the showcase never links to the tracker.\n\n## 2. The composition\n\nThree states on one page. No route changes, no page transitions.\n\n**State 1 — SEALED.** Full-bleed night. One jar, centered, lid on, standing on a plank. Inside: every\nnote in the vault as a firefly, drifting, breathing, colliding softly with the glass. Fireflies are\nalready sorted into their real clusters, so the jar reads as several distinct glowing masses rather than\nsoup. Etched on the glass label: real counts — notes, links, links per note, orphans, unresolved.\nCopy sits **outside** the glass, capped at `min(40ch, 36vw)` (the Observatory's phase-A finding, taken as\ngiven rather than re-learned). One affordance: the lid.\n\n**State 2 — OPEN.** Lift the lid. Fireflies rise out and spread across the whole viewport, each one\nwalking its own path from its place in the glass to its place in the room — a layout morph, not a zoom\n(again, Observatory phase A: a zoom inflates nodes and drops half the vault off-screen). The jar demotes\nto a rim vignette plus the base it's standing on, so you stay inside the metaphor. Labels appear at\nreadable size. This is the state the page lives in.\n\n**State 3 — SELECTED.** *(Revised 2026-09-10 by Pierce's answer to §9 Q1: reading lives at `/vault/`.)*\nPress a firefly. It holds still and brightens; its linked neighbours stay lit and named; everything else\ndims to embers. The firefly's own card shows title, folder, tags and link counts, and the way to the\nwords themselves is a link out to `/vault/#docs/BLUEPRINT.md`. The jar does not render note bodies and\ndoes not need a Markdown renderer — `/vault/` already is the reading surface and does it properly.\n\nThe cost of that answer, stated so it isn't a surprise later: pressing a firefly eventually leaves the\npage, which is the one place §2's \"no route changes, no page transitions\" gives way. It buys not having\na second reader to keep in step with the first.\n\nPhone is a **third composition, not a reflow**: jar upright in a band, copy above, controls below,\nreading panel as a sheet.\n\n## 3. What the fireflies encode\n\nEvery visual property is data. Nothing is decorative-only.\n\n- **Cluster → hue.** Communities from label propagation over the link graph. Labels are *derived, never\n  invented*: shared folder (≥60%) → shared tag (≥60%) → *\"around HUB\"* → **\"unnamed group · 6 notes\"**.\n  Rule 4 is load-bearing: if the vault hasn't earned a name, the cluster doesn't get one.\n- **Degree → brightness and size.** A hub note is a bright, slow, heavy firefly. A leaf is a faint spark.\n- **Recency → flicker rate.** Recently-edited notes pulse quickly; stale notes fade to a slow ember.\n- **Orphans → dark, drifting alone,** counted in the readout even though nothing connects to them.\n  Hiding them flatters the vault; R15's argument.\n- **Unresolved links → a firefly flying toward nothing** and winking out at the edge.\n\n## 4. Controls\n\nFour, all of them earning their place. Everything else stays cut.\n\n1. **The lid** — seal / open. The only control in state 1.\n2. **The telescope** *(kept from today's Atlas)* — draws the link filaments between fireflies. Off by\n   default: the clusters read better as light, and the lines are the \"prove it\" toggle.\n3. **Find groups** — hulls + earned labels over the clusters. Off by default; grouping is an\n   interpretation laid over the honest view, and toggling it never re-lays out the swarm.\n4. **Ship's log** *(kept)* — the vault by time. Not a separate crate any more: it re-sorts the swarm into\n   a timeline band, newest fireflies near the surface, oldest settled low. Same fireflies, second axis.\n\n**Cap, stated honestly.** ~90 fireflies drawn, ranked by degree then recency, with the readout\n*\"showing 90 of 312 — ranked by connections.\"* Never a silent truncation. Plus a largest-connected-\ncomponent toggle. This is what makes pointing the page at a few-hundred-note vault safe.\n\n## 5. Motion budget — \"rich but steady\"\n\nEvery effect is an opacity, a transform, or a `stroke-dashoffset`. Nothing touches layout, nothing\nreflows per frame.\n\nPer-firefly drift on its own seeded path · brightness breathing · a slow warm wash inside the glass ·\ndust motes outside it · a specular sweep on the jar shoulder and one rim light on the base (two marks,\nnot ten) · pulses running down filaments when the telescope is on · the lid's rise.\n\n`prefers-reduced-motion` is a **path, not a switch**: the morph becomes an instant placement, drift stops,\nflicker becomes static brightness. Page still works; it just stops moving.\n\nPositioning and animation live on **separate nested groups** — a CSS animation on the same element the\nscript positions snaps it to the origin. Written down because it already bit the Observatory build.\n\n## 6. Data\n\nPierce picked **his real Obsidian vault**, not the curated lesson notes.\n\nPath: `tools/obsidian-sync.js --vault \"<vault>\" --report`, read the report, then `--public`. The publish\npipe, the scrubber and the `publish:false` / `#private` / `nopublish/` exclusions are already built\n(BACKSTAGE_PLAN P1–P3). Until that runs, the page reads today's `assets/data/vault-public.js` snapshot\n(13 notes, the site's own `docs/` folder) so the visual can be judged before anything irreversible.\n\n**The flag, once, then out of the way:** the commit that publishes personal notes is permanent, git\nhistory included. BACKSTAGE_PLAN §4 and §7 are unanswered — third-party names, whether the report changes\nthe answer. The design does not depend on which vault it points at; the swarm scales either way.\n\n> **Amended by step 2, 2026-09-10.** That last sentence is now the one thing in this plan that step 2\n> contradicted. The swarm *scales* either way, but it does not *read* either way — see finding 2. On the\n> docs snapshot the page is a nice picture; at real-vault scale it is the page this document describes.\n\n## 7. Files\n\n| File | Change |\n|---|---|\n| `atlas/index.html` | rewritten — jar scene, copy, controls, reading panel, Ship's log |\n| `assets/css/atlas.css` | rewritten — night palette kept, sea/sand/palms/star-sky removed |\n| `assets/js/atlas-ui.js` | replaced by `atlas-jar.js` — swarm layout, morph, clustering, reading |\n| `assets/js/notes-graph.js` | reused unchanged for link structure |\n| `assets/data/vault-public.js` | data source (regenerated when Pierce runs the publish) |\n| `assets/js/atlas-data.js` | kept on disk; the lesson notes become an optional second source |\n\nPalette from today's `atlas.css` (`--n-top #060b1c` → `--n-horizon`, `--sky-ink`, `--n-paper`), fonts\nArchivo / IBM Plex Sans / IBM Plex Mono / Kalam. Firefly hues from the six domain colors already in\n`atlas-data.js` (`#5FAE5A`, `#E8A33D`, `#4CC9F0`, `#c084fc`, `#37d7c2`, `#a9bcd6`), hue-shifted per\ncluster so a note's identity color survives grouping.\n\n**As built in step 2**, the new code went to new files rather than over the old ones, so `/atlas/` keeps\nworking while §9's question 3 is open:\n\n| File | State |\n|---|---|\n| `assets/js/vault-groups.js` | new — `PNVaultGroups.groups(notes)`, pure, seeded, 17 tests |\n| `assets/js/atlas-jar.js` | new — `PNJar.mount(el, {data, cap, onOpen})`, the sealed jar |\n| `assets/css/jar.css` | new — will become the rewritten `atlas.css` at swap time |\n| `tools/tests/vault-groups.test.js` | new — the D2 spec's four fixtures plus the labelling rules |\n| `vault/concepts/jar/` | new scratch route, gitignored like the Observatory's comps |\n| `atlas/index.html`, `atlas.css`, `atlas-ui.js` | **untouched** — the before-picture still runs |\n\n## 8. Build order\n\nEach step ends somewhere it's safe to stop.\n\n| Step | What | Status |\n|---|---|---|\n| **1** | Recreate `/atlas/` as-is as the before-picture, from the real source files | ✅ done — the live page *is* the real source, so it was captured rather than rebuilt |\n| **2** | **The jar, sealed** — geometry, glass marks, label etching, fireflies drifting, real counts. Judge at full size before anything else | ✅ **done 2026-09-10** — findings in §8a. Verdict: it holds, and it wants the real vault |\n| **3** | Open + the morph — second layout, per-node paths, jar → vignette | ✅ **done 2026-09-10** — findings in §8b |\n| **4** | Selection — neighbour highlighting, a card of title/folder/tags/counts, and a link out to `/vault/#<path>`. No in-page reader, no Markdown renderer | unblocked — §9 Q1 answered |\n| **5** | Telescope, Find groups, the cap, the readout | the cap and readout are already built; clustering engine is done and tested |\n| **6** | Ship's log as the timeline band | **blocked on data** — see finding 3 |\n| **7** | Phone composition, then reduced-motion, keyboard, contrast | phone + reduced-motion + lid keyboard done in step 2; contrast pass outstanding |\n\n## 8a. Step 2 — done 2026-09-10. What the jar decided.\n\nFull viewport, real geometry, real type, and **13 real notes / 17 real links read straight out of\n`assets/data/vault-public.js`** — plus a rehearsal switch that clones the snapshot up to 90 so the\ncomposition could be judged at the size it will actually run at. Desktop and phone.\n\n### Three findings about the data, and they matter more than the drawing\n\n**1. The docs snapshot is one cluster, not several.**\n`PNVaultGroups.groups()` on the 13-note snapshot returns **one** community of 10 notes plus 3 orphans —\neverything links to `ROADMAP`. State 1's premise, \"the jar reads as several distinct glowing masses\nrather than soup\", is simply false on this data. It is not a bug in the clustering; it is what the docs\nfolder is. The engine is honest about it and the readout prints `1 group`.\n\n**2. At 13 fireflies this is a nice picture. At 90 it is the page §2 describes.**\nThe rehearsal (`vault/concepts/jar/?n=90`) resolves into **7 groups**, the hues separate, and the jar\nfills with distinct masses of light. The difference is not subtle and it is not a matter of polish. This\ndesign has a floor below which its whole idea — *clusters, as light* — has nothing to show. That makes\n§9's question 2 the one that decides whether this page is worth finishing, and it argues for the real\nvault rather than against it.\n\n**3. Recency carries no signal at all in the published snapshot, and that blocks step 6.**\nAll 13 notes' `created` and `updated` timestamps fall within **8 milliseconds** of each other — they are\nthe clock of the publish run, not the notes' history. `tools/obsidian-sync.js` falls back to\n`stats.mtime`, and git does not preserve mtime, so a fresh checkout of `docs/` stamps every file at\ncheckout time. Consequences:\n\n- The **recency → flicker rate** encoding is inert. Rather than fake a gradient across 8ms, the component\n  detects a spread under an hour and gives every firefly the same steady breathing, and the readout says\n  `recency: no signal in this snapshot`.\n- **Step 6, the Ship's log, cannot be built or judged on this data.** A timeline band over 8ms is a flat\n  line. It is not blocked on design; it is blocked on pointing at a vault with real mtimes.\n- This is *not* a bug to fix in the pipeline. Read off Pierce's disk, where Obsidian writes files as he\n  edits them, mtime is real. It is another argument for §9 question 2.\n\n**4. A folder label does not discriminate on a single-folder vault.** Rule 1 fires for every cluster in\n`docs/` and names them all `docs/`. The engine implements the spec as written and additionally reports\n`stats.labelsDistinct`, so step 5 can decline to draw three hulls that all say the same word rather than\nsilently inventing a better name.\n\n### What the comp decided about the drawing\n\n- **The plank had to leave the viewBox.** Drawn inside it, the jar read as an object in a box. Drawn from\n  x −1400 to 1860 with `overflow:visible` on the svg, it becomes a shelf running off both edges of the\n  page and the scene turns into a room. Single biggest composition win, and it cost one path.\n- **The first jar was a bottle.** 266 × 522 with a 55% mouth read as the *other page's object*. A real\n  regular-mouth quart jar is squatter and wider-mouthed: now 296 × 390 with a 68% mouth, and it reads\n  correctly at a glance. §1's separation table is a design constraint, not just a note.\n- **The glass needed a dark liner.** One bright stroke on a background of nearly the same value loses the\n  silhouette. A 4.5px near-black stroke *under* the 1.9px bright one holds the edge. The Observatory's\n  \"two marks, not ten\" still holds — the liner is part of the outline, not a third mark.\n- **The lid is an SVG `<g role=\"button\" tabindex=\"0\">`, not an HTML overlay.** An overlaid button is wiped\n  the instant `mount()` writes `innerHTML`, which is how the first version broke. Enter, Space and click\n  all verified.\n- **The etching is illegible on a phone** — it scales with the jar and lands around five pixels. The panel\n  is hidden below 900px and the readout directly under the jar carries the same five numbers at a\n  readable size.\n- **The phone readout had to become its own grid child.** `order` on an element nested inside the copy\n  column does nothing; copy / jar / readout are three siblings now, which is what lets the phone stack\n  them in the order §2 asks for. Whole scene fits a 375 × 812 frame with the jar above the fold.\n\n### Verified, not assumed\n\n- **Frame budget is a non-issue at the cap.** The drift loop costs **0.091 ms/frame at 90 fireflies** —\n  0.5% of the 60 fps budget. The Observatory's worry was unfounded at 13 and is still unfounded at 90.\n  Compositing cost of 180 blended gradient circles is *not* measured here and stays unverified on real\n  hardware.\n- **Both inherited rules hold.** Position lives on `.ff`, animation on `.ff-a` (verified via computed\n  style); no firefly enters the shoulder or the neck (y stays inside 262–616).\n- **`prefers-reduced-motion` was exercised, not assumed** — forced through the real code path: drift\n  stops, positions still place correctly rather than snapping to the origin, flicker becomes a static\n  opacity, motes stop, the label still draws.\n- **All eight test files pass**, including the new 17.\n- **`.gitignore` swallowed a test file again.** `tools/tests/vault-groups.test.js` matched the\n  default-deny and needed its own allowlist line — the same trap as BACKSTAGE_PLAN §1, which is now the\n  third time. Test files are enumerated one by one in `.gitignore`; anyone adding one must add the line.\n\n### Still open after step 2\n\n- Nothing has been looked at on real hardware — only this machine's browser, same caveat the Observatory\n  carries.\n- Contrast has not been measured against WCAG; the etched label over a moving swarm is the risky one.\n- The jar's interior above the swarm is dead space at 13 notes and correctly full at 90. If §9 Q2 comes\n  back \"stay on `docs/`\", the jar wants to be shorter.\n\n## 8b. Step 3 — done 2026-09-10. What the morph decided.\n\nThe lid lifts, the swarm pours out through the mouth and spreads across the whole scene, the jar\ndemotes to a rim vignette and the shelf it stands on, and the names appear. Sealing runs it backwards.\nBoth directions verified at 13 and at a rehearsed 90, on desktop and on a 375 × 812 frame.\n\n### The refactor step 3 turned out to be\n\n**The swarm had to move into scene coordinates.** A jar drawn in its own 460 × 748 box cannot hold a\nswarm whose job is to fill the viewport. The svg's viewBox is now *the scene in CSS pixels* — 1 unit =\n1 px, so nothing distorts at any aspect — and the jar is a transformed group inside it. The swarm stays\nin jar units, which is what lets one clip path hold the fireflies in while sealed and one transform\nplace the whole jar. That was most of the work in this step; the animation on top of it was small.\n\n**The path out runs through the mouth.** The Observatory walked each node straight from A to B, and\ncopying that here sends every firefly through the side of the glass. Each node now travels a quadratic\nBézier whose control point sits just above the neck, so the swarm pours out of the opening and funnels\nback down into it when sealing. Three numbers per node, and it is the detail that makes the metaphor\nsurvive the transition.\n\n### Three bugs worth writing down\n\n- **Measuring the copy panel once was measuring it wrong.** The copy shrinks when the jar opens; the\n  ResizeObserver sees the scene change and re-runs the layout; the re-run measures the panels *in their\n  open geometry* and happily parks a firefly exactly where the panel is about to settle. The fix is to\n  keep the **union** of every box the panels have taken at the current scene size, so it does not matter\n  which state, or which frame mid-transition, the measurement lands in. This cost the most time in the\n  step and it looked like three different bugs before it looked like one.\n- **The nearest edge is often the wrong edge.** Pushing a firefly out of the copy panel's *left* side is\n  the shorter move, and it puts the firefly off-screen — after which the bounds clamp slides it straight\n  back under the panel, so it never escapes. Only edges the node can actually reach are considered now.\n- **A named firefly is as wide as its name, not as wide as its dot.** Clamping the dot inside the scene\n  still runs the label off the edge, and it is the orphans — pushed to the margins precisely because\n  nothing links them — that carry some of the longest titles in the vault.\n\n### Two things the scene decided\n\n- **How many names fit is a property of the room, not a constant.** Twenty-four labels read cleanly at\n  1440 × 900 and turn into overlapping mush at 375 wide. The count is set from the scene's area (floor of\n  six), and the readout says `10 of 90 named — the rest need more room` rather than quietly showing a\n  quarter of them. Same rule as the node cap: never a silent truncation.\n- **The ghost jar earns its keep at .12 opacity.** Higher and it reads as a jar hanging in the room;\n  gone entirely and you have left the metaphor. What survives OPEN is the faint shell, the two glass\n  marks, the vignette and the shelf — which is §2's \"rim vignette plus the base it's standing on\".\n\n### Verified, not assumed\n\n- **Round trip is clean.** After open → seal, every firefly is back inside the glass and the clip is\n  restored; after seal → open, zero labels sit behind a panel or off-screen, at 13 and at 90, on desktop\n  and phone.\n- **Frame budget is still a non-issue.** 0.196 ms per OPEN drift frame at 90 fireflies with the\n  panel-avoidance work included — 1.2% of the 60 fps budget. Compositing remains unmeasured.\n- **Reduced motion was exercised through the morph, not just at rest**: opening places the swarm\n  instantly, nothing drifts afterwards, positions are correct rather than snapped to the origin, and the\n  vignette and clip release still happen.\n- **The phone holds the whole scene with no page scroll** at 375 × 812, in both states.\n\n### A note for whoever verifies this next\n\nWith the browser pane hidden, `requestAnimationFrame` **and CSS transitions are frozen**. A DOM\nmeasurement taken after a `setTimeout` therefore reports the *pre-animation* state, and a computed\n`opacity` can sit mid-transition indefinitely. Screenshots pump frames. Three separate \"bugs\" in this\nstep were that artifact; the way to tell them apart is to disable the transition and re-read, or to\nscreenshot before measuring.\n\n### Still open after step 3\n\n- Real hardware, still. Everything here is one machine's browser.\n- Contrast has not been measured against WCAG. The label stroke (`paint-order: stroke`, 3px of near-black\n  under the text) was added for legibility over a moving swarm but has not been checked with a meter.\n- The lid, once lifted, parks at the top of the scene and is the only thing in OPEN that is not either a\n  note or the room. It reads fine; it has not been designed.\n\n## 8c. Definition of done — every push, no exceptions\n\nDecided by Pierce, 2026-09-10, after the first push of this work went to a branch and he had no way to\nsee any of it.\n\n**A step of this rebuild is not done when the code works, and not done when it is pushed. It is done\nwhen Pierce can see it on the live site in his pinned Chrome tab.** Four things, all of them, every\ntime:\n\n1. Merged to **`master`** — the only branch GitHub Pages builds (`source: {branch: master, path: /}`).\n   A branch push deploys nothing.\n2. The `pages build and deployment` run finished green.\n3. The change **confirmed on `https://piersonnorris.github.io` in the pinned Chrome tab** — looked at,\n   not assumed. Deploys lag the push by a minute or so, so a check run too early lies.\n4. The console clean on whichever page changed.\n\n**This check runs on every single push, not once at the end of a phase.** The two things it catches are\nboth already in this project's history: a branch that never reaches `master` (which is what happened on\nthe first push of steps 1–3), and a green Pages build serving a 404 because `.gitignore` silently ate\nthe file — [[BACKSTAGE — the vault, in public, with no gate]] §1, six pages pointing at a live 404 for a week. This rebuild is\nespecially exposed to the second one, because `.gitignore` here denies by default and every new file\nneeds an allowlist line; `vault-groups.test.js` already needed one.\n\nWorth stating plainly so nobody expects otherwise: **pinning the tab is Pierce's action, not something\nthe tooling can do.** Browser tab-strip state is outside what page automation can reach. What gets\nverified automatically is that the URL is live, correct and clean; the pinned tab is where he sees it.\n\nAlso standing rule 8 in [[ROADMAP — piersonnorris.com]] §4, which is where it applies to the whole site rather than\njust this rebuild.\n\n## 9. Open — need Pierce's call\n\n1. ~~**Reading surface.**~~ **Answered 2026-09-10: reading lives off-page at `/vault/`.** The jar\n   highlights, names and links out; it does not render note bodies. §2 state 3 and §8 step 4 revised.\n2. ~~**Which vault, and when.**~~ **Answered 2026-09-10: run `--report` first, and decide with the report\n   in hand.** Which is also what BACKSTAGE_PLAN §4 already required before a first publish. Steps 3–5\n   continue against the `docs/` snapshot meanwhile; step 6 waits on real mtimes (finding 3).\n\n   ```\n   node tools/obsidian-sync.js --vault \"<path to the Obsidian vault>\" --report\n   ```\n\n   Read it top to bottom. The three things to look for, given step 2's findings: **how many notes**\n   (finding 2 — under ~40 and the clusters idea has nothing to show), **how many folders** (finding 1 and\n   4 — a single-folder vault clusters into one blob and labels everything the same), and **whether the\n   dates look real** (finding 3 — if they are all within seconds of each other, mtime was lost and the\n   Ship's log stays blocked wherever we point this).\n3. ~~**The old page.**~~ **Answered 2026-09-10: the night sky stays, and this rebuild is parked.** Pierce asked for the fireflies to fly from the jars to the stars — an enhancement to the live R20 page — and was shown the fork explicitly: build it there, or push this rebuild forward instead. He chose the night sky. ROADMAP **R23** is that work.\n\n   This is a park, not a deletion. Steps 1–3 are real, tested code (`vault-groups.js` with 17 tests, `atlas-jar.js`, `jar.css`) and the scene is still judgeable at `vault/concepts/jar/`. **Nothing here is wasted if it is picked back up** — and §8b's central finding is what made R23 work at all: a jar drawn in its own box cannot hold a swarm whose job is to cross the page. `atlas-flight.js` is that lesson applied to the page that shipped.\n\n   Before restarting this, re-read §8a finding 2: at the `docs/` snapshot it is one cluster plus three orphans, and it wants the real vault (BACKSTAGE_PLAN P4) before it can be judged fairly.\n4. ~~**Nav.**~~ **Moot while Q3 is parked** — the page is still the night sky, so the label is still \"Atlas\". Reopen with this rebuild.\n","tags":["060b1c"],"created":"2026-09-15T21:06:44.122Z","updated":"2026-09-15T21:06:44.122Z"},{"path":"docs/ROADMAP.md","folder":"docs","title":"ROADMAP — piersonnorris.com","body":"# ROADMAP — piersonnorris.com\r\n\r\nThe shared task list for the three people building this site: **Pierce** (owner, decisions, secrets), **Claude** (gated tooling, docs, Drive research, build pipeline), and **ChatGPT** (public pages per [[BLUEPRINT — piersonnorris.com]], stock-update workflow). Read this first; it says what's in flight, what's next, and who owns it.\r\n\r\nSync rule: this file is canonical. The same list is mirrored to `TASKS.md` (Pierce's local Obsidian vault note, untracked) and seeded into `assets/js/taskboard.js → seed()`, which is what `/island/` reads — so a card added there shows up in three places automatically. (The tracker's private Projects board read the same seed until the tracker moved to its own repo on 2026-09-15.) When a task changes state, update this file and the mirror you touched — whoever commits next reconciles the third.\r\n\r\nUpdated: 2026-09-15 — **the portfolio tracker moved out** to its own private repo, `stock-trackers`, which is what lets it run with no password (§1, first entry). Earlier 2026-09-10 (later) — **R21 step 3 done**: the jar opens. The lid lifts, every firefly walks a quadratic Bézier out through the jar's *mouth* (a straight per-node walk sends them through the glass), the swarm spreads across the whole scene into its real clusters with names at readable size, and the jar demotes to a rim vignette plus the shelf it stands on; sealing runs it backwards and every firefly ends up back inside the glass. The step was mostly a coordinate refactor — the svg's viewBox is now the scene in CSS pixels with the jar as a transformed group, because a jar drawn in its own box cannot hold a swarm whose job is to fill the viewport. Three bugs worth knowing: measuring the copy panel once measures it wrong (it shrinks on open, the ResizeObserver re-runs the layout against the *new* geometry, and a firefly gets parked where the panel is about to be — fixed with a union of every box the panels have taken); the nearest edge to push a firefly out of is often the one with no room behind it; and a named firefly is as wide as its name, not its dot. How many names fit is now a function of scene area (24 on desktop, 10 on a phone) and the readout says how many were withheld. 0.196ms/frame at 90 fireflies in the open state. §8b. Earlier 2026-09-10 — **R21 steps 1–2 done**: `/atlas/` is being redesigned again, as **one great jar** — the vault as fireflies in a single mason jar on a shelf, direction B, picked by Pierce 2026-09-10. Step 2 (the sealed jar) is built and judgeable at full size on the local scratch route `vault/concepts/jar/`; `/atlas/` itself is **untouched** so the before-picture still runs. New `assets/js/vault-groups.js` (`PNVaultGroups.groups()` — seeded label-propagation clustering with labels derived from folder, tag or hub and never invented, 17 tests in `tools/tests/vault-groups.test.js`), `assets/js/atlas-jar.js` (`PNJar.mount()`) and `assets/css/jar.css`. Three findings, and they are about the data rather than the drawing: the `docs/` snapshot is **one** cluster plus 3 orphans, not the several the design needs; at a rehearsed 90 notes it resolves into 7 groups and becomes the page the plan describes; and every note's timestamp falls inside **8ms** of the publish run, so recency is inert and the Ship's log (step 6) is blocked on data, not design. All three argue for pointing this at the real vault — BACKSTAGE_PLAN P4. Drift costs 0.091ms/frame at 90 fireflies. See [[KNOWLEDGE ATLAS → ONE GREAT JAR]] §8a. Earlier 2026-09-09 — **R20 shipped**: `/atlas/` rebuilt as a night sky over the island — 25 stars (the notes, sized by degree), six firefly jars (the domains), and a telescope that draws the 30 real `[[wikilinks]]` between them; the three-pane shell it replaced moved to `assets/css/workspace.css`, which is what `/vault/` was actually using it for. New `tools/tests/atlas-links.test.js` pins the sky’s own link resolution to `PNGraphify`’s. Earlier 2026-09-07 (later) — R15 shipped: an Obsidian connectivity pass in two halves — a vault **graph view** (`PNGraphify`) on `/notes/` and the tracker’s Obsidian tab that counts orphans and unresolved `[[wikilinks]]` and lets you fix both from the picture, and `tools/obsidian-sync.js`, which reads a real Obsidian vault folder into an importable bundle (upserts by vault path, so re-syncing updates instead of duplicating) with a `build.js` seam to bake it into the encrypted tracker payload. See [[Obsidian connectivity — graph view + vault sync]]. Earlier 2026-09-07 — R9 moved from parked to mostly-done: a session-side Google Calendar connector now feeds real events into the encrypted tracker (one-way, snapshot + rebuild, not live two-way sync — that still needs an OAuth backend), the Projects tab grew a compact \"main calendar\" under the board, and the to-do board (private + `/island/`) now sorts by workflow stage then target date instead of raw insertion order. Previous update 2026-09-06 — R13/R14 shipped (chart sidebar + notes, then V2 customization: candles, RSI/MACD, sort, remembered prefs) with a new `charts.test.js`; §5 PN Tasks added (Pierce's open-questions queue + idea backlog); §3a: 14 of the 22-item UI backlog built same-day, 8 deliberately deferred (each says why).\r\n\r\n---\r\n\r\n## 1. Now — in flight\r\n\r\n### The portfolio tracker moved out — ✅ done 2026-09-15\r\nPierce: *\"remove the stock part of the git repo and create a new trpo called stock trackers this eliminates the need for a password.\"* The tracker left `/tools/tracker/` for its own **private** repo, `piersonnorris/stock-trackers`. A public repo was the only reason its holdings ever needed a PIN; in a private one access is GitHub's job, so `build.js` there builds open by default — no PIN, no lock screen, no `TRACKER_PASSWORD` — with `--sealed` kept as an opt-in.\r\n- **Moved there:** the page, template and build script; `charts.js`, `prices.js` and `updates.js` (no other page loaded them); copies of the shared modules the tracker uses; `charts.test.js` and the build-seam tests from `notes-graph.test.js`; `ASSET_TRACKER_SPEC.md`, `STOCK_CHART_PLAN.md`, `PORTFOLIO_CALENDAR_PLAN.md`, `PORTFOLIO_TASKBOARD_PLAN.md`; and `refresh-tracker.yml`, which had failed here every day for want of secrets and now skips until they are set. The open build's guards came along unchanged: no calendar events or note bodies in plaintext, four tabs hidden.\r\n- **Moved with it from this file:** R1, R2, R3, R5, R6, R9/R9b, R12, R13, R14, the front-desk half of R22, U10–U16, the tracker halves of R10 and R11, and the two chart items from §5. They live in `stock-trackers` `docs/ROADMAP.md`; R2 was rewritten there, since the password it waited on no longer exists.\r\n- **Removed from this site:** the Tracker link from every nav, the home page's tracker CTA and its tracker and stock-desk tiles, the Experience entry's link, the Modules card (seven modules → six), the Island bottle link, the `robots.txt` disallows and the `llms.txt` lines. Site tests moved from `tools/tracker/` to `tools/tests/`, and `/vault/`'s snapshot was regenerated without the stock docs. [[CONTENT — piersonnorris.com]] §7 has no holdings exception any more.\r\n- **Not changed:** this repo's git history still holds every tracker build, the open ones included. No rewrite.\r\n\r\n### R4. Content decisions — **Pierce** (blocking Claude + ChatGPT)\r\nOpen questions that block copy on the public pages (details in [[CONTENT — piersonnorris.com]] `[OPEN]` markers):\r\n1. Club title: still \"incoming president\" or president now? (five files change together)\r\n2. The LLC's legal name — docs disagree: True North **Services** vs **Maintenance** vs North Shore Services (see CONTENT.md §3a).\r\n3. Name Ryan Cravens publicly, or keep \"operations lead\"?\r\n4. Oasis → True North: one continuous story or two chapters?\r\n5. Keep Student Maintenance LLC (2025) on the public site?\r\n6. Keep the \"before Elon\" prologue?\r\n7. Anything to add for the thin 2024/2025 years?\r\n\r\n## 2. Next — ready to build\r\n\r\n### R7. Public pages M2/M3 — **ChatGPT** (copy from CONTENT.md only)\r\nAbout, Projects, Contact, Tools hub, 404, `sitemap.xml`, per-page OG tags. Blocked partly on R4. Rules: copy verbatim from [[CONTENT — piersonnorris.com]], `[OPEN]` means ask Pierce, no frameworks, no build steps, keep `llms.txt` in sync.\r\n\r\n### R19. Island to island — the work as a chart — 📝 planned 2026-09-08\r\nFull write-up in [[ISLAND TO ISLAND — the work, laid out as a chart]]; preview is direction 06 in `vault/concepts/index.html`. Direction 02's cartography, pointed at the work history instead of the vault — Pierce's call, the same session he picked 01 for the bottle.\r\n\r\n- **Every chapter is an island**, placed by date and sized by duration: the portfolio (2020 —), Oasis (May 2023), Elon (Aug 2023), True North (Apr 2026 —), Screencastify (Jun–Aug 2026), this site (2026 —). The ship sits at *today*. The incoming AI Club presidency is drawn as a **dashed outline** — charted, not yet made landfall.\r\n- **The lanes are the argument.** Not decoration and not `[[wikilinks]]`: each is labelled with what actually carried over — crew and door-to-door out of Oasis, *hustle → institution* into True North, the documented system into the internship, the workflow method back out onto this site. A vertical timeline can only show order; a chart shows what fed what.\r\n- **No new copy.** Islands, dates and inscriptions come from [[CONTENT — piersonnorris.com]] §3 verbatim, each island's *Takeaway:* line as its inscription, in one `voyage-data.js` file under the same rule as `atlas-data.js`.\r\n- **Nothing invented to fill the water.** Student Maintenance LLC is `[OPEN]` in §3, so it gets no island; Oasis → True North is drawn as two islands and a lane, which is true whether or not it turns out to be a rebrand.\r\n- **Placement is derived, not drawn** — longitude is time. An island placed by eye makes this a picture that drifts out of date the first time a chapter is added.\r\n- **The chart is an enhancement; the ordered list is the content.** It must degrade to the existing timeline under 760px, and serif-on-parchment has to clear 4.5:1 (U1).\r\n- **Blocked on Pierce:** the six lane labels are claims about his own career and need his words, not drafted ones.\r\n\r\n### R18. Ship-in-a-bottle Obsidian showcase — ✅ direction 01 picked 2026-09-08, phase A next\r\nFull write-up in [[SHIP IN A BOTTLE — the Obsidian showcase]]; live previews at `vault/concepts/index.html` (local only). Pierce's brief: *\"an amazing in-a-bottle Obsidian visual that looks impressive … one click … no gate … a crazy looking UI to showcase the use of Obsidian, no other purposes.\"* This finally closes out the Easter-egg idea in §5 that has been open since 2026-09-06 — the porthole's \"deliberately not built\" viewer is exactly what this is.\r\n\r\n- **Separation from the stock page — done.** The bottle porthole's second button was **Stock desk → `/tools/tracker/#stocknotes`**; that was the only place the two surfaces touched, and it is gone. The buttons are now `/vault/` and `/notes/`. The tracker keeps its own Obsidian tab, deliberately: those are *stock* notes about holdings and they belong next to the holdings. Standing rule from here: the showcase never links to the tracker, and the tracker never links to the showcase.\r\n- **Five directions**, each previewed as live CSS/SVG at real proportions rather than described: **01 Bottle Observatory** (the vault as a constellation inside the glass, camera flies through it), **02 Cartographer's Table** (notes as islands, links as inked shipping lanes, unresolved links as *hic sunt dracones*), **03 The Descent** (scroll is diving, depth is age), **04 Orrery in Glass** (domains as suns, orbital period is recency), **05 Vault OS** (phosphor terminal boots the vault, ASCII graph morphs into the real SVG one).\r\n- **Picked: 01, and the scope went up.** Pierce, same session: *\"i like 1 … before this starts add in to refine the visual and build it out because this is a whole home page ui update.\"* So the Observatory is **not a sub-page** — it becomes the home page's new front, which means \"looks impossible\" and \"loads fast, reads clearly, works on a phone\" both have to be true at once.\r\n- **Phase A — ✅ done 2026-09-08.** Comp at `vault/concepts/observatory/index.html`: full viewport, real geometry, real type, and **13 real notes / 16 real links read out of `vault-public.js`**, three states, two depths, desktop and phone. **Verdict: 01 holds up at full size**, so the re-pick branch is closed and Vault OS is off the table. Two findings changed the build:\r\n  - **\"Inside\" is not a zoom of \"sealed\" — it is a second composition.** Pushing the viewBox in inflates the nodes and leaves four notes of thirteen on screen: the vault disappears at the moment you arrive in it. Inside gets its own layout (notes re-laid across the viewport, labelled, glass demoted to a rim vignette). **So phase C is re-scoped from \"camera\" to \"layout morph\"** — every node travelling its own path between two layouts. Materially bigger than assumed, and free to discover now.\r\n  - **The desktop composition does not survive portrait.** Copy, bottle and Currently card all want the middle third. Phone is a *third* composition, not a reflow: the scene becomes a band with copy above and card below. Works, still the weakest state; standing the bottle upright is phase B.\r\n  - Settled while looking at it: the name sits **outside** the glass (the first comp put a 72px `h1` straight through the bottle); the bottle lies base-left/cork-right so the affordance points away from the headline; the Currently card survives bottom-right over the glass; **depth, not light/dark** — `site.css` has zero `prefers-color-scheme` rules, so that axis was wrong in the plan; nodes never enter the shoulder or neck; two marks make it read as glass, not ten.\r\n- **Phases B and C — ✅ done 2026-09-08.** `PNObservatory` is real code now: `assets/js/observatory.js` + `assets/css/observatory.css`, driven from the scratch route `vault/concepts/observatory/live/`, so phase F is a swap rather than a rewrite. Two full compositions (landscape lies the bottle on its side; portrait stands it on its base), each with its own viewBox, silhouette, node field, cork travel and furniture — an orientation flip rebuilds rather than reflows. **C shipped with B**: the morph turned out to be ~40 lines once both layouts existed, and the frame-rate worry was unfounded at 13 nodes and 17 edges, which makes D's node cap load-bearing rather than optional. Glass is three gradient stops and two marks (one travelling glint, one rim light); ambient life is shafts, caustics, bubbles, a bobbing ship and pulses running down the links — all opacity/transform/dashoffset, none touching layout. `prefers-reduced-motion` is a path, not a switch: the morph becomes an instant placement.\r\n  - **Three bugs worth remembering.** (1) `seedOf` used `h * 16777619`, which overflows 2^53 — JS drops the low bits, every seed came out a multiple of 4, and `seed % 4` picked colour 0 thirteen times out of thirteen. It looked deliberate, which is why it survived a screenshot; `Math.imul` fixes it. (2) **A CSS animation beats the SVG `transform` attribute** — drift and bob were on the same elements the script positions, so nodes snapped to the viewBox origin the instant their delay elapsed and the ship was already parked at `left:-75px`. Position and animation now live on separate nested groups; that is a rule for anything animating SVG here. (3) The ship stayed for the inside state and became a schooner moored in the constellation — it and the waterline are what you see *through* the glass, so they fade with it.\r\n  - **Not verified on real hardware**, only this machine's browser.\r\n- **Phase D — 📝 specced 2026-09-08, not built.** Two halves, the second one new (Pierce: *\"add a button to find groups for obsidian and labeling them\"*). **D1 — the node cap**: draw ~90 of N ranked by degree then recency, with an honest readout (\"showing 90 of 312\"), plus a largest-connected-component toggle; orphans stay *counted* even when hidden, because R15's whole argument was that hiding them flatters the vault. This is what makes R17 P4 safe. **D2 — \"Find groups\"**: label propagation over the  graph (~30 lines, no tuning knob to fake, seeded iteration order or it re-clusters on every load), each community tinted and hulled. **Labels are derived, never invented** — shared folder at ≥60%, else shared tag at ≥60%, else \"around <hub note>\", else it stays **\"unnamed group · 6 notes\"**. That last rule is the point: the tempting version reads the notes and writes a clever theme per blob, which would have a page whose entire premise is \"the structure is real\" inventing claims about Pierce's thinking that no data supports. Grouping is a toggle, off by default, and toggling off restores the exact previous positions. Pure  unit-tested like ; the drawing half left to the browser. Full spec:  §2a-0c.\r\n- **R19 is the home page's data layer, not a follow-up** (Pierce, 2026-09-08). Phase A left the six console tiles with nowhere to go; `voyage-data.js` becomes what the page reads below the fold. It must exist **by phase E**, since E is what replaces the tiles — which puts R19's lane labels on this pass's critical path.\r\n- **Ends of the line:** if C can't hold frame rate on a mid-range laptop it degrades to a cross-fade between the two layouts rather than a per-node morph, and the page is still good.\r\n- **No gate, on purpose.** Pierce's call: only he links to it, and it can be gated later in one commit — reversible in a way publishing content is not.\r\n- **The other three are shelved, not dead.** 02 became R19; 04 is worth revisiting if the site ever wants a second showpiece, since nothing about it overlaps with 01.\r\n\r\n### R24. The open jar spreads out, and the telescope shows everything — ✅ done 2026-09-11\r\nPierce: *\"make the jars have the stars spread out and the telascope should show all stars.\"* Two changes to `/atlas/`, and they turn out to depend on each other.\r\n\r\n**An open jar now lays its own stars across the whole sky.** The home layout packs each domain into a tight cluster around its sky anchor — the right picture for the whole constellation and the wrong one for reading a single domain. `atlas.css` had already admitted as much in a comment: *\"five labels all hanging below it would overlap\"*. Alternating labels above and below bought two of them; it does not buy six. Opening **Operations & leadership** put six names on top of each other.\r\n- **`layoutSpread()` measures the labels rather than modelling them,** and the first attempt is why. Pushing stars apart on an ellipse the size of one assumed 172px label left **nine overlapping pairs on `ops` alone**. Two reasons a model cannot see: these titles run from \"One path per lead\" to \"No form, no distribution without it\", so the pills vary from ~70px to 208px and one assumed width is wrong for nearly all of them; and the labels alternate above and below the star, so two stars at the same height may be 50px apart or exactly aligned.\r\n- **Rectangles, not circles.** Two pills collide only when they overlap on *both* axes, and the cheap way out is whichever axis needs the smaller shove. Pushing along a radius moves labels that were never going to touch.\r\n- **A label’s size does not depend on where its star is,** so one measurement per star is enough and the relaxation stays pure arithmetic after that. Only 2–6 stars are ever in it.\r\n- **Audited on all six jars: 0 overlapping labels, 0 labels off the sky** — at 1385×600, and again at 375×487.\r\n- **The morph is free.** Stars are positioned in percentages and `.star` transitions `left`/`top`, so swapping the position lookup under `place()` *is* the animation. There is no animation code behind it, which is why it cannot fall out of step with the fireflies.\r\n- **The fireflies aim where the star is going, not where it is.** The star moves for .62s while a flight takes 1.05–1.67s, so reading a live rect at launch would aim every firefly at a halfway house nobody ends up at. `starPoint()` computes the target from the layout instead.\r\n\r\n**The telescope shows all 25 stars, whatever jar is open.** It draws the links between lessons, and links cross domains — so filtering to one jar made it least useful at exactly the moment somebody had chosen a subject: most of the 30 lines had one end dimmed to 9%. Out telescope, out domain filter. A typed query still narrows, because that is someone asking for something specific rather than browsing.\r\n- **And everyone goes home for it.** One domain fanned across the sky *on top of* the other nineteen would be a worse picture than the one it replaced — the telescope’s job is the true shape. So spreading applies only while the telescope is stowed, which also means the lines never have to morph while anyone can see them.\r\n- **Names go quiet too:** 25 at once is a wall of text, so an open jar names its stars only while it is the only thing on screen.\r\n\r\n**A verification trap worth writing down.** The Browser pane was hidden for part of this, and a hidden pane **stops running layout**. Setting a star to `left:10%` and then `left:80%` returned `offsetLeft: 253` both times, and two completely different layout algorithms scored *identical* overlap counts (9/2/1/4/0/0) because both were measured against the same frozen geometry. `getBoundingClientRect()` is not trustworthy while the pane is hidden. What settled it was arithmetic: take the percentages the algorithm wrote, take the intrinsic label sizes (which survive the freeze, being position-independent), and compute the overlaps directly.\r\n\r\n### R23. The fireflies go to the stars — ✅ done 2026-09-10\r\nPierce: *\"for the obsidian UI lets enhanse it so the fireflys go to the stars and it makes you open the jars and also add the firefly page to the home page as a bottle make it more visualy impressive.\"*\r\n\r\n**What was wrong with the old jar-opening.** Pressing a jar did two things that looked unrelated. Three decorative motes puffed out of the lid — inside that jar's own `viewBox=\"0 0 76 112\"`, down in the sand — while somewhere else entirely, instantly, that domain's stars lit up. The fireflies never left the jar, so nothing connected the cause to the effect.\r\n\r\n**Now they make the trip.** New `assets/js/atlas-flight.js` (`PNFlight.mount/release/recall`) plus a `.flight` layer. Open a jar and the lid lifts, the glass empties, and **one firefly per note** rises out of the mouth on a quadratic Bézier, crosses the beach and the sky, and lands on its own star — which flares as it arrives. Seal the jar and they fly home and the glass fills again. The three old `.escape` motes are gone; they were a gesture at a journey rather than the journey.\r\n- **The layer hangs off `.shore`, not `.scene`, and that is the whole trick.** The page is `.shore > [.scene > .sky, .sand > .beach > #jars]` — the stars and the jars are in different sub-trees. `.scene` stops at the horizon (measured: bottom 843) and the jars start at 917, so a layer on `.scene` clips every flight before it begins. This is the same wall R21 hit and wrote down in `ONE_GREAT_JAR` §8b: *\"a jar drawn in its own box cannot hold a swarm whose job is to fill the viewport.\"*\r\n- **Endpoints are measured at launch, never cached**, so the field log taking 420px and reflowing the percentage-positioned sky cannot leave a flight path pointing at where a star used to be.\r\n- **CSS `offset-path`, not requestAnimationFrame.** The compositor interpolates the curve; the script sets two custom properties and gets out of the way. 25 fireflies is the entire vault, so this never needs R21's 90-node frame budget.\r\n- **The control point sits high and only a third of the way across**, so a swarm blooms up out of the mouth before it fans toward its own corner of the sky — a firefly that travels a symmetric arc reads as a bullet.\r\n- **Verified by scrubbing the timeline, not by eye.** Sampling one flight at 0/25/50/75/100% gives (380,974) → (352,847) → (281,601) → (235,483) → (222,456): the jar mouth to the star, arcing up and left, with the dot lit only between the lid and the landing.\r\n- **`prefers-reduced-motion`** skips the flight entirely in JS and kindles the stars immediately; the CSS block is belt and braces for anything already airborne when the setting changes.\r\n\r\n**The sky was left lit** (Pierce's call, against the two gating options offered). Opening a jar is the reward, not the price of admission — so an arriving firefly makes an already-burning star *flare* rather than igniting a dark one. Nothing about what the sky **means** changed.\r\n\r\n**The home-page bottle got both halves.**\r\n- **Fireflies in the glass, before you click.** The bottle was a still life — a ship and two waves, and nothing on it moved until you touched it. Seven deterministic motes now drift inside, clipped to the body with everything else. They pick up `--pnb-acc-hi` from whichever page they are mounted on, so they are Fern green on Home and blue on `/island/` for free.\r\n- **The porthole tells the same story as the atlas.** It used to be a flat dotted sketch that sat there breathing. A jar now stands on the ground, nine fireflies rise to the nine points of the constellation, and each point lights as its firefly lands — hub first, because every line runs to the hub and lighting it last leaves the drawing looking broken for most of the cycle. A line may not appear until **both** of its ends are alight.\r\n- **SMIL `<animateMotion>` here, CSS `offset-path` on `/atlas/`,** and the difference is deliberate: the porthole is `width:100%` with a scaling `viewBox`, so a path in user units follows the scaling for free. `atlas-flight.js` pays the re-measuring cost only because it crosses real page boxes.\r\n- **One shared 7s period.** Every delay is a fraction of it, so the fireflies, the points they light and the lines between them cannot drift out of step the way three independent loops would.\r\n- **The copy did not change and did not need to.** It is still a generated sketch, still not a graph of anyone's notes, and the porthole still says so. Only the telling changed.\r\n- **Three keyframes were deleted, each checked for other users first:** `pnb-breathe` (points now light on arrival), `pnb-sail` and `.pnb-boat` (the little boat crossed the floor where the jar now stands), and `pnb-flow` (the line animates inside `pnb-edgein`, which has to own the whole cycle).\r\n\r\n**A bug that was not one.** At 375px a star near the right edge hangs its name label to x=400 and `scrollWidth` reports 401 against a 375 client width. That reads like sideways scroll and is not: `body.atlas-page` already carries `overflow-x:hidden`, so `scrollTo(9999,0)` leaves `scrollX` at 0. A clip was added, measured, found to fix nothing, and removed again — it would only have trimmed edge star names. R20 had already handled it.\r\n\r\n**This answers `ONE_GREAT_JAR` §9 Q3: the night sky stays.** R21's one-great-jar rebuild is parked as a concept at `vault/concepts/jar/` — Pierce's call, made with the fork stated. The firefly-to-star flight is the night sky's own idea finally paid off, and polishing a page that was about to be replaced would have been the wrong trade.\r\n\r\n### R22. The studio, and the front desk — ✅ done 2026-09-10\r\nTwo pieces of one sitting, both Pierce's calls, both about what a visitor should be looking at.\r\n\r\n**R10 closed for `/notes/`: the Research studio direction, as a skin.** Pierce picked Version 3 from `notes/visual-options/` over Slate command center and Paper canvas, and picked the scope with it: *a skin, not a rebuild*. `assets/css/notes-studio.css` is the whole change, linked by `notes/index.html` alone. Not one line of `notes-ui.js` or `notes.css` moved.\r\n- **Why Studio and not Slate.** `/vault/` is already Slate (R17). The bottle's porthole offers `/vault/` and `/notes/` as two doors, and two doors onto the same picture is worse than two rooms. Studio is also the only one of the three drawn around **visible backlinks** — the thing this page most lacked, and the thing the porthole's own copy advertises.\r\n- **Ten custom properties do most of the work.** Every `.nv-*`, `.md` and `.pg-*` rule in `notes.css` reads `--acc`, `--ink`, `--bg` and friends, so overriding them on the mount point reskins the component without editing its stylesheet. Scoped to `#vault` and `.head`, never `<body>`: nav and footer stay brand Fern, because the green is the thread running through every page and the blue is this one room.\r\n- **The Linked pane is real and cost no markup.** `.nv-backlinks` is already the last child of the editor form, so grid placement pins it to a second column and everything else to the first — Studio's right-hand pane, out of the DOM that was already there. `:has()` gates it so a note with no backlinks does not leave a 208px hole; a browser without `:has()` gets today's bottom block, which is a fallback rather than a break.\r\n- **The three how-to panels came out** (Pierce). They pushed the vault ~595px down the page: you read about the tool before you could reach it. The data-loss warning — clearing site data really does delete the vault — moved into the lede rather than vanishing. The rest is in `notes/README.md` in more detail than a 13px panel ever carried.\r\n- **The palette was measured, not eyeballed.** Every ink/ground pair went through a contrast check before it was written down; the worst is `--dim` on the editor at **4.83:1** against a 4.5:1 floor. U1 is why that step is not optional.\r\n- **Two pre-existing bugs found while looking.** (1) At 375px, `.nv-side` and `.nv-main` rendered **573px wide inside a 339px `.nv-app`** — a grid item's automatic minimum is min-content, and the single-column breakpoint let `.nv-list` set a floor the track could not go below. Confirmed pre-existing by re-measuring with the skin disabled. `min-width:0` releases it; scoped here. The tracker's copy of the bug moved with the tracker to `stock-trackers`. (2) `.head::before`'s glow is inset `-25%`, so at 375px the document stretched to 442px and the whole page scrolled sideways. `overflow-x:clip`, the same fix `.atlas-head` and `.vault-head` already carry.\r\n- **Not done here:** the tracker's Obsidian tab. It left this repo with the tracker on 2026-09-15 and is tracked in `stock-trackers`.\r\n\r\n**The tracker's front desk stops showing the workshop** — the second half of this sitting moved with the tracker on 2026-09-15; its write-up now lives in `stock-trackers` `docs/ROADMAP.md`.\r\n\r\n### R20. Atlas as a night sky — `/atlas/` rebuilt — ✅ done 2026-09-09\r\nPierce asked for \"a better UI for atlas… jars labeled with top things in Obsidian… stars — this is the Obsidian nodes… when you press the telescope it shows the connectors… stars should be able to get pressed and show what they are… follow the island theme.\" Built as exactly that: `/island/` is the island at midday, `/atlas/` is now **the same island after dark**. Same sand, palms and waves; sun down, sky full of stars. The three-pane workspace R16 shipped is gone from this page — it was a good reading surface and a forgettable picture, and this page's whole argument is that the structure is worth looking at.\r\n- **Stars are the notes.** 25 `<button>` stars, sized by degree (the most-linked lesson is literally the brightest), coloured by domain. Layout is deterministic — a hashed seed per note id, a golden-angle spiral around a per-domain sky anchor, then ~150 passes of collision relaxation in aspect-corrected units — so the constellation is the *same shape* on every load and still spreads properly on a phone (measured: 39px minimum gap at 390px wide, 48px at desktop). The moon is a no-fly zone. Arrow keys walk star to star geometrically; every star is focusable and titled.\r\n- **Jars are the domains.** Six glass jars half-buried in the sand, one per top-level domain, each with a firefly per note (capped at 7) and a rope-tied paper tag carrying the label and count. Opening a jar lights that domain's stars, dims the rest to 9%, names them, and lets three fireflies drift out of the lid.\r\n- **The telescope draws the links.** Press it and 30 constellation lines draw themselves in on a staggered `stroke-dashoffset`; press again to stow it. Selecting a star traces *its own* lines even with the telescope stowed, so you are never lost.\r\n- **Pointing at a star does everything selecting it does, except open the panel** (Pierce, 2026-09-09: *\"make the stars more interactive for users to use\"*). Hover or keyboard-focus a star and it swells, its own lines light, its neighbours lift, the rest of the sky steps back to 38%, and a small **peek card** names it — domain, source, when it was learned, how many links, and \"press to open the field log\". The card is deliberately tiny (224×109) and goes to whichever side of the star has more empty sky, because a card thrown onto the crowded side covers the neighbours it just lit. Hover and selection share one code path — `state.hoverId || state.noteId` is the sky's single \"what is this pointing at\", so the two can never disagree.\r\n  - **The links are objects too.** Each line has an invisible 15px twin underneath it, live only while the telescope is out: hover a line and both lessons it joins light up and say their names.\r\n  - **The deck reaches the sky.** Hovering a title in the field notes, the ship's log, or a rope link in the open panel lights that star in the constellation — without a card, since the star may be scrolled out of view.\r\n  - **Also:** a ring pulses off a pressed star, clicking empty sky puts the sky down (closes the panel), and a touch gets the highlight but never the card — a tap there is a press, and a card would be in the way.\r\n  - **One bug found doing it:** hovering a title in the field notes and then moving to that same star showed no card. The id had not changed, so the \"did it change\" guard returned early and the card was never built. \"Is there a card up\" is now asked separately from \"did the note change\".\r\n- **The field log.** Pressing a star opens a parchment-on-wood panel — crumb, handwritten title, learned date, evidence chips, the body with clickable `[[wikilinks]]` that fly you to that star, and rope-tag lists of lines out / lines in. On desktop the shore is *padded*, not covered: the panel takes 420px and the percentage-positioned sky reflows into what's left, so the panel never hides the constellation it describes. Bottom sheet under 1080px. Esc closes; `#note-id` still deep-links, and now a `hashchange` listener makes a pasted link work while already on the page.\r\n- **Every line is still real.** The sky computes links itself (the page no longer loads `PNGraphify`), so a new test — `tools/tests/atlas-links.test.js` — pins the two implementations together: identical undirected pairs, identical unresolved count, plus unique ids/titles, every note in a declared domain, and nothing adrift. Verified failing on a typo'd `[[wikilink]]` before being accepted. Shape unchanged from R16: **25 stars, 30 lines, 2.4 lines per star, 0 adrift, 0 unresolved**.\r\n- **One stat was wrong and is now right.** R16's status bar divided links by notes and printed `1.2`, while this file has always said 2.4 per note. 2.4 is average *degree* — each undirected line counted at both ends — which is the number \"lines per star\" actually asks for. Fixed in the rebuild.\r\n- **Three bugs found on the way.** (1) The sky's telescope-on state class was `scope`, so `.scope svg` — the telescope button's own rule — also matched the constellation's `<svg>` and squashed it to 142px in the corner. Renamed `scoping`, with a comment saying why. (2) `vector-effect:non-scaling-stroke` (needed to keep strokes even under a stretched viewBox) makes the browser compute `stroke-dasharray` in screen space, which broke the `pathLength=\"1\"` draw-on and rendered every line dashed; the SVG now uses a 1:1 pixel viewBox kept in sync by a `ResizeObserver`. (3) The FNV hash seeding the layout barely moves its high bits when only the last character changes, so `dust0`…`dust95` came out as neat diagonal rows of specks instead of a starfield — fixed with a murmur3 finalizer.\r\n- **`/vault/` was linking `atlas.css` for the three-pane shell**, so rewriting this page would have broken that one. The shell moved to its own `assets/css/workspace.css` (its real owner — `/vault/` was already the only page reusing it), and `/vault/` now links that. Verified rendering unchanged: grid still `240px / 718px / 230px`, tabs, tree, inspector and status bar all intact.\r\n- **Reduced motion** kills every animation — twinkle, drift, waves, fireflies, the line draw and the panel slide. All six tracker test files still pass.\r\n- **Also:** `web` moved from `#45c26b` to teal and `study` from grey to a moonlit blue, because two greens next to each other in a starfield is not a legend.\r\n- **The \"How this page works\" paragraph is gone** (Pierce, 2026-09-09). The publication-boundary claim it carried — curated public file, not the vault, every claim traced to [[CONTENT — piersonnorris.com]], no client names or portfolio values — is unchanged and still enforced; it now lives only in [[CONTENT — piersonnorris.com]] §7 and the header comments of `atlas-data.js` and `atlas-ui.js`, not on the page. **If the atlas is ever linked somewhere that needs the boundary stated on the page itself, put a one-line version back** rather than re-adding the paragraph.\r\n\r\n### R17. Backstage — the vault in public, no gate — ✅ P0–P3 done 2026-09-08, P4 waiting on Pierce\r\nFull plan and current status in [[BACKSTAGE — the vault, in public, with no gate]]; the tool half is written up in [[Obsidian connectivity — graph view + vault sync]] §3. Pierce asked to \"view the website back end Obsidian, no gate to get in, and a good visual,\" and chose **the whole personal vault, ungated** over two narrower scopes.\r\n\r\n- **The gate was never the work.** `/notes/` has no server behind it — the vault is browser-local, PIN-derived AES, on one machine, so removing the PIN would publish nothing. Making a vault publicly viewable means *committing a snapshot of it into this repo* and building a reader. That is a one-way door, which is why the phases are cut where they are.\r\n- **Shipped:** `/vault/` (\"Backstage\"), read-only and ungated — folder tree, rendered Markdown, backlinks/outgoing/unresolved inspector, per-note table of contents, ⌘K palette over notes *and* headings, `#path/to/note.md` deep links, and Reader / Graph / Timeline. `/notes/` and its PIN are untouched: one is a writing surface, the other a reading surface.\r\n- **The publish pipe:** `tools/obsidian-sync.js` gained `--report`, `--public`, `--label`, `--prefix`, `--waive` and `--no-link-paths`. Default is publish; a note opts out with `publish: false`, a `#private` tag or a `nopublish/` folder. The scrubber **refuses and prints `path:line`** rather than silently redacting, and named terms come from a gitignored `private/.publish-blocklist`. 26 tests in `tools/tests/vault-publish.test.js`.\r\n- **New: a Markdown renderer.** `assets/js/markdown.js` (`PNMarkdown`), ~250 lines, no dependency, **escape-first** — every character is escaped before a tag is emitted, so raw HTML in a note is shown rather than executed. Headings, lists, task boxes, tables, fences, quotes, Obsidian callouts, wikilinks. Images are described, never fetched; `javascript:`/`data:` hrefs render as text. 24 tests.\r\n- **Visual:** Slate command center (Version 1 in `notes/visual-options/`) — built over the atlas's own three-pane shell (then `atlas.css`, now `workspace.css` — see R20) rather than a second copy of it. **Correction 2026-09-10:** this line used to say Slate here \"closes R10\". It did not. R10 is a ticket about `/notes/` and the tracker's Obsidian tab, and neither had been touched — §3 went on listing R10 as open, which is how the contradiction was caught. Applying a direction to Backstage closed Backstage's visual question and nothing else. R10 is now closed for `/notes/` by R22, which picked **Research studio** precisely *because* this page is Slate: the bottle's porthole offers both as doors, and two doors onto the same picture is the worse answer.\r\n- **What is actually published: the site's own `docs/` folder** — 11 notes already public in this repo. Deliberate: it exercises the whole pipe and makes the look judgeable *before* anything private ships. Pointing it at the real vault is one flag, and it waits on Pierce (§5, and [[BACKSTAGE — the vault, in public, with no gate]] §7).\r\n- **Two bugs found on the way.** (1) `atlas/index.html` was **gitignored and had never deployed** — the allowlist never got an `!/atlas/` pair, so R16 was linked from six pages and served a 404. Fixed, and `/vault/` allowlisted with it. (2) `PNGraphify` counted `[[links]]` written inside code spans, so a note documenting the syntax invented edges and inflated the unresolved count; fixed in the shared engine with tests, which took the `docs/` snapshot from 6 unresolved to 0. `/notes/` and `/atlas/` get the fix too.\r\n- **Also fixed:** `.atlas-head::before`'s glow bled 25% past the viewport and scrolled the page sideways at desktop widths. `overflow-x:clip` on both head sections.\r\n- **Not built on purpose:** tree virtualisation and the graph's largest-connected-component control. Eleven notes need neither; both become real work if P4 lands a few hundred.\r\n\r\n### R16. Knowledge atlas — `/atlas/` — ✅ done 2026-09-07, UI superseded by R20\r\nA public, third Obsidian-shaped surface, and the first one that is **not** a vault: `/atlas/` is a three-pane workspace over 25 curated notes on what Pierce has learned and where. Explorer (domains → notes, plus source and tag filters), reader (frontmatter block, evidence chips, inline `[[wikilinks]]`, backlinks inspector), and three views — Reader, **Graph**, Timeline. ⌘K focuses search; `#note-id` deep-links a note.\r\n\r\n- **The graph is real.** It is `PNGraphify` — the same engine `/notes/` uses — fed the atlas notes, so every edge is an actual `[[wikilink]]` in the copy above it, not a decorative drawing. Tag nodes are toggled off on mount (33 tags against 25 notes buries the structure); the engine's own toggle puts them back. Current shape: 25 notes, 30 links, 2.4 per note, **0 orphans, 0 unresolved** — the status bar and the graph's stat strip count links the same way (unique undirected pairs) so they cannot disagree on one page.\r\n- **Data, not a vault.** `assets/js/atlas-data.js` is a curated public file. Every claim traces to [[CONTENT — piersonnorris.com]] (§3 takeaways, §4 skills, §7 cleared numbers); note titles reuse Pierce's own documented wording where a takeaway exists. Nothing marked `[OPEN]` is asserted — Student Maintenance LLC is left out, and Oasis → True North is described as two dated chapters, not a rebrand. The §7 \"never on the site\" list is respected: no client names, portfolio values or former-employer figures.\r\n- **Wiring.** Nav entry on all six pages plus the tracker template, `sitemap.xml`, and an `llms.txt` line. Page-scoped `body.atlas-page main.wrap{max-width:1240px}` — a three-pane workspace does not fit the site's 900px reading column, and the header/footer keep the shared width.\r\n- **Still open:** the atlas is hand-maintained. When [[CONTENT — piersonnorris.com]] changes, `atlas-data.js` has to change with it — same rule as the taskboard seed.\r\n\r\n### R15. Obsidian connectivity — graph view + vault sync — ✅ done 2026-09-07\r\nTwo halves, one goal: make the vault’s links visible, and get the real vault in front of them. Full write-up in [[Obsidian connectivity — graph view + vault sync]].\r\n\r\n- **Graph view.** New `assets/js/notes-graph.js` (`PNGraphify`), behind a **Graph** button in the notes toolbar on both `/notes/` and the tracker’s Obsidian tab. Force-directed SVG with pan/zoom, node dragging, neighbourhood highlight and click-to-open. `build()` is a pure function and is unit-tested (`tools/tests/notes-graph.test.js`); the SVG half is not. It is deliberately about connectivity: **orphans** (no `[[wikilink]]` either way) and **unresolved links** (brackets pointing at notes that don’t exist) are two separate counts, and clicking an unresolved node writes that note. Sharing a `#tag` is drawn but is *not* counted as a link — conflating the two would flatter the vault.\r\n- **Vault sync.** New `tools/obsidian-sync.js` reads a real vault folder (skipping `.obsidian`/`.trash`), reuses the site’s own `PNVault.fromMarkdown` rather than reimplementing the parser, adds block-style YAML tags and aliases, and writes a `pn-vault-bundle` JSON to `private/notes/obsidian-vault.json` (gitignored) plus a connectivity report. It prints counts, never note text, unless you pass `--list`.\r\n- **Import upserts now.** `PNVault.importBundle()` matches on each note’s vault-relative path (also written into exported frontmatter as `source_path`), so re-running the sync after editing in Obsidian updates notes in place. The old `.md` path only ever *added* and skipped duplicates, which made repeat imports useless. Nothing is ever deleted.\r\n- **Baked route.** `build.js` gained `loadObsidianVault()` — drop the bundle at `private/tracker/obsidian-vault.json` and it seals into the payload as `obsidianVault`, exactly like `googleEvents`; the tracker’s Obsidian tab then offers a one-click merge. The merge is a button, never automatic.\r\n- **Known boundary, same as R9:** a snapshot, not live sync. The static site can’t read a folder on disk and CI can’t run the pull; refreshing means re-running the script on the machine that has the vault. Note text never enters the repo.\r\n- **Not verified in a browser:** the tracker’s merge banner — an open item that moved with the tracker to `stock-trackers` on 2026-09-15.\r\n\r\n### R8. Résumé PDF — ✅ done\r\n`/assets/resume/pierson-norris-resume.pdf` exists and the Experience page's button now links straight to it (`download` attribute) instead of the \"pending\" placeholder span.\r\n\r\n## 3. Later — parked on purpose\r\n\r\n- **R10. Obsidian visual style** — ✅ **`/notes/` done 2026-09-10** (Research studio, picked by Pierce — see R22). The tracker's Obsidian-tab half moved with the tracker to `stock-trackers` (2026-09-15).\r\n- **R11. Real launch hardening** — before promoting the site: run the BLUEPRINT §10 definition-of-done list, attach the `piersonnorris.com` domain (CNAME + absolute-URL sweep). The tracker template's demo-login question moved with the tracker to `stock-trackers` (2026-09-15).\r\n\r\n## 3a. UI polish backlog — audited 2026-09-06, 10/15 built here\r\n\r\nA full pass over the live site (Home, Experience, Notes, Tracker, Island), verified against the actual code rather than assumed — contrast ratios were measured, the favicon/skip-link/og:image gaps were grepped for, not guessed. They mirror the taskboard seed (`assets/js/taskboard.js`), so they show up on the public `/island/` page. 14 of the original 22 were built the same day; 8 stayed in `backlog` on purpose (see each item). The seven tracker-specific items, U10–U16, moved to `stock-trackers` with the tracker on 2026-09-15.\r\n\r\n**Accessibility & standards**\r\n- ✅ **U1 — Fix low-contrast meta text.** `--dim` measured 3.25:1 on `--bg`, below the 4.5:1 WCAG AA floor. Bumped to `#7a8392` (4.94:1).\r\n- ✅ **U2 — Restore visible focus rings.** Correction made *while* fixing this: input/textarea/select were already fine (a later `:focus-visible` rule with equal specificity already wins the outline back in the cascade) — only `.pnchart-graph .vg-node:focus` was genuinely broken, because its two-class selector (0,0,3,0) outranks the generic `[tabindex]:focus-visible` rule (0,0,2,0) regardless of source order. Added `.vg-node:focus-visible` with its own ring.\r\n- ✅ **U3 — Add a skip-to-content link.** Added to all five pages + 404.\r\n- ✅ **U4 — Ship the favicon.** A bold green \"P\" mark, dark rounded-square ground, as an SVG (`assets/img/favicon.svg`) linked from every `<head>`.\r\n- 🔲 **U5 — Complete Open Graph + og:image.** Still open: a compliant og:image needs a real raster (PNG/JPG) asset — Facebook/Twitter's crawlers don't reliably render SVG og:images, and this session has no image-generation tool. Needs a dedicated design pass.\r\n\r\n**Navigation & information architecture**\r\n- ✅ **U6 — Mobile nav menu** (built 2026-09-06, its own pass). Hamburger toggle (`assets/js/nav.js`) shared across all six pages, collapsing `.navlinks` under 760px into a push-down panel. Caught a real trap: Experience defines its own unconditional `.navlinks{display:flex}` which — equal specificity, later in the cascade than site.css — would have silently beaten a site.css-only fix, so it got its own matching override. Verified at true desktop width (toggle hidden) and mobile (opens, closes, auto-closes on link click) on all six pages including the real encrypted tracker.\r\n- 🔲 **U7 — One real icon set.** Deferred as a real design project; U15's empty-state icon is a first small step toward it.\r\n- ✅ **U8 — Wire the home feed to `updates.js`.** Re-scoped before building: `updates.js` tracks *website-engineering* changes, wrong subject matter for a career-facing homepage. Pinned an explicit sync-note comment to the real source of truth (the Experience page / CONTENT.md §3) instead.\r\n- 🔲 **U9 — Site-wide \"last updated\" stamp.** Re-scoped: `updates.js` is the wrong source here too, for the same reason as U8. Needs someone to decide what \"last updated\" should mean for a *public* page before anything gets built.\r\n\r\n**Tracker-specific UI** — U10–U16 moved to `stock-trackers` with the tracker on 2026-09-15.\r\n\r\n**Visual polish & consistency**\r\n- 🔲 **U17 — Consolidate duplicated component CSS.** Deferred: real but risky, needs its own careful pass rather than a rushed refactor.\r\n- ✅ **U18 — Subtle entrance animation** on Home's console tiles and the Experience timeline's year sections, via a shared `.enter-fade` keyframe respecting `prefers-reduced-motion`.\r\n- ✅ **U19 — Build the 404 page.**\r\n- ✅ **U20 — `theme-color` meta tag** on every page (island uses its own sky-blue).\r\n- 🔲 **U21 — One radius/shadow scale.** Actually counted it: 13 distinct border-radius values (2–16px) across the repo, not a small drift. Forcing them all onto the existing two tokens would visibly break small elements (a 3px nail dot doesn't want a 10px radius) — the real fix is a proper 4–5 step scale mapped by hand, not a blind find-replace. Deferred rather than done carelessly.\r\n- ✅ **U22 — Tie the Island page's look back to the brand** — added a soft ground shadow under the sign board so it reads as sitting in the sand.\r\n\r\n## 4. Standing rules (all three of us)\r\n\r\n1. **Secrets never in the repo.** No PIN, keys, tokens, holdings, or client data in tracked files, commit messages, or chat meant for the public build. `private/` is never allowlisted in `.gitignore`.\r\n2. **[[CONTENT — piersonnorris.com]] is the source of truth for public copy.** [[BLUEPRINT — piersonnorris.com]] for architecture. Don't invent facts; ask Pierce.\r\n3. **Plain HTML/CSS/JS.** No frameworks, npm dependencies, or build steps for the site itself (plain Node scripts under `tools/` are the one sanctioned tooling seam).\r\n4. **No holdings on this site.** The portfolio tracker has lived in the private `stock-trackers` repo since 2026-09-15. Portfolio values, share counts and holdings never go in this repo's pages, docs, `llms.txt`, the atlas or the `/vault/` snapshot.\r\n5. **Board ↔ Obsidian ↔ this file stay in sync** (see sync rule at top).\r\n6. **Questions for Pierce get logged, not just asked in chat.** Anything Claude or ChatGPT can't resolve without Pierce goes in **§5 PN Tasks → Open questions** so it survives past one session.\r\n7. **Local test loop:** run every `node tools/tests/*.test.js`, then click through the pages you touched.\r\n8. **A website task is not done until it is live and somebody has looked at it.** Working locally is not done. *Pushed* is not done either. Done means all four: merged to **`master`** (the only branch GitHub Pages builds — `source: {branch: master, path: /}`), the `pages build and deployment` run finished green, the change **seen on `https://piersonnorris.github.io` in the pinned Chrome tab**, and the console clean on the page that changed. Run that check **every single time anything is pushed**, not once at the end of a piece of work. Two failure modes this exists to catch, both of which have already happened here: a branch that never reaches `master` deploys nothing at all, and a Pages build can go green while the page 404s on a file `.gitignore` quietly ate — [[BACKSTAGE — the vault, in public, with no gate]] §1 is exactly that, six pages linking to a live 404 for a week. **If the pinned tab does not show it, the task is still open.**\r\n\r\n## 5. PN Tasks — Pierce's queue\r\n\r\nA dedicated inbox for anything that specifically needs Pierce: an open question Claude or ChatGPT can't resolve alone, or a feature idea that needs his call before anyone starts building. Standing rule (also in §4): **log it here as soon as it comes up — don't just ask in chat and let the answer evaporate.** Every entry — question or idea — carries a **Source** line (who raised it, where, when) so it's traceable later instead of a bare, unattributed bullet. Mirrors `TASKS.md` and the taskboard seed (`assets/js/taskboard.js`) like everything else in this file.\r\n\r\n### Open questions for Pierce\r\n\r\n- **Backstage (R17) — four calls before P4, the one irreversible step.** `/vault/` is live and ungated, but it is publishing the site's own `docs/` folder, not the personal vault. Before pointing it at the real one: run `node tools/obsidian-sync.js --vault \"<vault>\" --report`, read the list it prints, and answer these. (1) Is the first `--report` allowed to change the answer, if it turns out to be 300 short daily notes? (2) Client and colleague names appear in the vault — publish, initial, or omit? (3) Is the `docs/` snapshot a stepping stone, or is \"Backstage = how this site is built\" actually the better permanent page? (4) Seven top-level nav links is a lot — does `/vault/` group under Notes, or stand alone? Detail in [[BACKSTAGE — the vault, in public, with no gate]] §7.\r\n  **Source:** Claude, drafting the Backstage plan, 2026-09-08.\r\n\r\n### Idea backlog — needs Pierce's decision before anyone builds it\r\n\r\n- **Ship-in-a-bottle Easter egg** — 🔨 entrance built 2026-09-07, viewer still open. Pierce picked the placement himself (\"homepage and the to-do list\"), so the *entrance* half is built: `assets/js/bottle.js` + `assets/css/bottle.css` (`PNBottle.mount()`), one component mounted twice — under the Currently card on Home, and washed up in the sand on `/island/`, wearing each page's own palette via `--pnb-*` variables. Click it, the cork pops, the ship sails out, and a porthole dialog opens onto the vault's front door: a generated constellation sketch, a lamp saying whether a vault exists *on this device* (key-presence only — the same check the home console already does), and links into `/notes/` and the stock desk. Esc/scrim close it and focus returns to the bottle.\r\n  **Deliberately not built: the viewer itself.** The original idea had the ship open \"an embedded Obsidian-vault viewer.\" A *public* page cannot show one — the notes are encrypted behind a PIN and decrypt only in the owner's browser — so anything drawn there would have to be faked, which would misrepresent what the site does. The porthole says so in its own copy, and the real viewer is `/notes/`, one click away. Making *that* polished was **R10** — ✅ **done 2026-09-10**: Pierce picked **Research studio**, and a skin-only pass applied it. The bottle's second door now opens onto a workspace rather than a form. See R22.\r\n  **Source:** Pierce, Cowork chat with Claude, 2026-09-06 — his own words: \"reaching a visual obsidian with some [Easter] eggs on the website like a shipping [bottle] ... in addition to something like that on the home site too that looks small, but has impact.\" Placement decided by Pierce 2026-09-07.\r\n","tags":["jars"],"created":"2026-09-15T21:06:44.122Z","updated":"2026-09-15T21:06:44.122Z"},{"path":"docs/SETUP_CHECKLIST.md","folder":"docs","title":"SETUP CHECKLIST — work through bit by bit","body":"# SETUP CHECKLIST — work through bit by bit\n\nMirrors the Build HQ page. Check items off here (or there) and tell Claude/ChatGPT what's done.\n\n## Phase 1 — this week (no code, ~45 min)\n\n- [ ] Confirm (or pick) the GitHub username — it becomes `username.github.io`; `piersonnorris` is ideal if free\n- [ ] Install GitHub Desktop (desktop.github.com) + VS Code (code.visualstudio.com)\n- [ ] Pick the public contact email (norrispierce506@gmail.com or a cleaner alias)\n- [ ] LinkedIn: claim a custom URL (currently `/pierson-norris-634954246`) + add Screencastify and True North entries\n- [ ] ~~Choose the tracker password~~ — no longer needed: the tracker moved to its own private repo on 2026-09-15 and runs without one\n- [ ] React to the timeline (accent A/B/C, confirm chips, 2024/2025 gaps, the 2027 headline)\n\n## Phase 2 — Google Cloud (tracker prep, ~25 min; click-by-click in the private Drive spec) — belongs to the `stock-trackers` repo now\n\n- [ ] Create a Google Cloud project (e.g. `pierce-asset-tracking`) + enable the Google Sheets API\n- [ ] Create a service account + download its JSON key (treat like a password)\n- [ ] Share the Asset Tracking sheet with the service-account email — Viewer, notify unchecked\n\n## Phase 3 — when the build starts (with ChatGPT)\n\n- [ ] Create the repo `piersonnorris.github.io` (public) + enable Pages (Settings → Pages → main / root)\n- [ ] Add Actions secrets `GOOGLE_SERVICE_ACCOUNT_JSON` and `SHEET_ID` — on the private `stock-trackers` repo, not this one (`TRACKER_PASSWORD` is no longer needed)\n- [ ] ChatGPT Project: upload the `docs/` files, paste `HANDOFF_PROMPT.md`\n\n## Later — post-launch\n\n- [ ] Buy piersonnorris.com (~$10–12/yr) → CNAME + DNS to Pages; update absolute URLs (blueprint §7)\n- [ ] Privacy-friendly analytics: GoatCounter (free) or Plausible — one script tag\n- [ ] Headshot for About (fresh, or Claude screens the May 2026 Drive photo set)\n- [ ] Install the monthly-tab Apps Script on the sheet (written; in the private spec)\n- [ ] Refresh the resume PDF → `/assets/resume/pierson-norris-resume.pdf` (Claude drafts it from CONTENT.md once timeline gaps close)\n","tags":[],"created":"2026-09-15T21:06:44.122Z","updated":"2026-09-15T21:06:44.122Z"}]};
